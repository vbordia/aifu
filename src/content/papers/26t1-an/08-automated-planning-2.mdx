---
title: "Q8 — AUTOMATED PLANNING 2"
short: "AUTOMATED PLANNING 2"
order: 8
paper: "26t1-an"
question: 8
label: "AUTOMATED PLANNING 2"
description: "Automated Planning 2 question"
topics: ["AUTOMATED PLANNING 2"]
---

## The Question

The domain description of a Blocks World with a single one-armed robot is given below.

**PREDICATES**
- `armEmpty`: The arm is not holding any block, it is empty.
- `holding(X)`: The arm is holding X.
- `onTable(X)`: X is on the table.
- `clear(X)`: X has nothing above it, it is clear.
- `on(X,Y)`: X is directly placed on Y.

**OPERATORS**

`Pickup(X)`: pick up X from the table.
- Preconditions: `{ armEmpty, clear(X), onTable(X) }`
- Add Effects: `{ holding(X) }`
- Del Effects: `{ armEmpty, onTable(X) }`

`Putdown(X)`: place X on the table.
- Preconditions: `{ holding(X) }`
- Add Effects: `{ armEmpty, onTable(X) }`
- Del Effects: `{ holding(X) }`

`Unstack(X,Y)`: pick up X that is directly sitting on Y.
- Preconditions: `{ armEmpty, clear(X), on(X,Y) }`
- Add Effects: `{ clear(Y), holding(X) }`
- Del Effects: `{ armEmpty, on(X,Y) }`

`Stack(X,Y)`: place X directly on top of Y.
- Preconditions: `{ holding(X), clear(Y) }`
- Add Effects: `{ armEmpty, on(X,Y) }`
- Del Effects: `{ holding(X), clear(Y) }`

**NOTE**: variables X and Y must bind to different objects.

Consider the following planning problem.

**Start State**: `{ onTable(A), onTable(B), on(C,A), on(E,B), clear(C), clear(E), clear(D), holding(D) }`

**Goal Description**: `{ on(D,E), on(A,D) }`

Based on the above data, answer the given subquestions.


| # | Question | Official answer |
|---|----------|-----------------|
| 8.1 | Which of the following are applicable actions for FSSP? | **A**, **C**, **D** |
| 8.2 | Which of the following are relevant actions for BSSP? | **B**, **C** |
| 8.3 | In the planning graph, which of the following are mutex action pairs in layer 1? | **C**, **D** |
| 8.4 | In the planning graph, which of the following are mutex proposition pairs in layer 1? | **B**, **D** |
| 8.5 | In the planning graph, which of the following are applicable actions in layer 2? | **A**, **C**, **D** |
| 8.6 | For the given planning problem, does a plan exist? | **A** |
| 8.7 | For the given planning problem, the length of the plan found by GraphPlan is __________ . | `5` |

## The Topic in 60 Seconds

> Deep-dive: browse the [Concepts](/concepts) section for the relevant topic page.

## Solutions

**8.1** — Which of the following are applicable actions for FSSP?

Options:

| Option | |
|---|---|
| **A** | Putdown(D) |
| **B** | Stack(A,D) |
| **C** | Stack(D,E) |
| **D** | Stack(D,C) |
| **E** | Unstack(E,B) |
| **F** | Unstack(C,A) |

**Answer:** **A**, **C**, **D**

**8.2** — Which of the following are relevant actions for BSSP?

Options:

| Option | |
|---|---|
| **A** | Putdown(D) |
| **B** | Stack(A,D) |
| **C** | Stack(D,E) |
| **D** | Stack(D,C) |
| **E** | Unstack(E,B) |
| **F** | Unstack(C,A) |

**Answer:** **B**, **C**

**8.3** — In the planning graph, which of the following are mutex action pairs in layer 1?

Options:

| Option | |
|---|---|
| **A** | Stack(D,E) and nop for armEmpty |
| **B** | Stack(D,E) and nop for clear(C) |
| **C** | Stack(D,E) and nop for holding(D) |
| **D** | Stack(D,E) and Putdown(D) |

**Answer:** **C**, **D**

**8.4** — In the planning graph, which of the following are mutex proposition pairs in layer 1?

Options:

| Option | |
|---|---|
| **A** | armEmpty and clear(C) |
| **B** | armEmpty and holding(D) |
| **C** | on(D,E) and clear(C) |
| **D** | on(D,E) and onTable(D) |

**Answer:** **B**, **D**

**8.5** — In the planning graph, which of the following are applicable actions in layer 2?

Options:

| Option | |
|---|---|
| **A** | Pickup(D) |
| **B** | Putdown(C) |
| **C** | Unstack(E,B) |
| **D** | Unstack(C,A) |

**Answer:** **A**, **C**, **D**

**8.6** — For the given planning problem, does a plan exist?

Options:

| Option | |
|---|---|
| **A** | Yes |
| **B** | No |
| **C** | Cannot be determined |

**Answer:** **A**

**8.7** — For the given planning problem, the length of the plan found by GraphPlan is __________ .

**Answer:** `5`

## Tricks & Tips

<Accordion title="Exam method">
1. Read the question carefully — identify the algorithm or technique.
2. Trace step by step, showing your working.
3. Verify your answer against the question constraints.
</Accordion>
