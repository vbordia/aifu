---
title: "Q2 — SEARCH ALGORITHMS"
short: "SEARCH ALGORITHMS"
order: 2
paper: "26t1-an"
question: 2
label: "SEARCH ALGORITHMS"
description: "Search Algorithms question"
topics: ["SEARCH ALGORITHMS"]
---

## The Question

Answer the given subquestions.


| # | Question | Official answer |
|---|----------|-----------------|
| 2.1 | Under what cases does Depth First Iterative Deepening (DFID) guarantee to find the shortest path if one exists? | **C** |
| 2.2 | Given a finite state space with unit edge costs, and a heuristic function whose properties are not known, which of the following algorithms are suitable for ... | **B**, **C**, **D** |
| 2.3 | If w is set to a large value (tending to infinity) then wA* algorithm will __________ . | **A**, **D** |

## The Topic in 60 Seconds

> Deep-dive: browse the [Concepts](/concepts) section for the relevant topic page.

## Solutions

**2.1** — Under what cases does Depth First Iterative Deepening (DFID) guarantee to find the shortest path if one exists?

Options:

| Option | |
|---|---|
| **A** | When it inspects only new nodes. |
| **B** | When it inspects new as well as open nodes. |
| **C** | When it inspects new as well as closed nodes. |
| **D** | None of these |

**Answer:** **C**

**2.2** — Given a finite state space with unit edge costs, and a heuristic function whose properties are not known, which of the following algorithms are suitable for finding the optimal path?

Options:

| Option | |
|---|---|
| **A** | Depth First Search |
| **B** | Breadth First Search |
| **C** | Dijkstra's algorithm |
| **D** | Branch and Bound Search |
| **E** | A* |
| **F** | WA* for some w |
| **G** | Sparse Memory Graph Search (SMGS) |

**Answer:** **B**, **C**, **D**

**2.3** — If w is set to a large value (tending to infinity) then wA* algorithm will __________ .

Options:

| Option | |
|---|---|
| **A** | behave like Best-First Search |
| **B** | behave like Dijkstra's algorithm |
| **C** | guarantee an optimal path if h is admissible |
| **D** | not guarantee an optimal path even if h is admissible |

**Answer:** **A**, **D**

## Tricks & Tips

<Accordion title="Exam method">
1. Read the question carefully — identify the algorithm or technique.
2. Trace step by step, showing your working.
3. Verify your answer against the question constraints.
</Accordion>
