foreign so now we want to look at this algorithm called gold stack planning which strives to combine the best features of forward State space planning which is that it always constructs a valid plan and backward State space planning which has a lower branching Factor because it only looks at what goals need to be achieved essentially so for our state space planning searches for applicable actions from the start State constructs a plan from the start State the first action that it finds is the forks first action in the plan but It suffers from high branching since s is a full description of the full State essentially and all possible actions are considered which may not have anything to do with the goal that you are trying to achieve or which may not be relevant to the gold essentially so the the it produces a valid plan but the branching factor is high essentially backward stage was planning searches for Relevant actions from the goal description constructs a plan from the goal description onwards the first action that it finds is the last one in the plan because it's that's the last action it finds first but we saw It suffers from spurious sub goal problems and that leads to extra work because you have to check whether your plan your phone is valid or not and then go and look for another plan essentially goal stack planning is an algorithm designed to search in a goal directed backward manner thus taking advantage of lobe branching but to construct plants in a forward manner thus adding actions that are always applicable so that they will never be spurious the states that we will reach will be never be spurious it is a form of linear planning and what we mean by linear planning is that you take the goal and break up a compound goal into its individual's goals or individual sub goals and solve them one by one so if if there are two goals G one and G two in the goal you say OK first let me achieve G one then let me achieve G two and then hopefully we have the plan essentially so in that sense it is a linear plan first that one goal at a time essentially and solve them one by one severely producing a plan that is a sequence of actions essentially which will also be a linear plan essentially first I have achieving the actions for gold one then the actions for goal two it is best suited to domains where gold can be solved severely one at a time so if that is possible then gold stack planning is a very good algorithm to use for example if you want to cook three dishes you can cook them one by one essentially I mean you don't have to do them in parallel but you could that's a separate point but each coal can be solved independently easily the goalsack planning algorithm employs a stack and that is why it is called the word stack and the title is a gold stack planning in which goals are pushed in a backward fashion as these are the goals you want to achieve and so they are pushed one by one into the stack and which is why it is a linear planner it will first try to solve one goal then the next goal and so on but along with the goals we will also push actions that would be required to achieve them so let us look at the details we will start with a function which we will call as push set and remember that the goal is a set of goals that you want to achieve or a set of predicates that you want to be true in a given state so for example we said on a b and on C D and so on and so forth so a basic operation in Gold stack planning is push set G remember that J is a set it's a set of goals that you want to achieve G one G two up to g n we can think of it as a compound ah statement in logic which is a conjunct of G one G two and g n essentially so you first push the compound goal or the goal set then you push the individual goals G one G two and G one in some order we do not specify the order but the order in which you push them in with would be the order reverse of order in which you would solve them because if you remember that is the property of a stack the stack has a property that lasts in first out so the last goal that you push into the stack or push onto the stack will be the first one to be solved essentially so this of course gives us a little bit of a handle that we can do some heuristics here by ordering the goals in a particular order essentially um so that possibility exists or you can do some other form of reasoning to decide which goals to put push push in first so the algorithms begin with calling this push set function where G is a goal that you want to achieve is actually the reason that one has to insert the compound goal so remember that we set two things one we said that push this entire set of goals as a compound goal and then we push the individual goals G one G two G three and the reason why we have to also push this compound goal is to check whether after having solved the individual goals independently the compound goal has indeed with salt or not essentially and we will see with some examples when sometimes this happens and sometimes it may not happen essentially but for for goals which can be solved independently this the compound goal will be naturally assumed essentially so our algorithm has started with the push set of the original goal onto the stack and the algorithm will maintain the current state at all time essentially so at all time we should be able to see what is the current state initially it is a start state but if you were to add some actions in a forward manner then we will progress to some new status and we will see examples so we have pushed the goal State remember push set operation set push the compound set compound goal and put push the individual goals and then we start popping stuff from the stack essentially so when a goal is popped there are two possibilities one is that the individual goal G belongs to the state s which means it is true in the current state so nothing needs to be done you just pop it and leave it alone essentially but very often that may not be the case that it will not be part of the given State s so when the goal is not true what do you do you want to think of some action which will make that goal true and goldstack planning pushes a relevant action remember the definition of relevant action is that which will make that goal true in the final State eventually after it has pushed the relevant action a onto the stack then it pushes the preconditions of a onto the stack because those preconditions would have to be made true for that relevant action to become applicable so we are trying to transform the problem by saying OK this is the element action for achieving the goal but for that action to be applicable its preconditions must be true so push them onto the stack to see whether they are true or not essentially so what does push set do it pushes the preconditions of a as a compound goal and also as individual goals which are there in this peak set of preconditions in some order essentially so these are the two situations either the goal is true in this state and remember that we are always keeping track of the current state if it is true then we do not have to do anything if it is not true we have to push the relevant action and for that relevant action to be applicable we have to push its preconditions also so now that we have also pushed actions onto the stack and action may be probed at some point if an action a is popped then it must be applicable because its preconditions would be true as we will also see in an example and then it can be added to a plan and as we said the plan group Grows by adding actions at the end of a given plan initially there is an empty plan then we add the first section then we add the second action then we have the third action and so on and then we progress to the state S Prime which we have seen that the progress operation is there in which we add the effects of this action a and we delete the negative effects of that action age we already seen this progress operator earlier so the algorithm goal stack planning takes as input a planning problem we have not given a formal definition of a planning problem but basically a planning problem can be defined by defining the start State s the goal description G and the set of operators O So this triple in some sense defines a planning problem what is the start set what is the goal description and what are the actions available to you to you so it takes all this as inputs and identifies the given State as a as a state s which is a star state it says that the plan is empty to start with and the stack is also empty to start with essentially what it does is that the given goal is pushed onto the stack so this is a little bit like going backwards so that I want to achieve this goal what are the actions that will achieve this goal then as long as the stack is not empty so we started by pushing the initial goal to the stack and then we will start popping stuff and maybe adding new more stuff but as it goes along but as long as a stack is not empty we do the following if we pop whatever is at the top of the stack and let us say the thing is X if x is an action a then we just augment the plan by saying that the plan is now plan followed by this section e essentially so we are kind of concatenated you know or adding the action to the end of the plan essentially remember that initially we started with an empty plan then as and when we find actions we will add them to the plan and we will also progress over the same action so the same action that we are adding to the plan we will progress over that action to go to a new state s so at any given point we will maintain one state which is the current state after all the applicable actions have been progressed over essentially so if if x is in action then that is what we do if x is a compound goal and that goal is not true then we just push it back again essentially so remember what is the compound goal compound goal is the conjunct of all the individual pools if the conjunct is not true then we say go and solve it again essentially so we push set that set g into the stack what will push set do it will push the compound goal G as well as the individual constituents of that in some order foreign if what you have popped remember X is what you have popped if it is a individual goal G essentially and its not true in the state s remember that we are keeping track of the status at all times then we need to add an action somehow or find the relevant action and then we choose a relevant action a that achieves that goal G we have used this non deterministic choose operation huh just to simplify our description of the algorithm in practice when you are writing this this part of the program you will have to in incorporate backtracking so what do we mean by choose as element action so for example if you want to have a goal which says holding a holding Block B is to be true what is the relevant action for holding B its either that you pick up B from the table or you pick up or you unstack B from something else so there are various choices there and we are saying that somehow we are making the right choice non-reinistically in practice we may have to try both or many options and do backtracking if you can't find an element action then you can report failure then there is no way to achieve the plan if you can find the element action a then you push that a a action onto the stack and we also push the preconditions of a onto the stack essentially so this is what we keep doing repeatedly if we pop an action we added to the plan if you pop a compound goal we check whether it is true or not and if it is not true then we push it back onto the stack if we pop an individual goal and then again we check whether it is true in the state s or not if it is not State true in the state s we find the relevant action a and push that action onto the stack and push the preconditions of those actions onto the stack eventually when the stack becomes empty we will come out and return the plan so there are two exit points either we have found a plan which is constructed on as we go along or we can return failure so let us look at the this small problem that we had seen earlier which we saw that backwards chpase planning ran into problems because it created all these Fourier States let us see how gold stack planning ah who works with this tiny problem essentially so this is the problem that we are trying to solve a b c are on the table and we want a to be on B and B to B on C essentially that is a goal State goal set as two predicates or two goals on a b and on BC then we start pushing that into the stack so we first call this push the set operation so it first pushes this whole set of those two goals on a b and on b c and in some order so here I have chosen the correct order we will see what happens if we do not choose a collector so what is the why is this the correct order because if you look at the goal State you will have to first stack B on to C and only then you can stack a one to B so it is better to solve on b c first and then solve on a b which means you must push on a b first and push on BC second because that will be popped first essentially remember this is a stack so last in first out so so push the in the correct order you would push on BC in the last essentially then we will pop this from the stack as if you remember the algorithm this is not true remember that we had any given point we are working with a given State essentially so this is a state on VC is not true in the state so we push the relevant action what is the relevant action we have already seen it is stack B on C so we push that onto the stack and then we also push the preconditions of Stack BC so these are the preconditions what are the pre conditions for stack BC you must be holding B and there must be nothing else on top of C which means C must be clear and we push them in some order essentially then we so this is a cycle push something and pop the stack what do we pop we pop clear C from the stack that happens to be true in the state so we do not have to do anything then we POP Holding B from the stack that is not true essentially because in the given State the arm is empty its not holding any anything all a b c are on the table essentially what is the relevant action for holding the is to pick up B essentially so we push pickup B onto the stack so this is the situation ah we have popped some things and I have removed that from this stack here ah and the last thing that you added was pickup B which is an action to the stack and we also add the preconditions ah for this action pickup B what are the preconditions for pickup B see notice that already there is an action in somewhere sitting in the stack which has not yet been popped So the plan is still empty essentially the state is still the start State now we have said ok pick up B is the next action we are going to put on the stack so we have pushed two actions first we push stack PC why because that is required in the goal that you want B to be on C so we push stack BC but we found the stack b c could not be done and if you want to do stack BC eventually you have to first be holding B for which we said the action is pickup B so we have pushed pickup B onto the stack and we have push push the preconditions of pickup B which is on table B clear B and arm empty onto the stack essentially then we start popping again ah arm empty is true as you can see in the state ah clear B is also true in the given state on table B is also true in the given State then when we pop the compound goal everything is true so now things are clear for the first action so now you pop the first action pickup B next and that's the first action that goes into the plan essentially so remember that when we pop an action we add it to the plan so what is the plan the plan at this point looks like ah only one action is there which is pickup B and the state has changed because now that you have picked the B the state is that you are holding B and A and C are along the table essentially so after you have popped that action and we have added it to the plan what remains in the so if you remember after that above after this we pop this the next thing would be holding B that compound goal that we had so that is what we have here and we pop that compound goal holding B and clear C you can see holding B is true in the given State and clear C is also true in the given state so we pop it out and this is true so we are done with that action next we pop we do not have to do anything else so next we pop the action stack p C and added to the plan essentially so the new plan is now pick up B and stack B one C so as you can see the plan is built from the first action to the second action and so on and so forth so it grows from the start State essentially so now we have got a plan and now we are left with the goal on a b to be sold so this is a plan and that the goal that we have shown on a b we pop from the stack its not true so we push in a relevant action and its preconditions what is the relevant action that to make on a b true stack a b is a relevant action what are the preconditions for stack a b that you must be holding a and clear B and so you push the compound goal as well as the individual goals essentially so holding a you push and clear B you push ah without going into this details we can see that b is clear in the given State the given State you can see in this diagram here B is clear so that can get popped off without any problems holding a is not true so when we POP holding a what will we do we will push an action pickup a onto the stack essentially so I have not drawn the details here ah but it will find that it will next find the action pick up a to make holding a true so it will push the action pick up a it will push the preconditions for pickup a which is that a is on the table a is clear and the arm is empty all of which is true in the given state so it will now find this action pick up a to make holding a true essentially so once holding a is true you will see that holding a and pick up and and clear B will also be true so we can pop that out and then we can pop the stack action a b out and that is the last section that this algorithm finds and it terminates by finding a plan which as you can see is a simple plan there were three objects on the table a b and c it says first pick up B stack B onto C then pick up a and stack it onto B and that is a valid plan for the state that we have found essentially so next we will see what happens if you had chosen the wrong goal order so there are two goals in this goal in this in this problem to be achieved one is on a b and the other is on BC so we said that you push on a b first and then you push on BC and it turned out to be a nice order because you first achieved on VC and then you achieved on a b what if you had pushed them in the wrong order what if you had pushed on BC first and then you had pushed only B we will look at that in the next video