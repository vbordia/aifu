okay so now we have identified that there are actually two kinds of problems that we want to solve using our state space search one is called the configuration kind of problems where a state satisfying a description is sought essentially so the end queen's problem for example you want to say that give me a board position in which no queen attacks any other boot and there are many such problems so the end queen's problem the crossword puzzle the sudoku map coloring or sat we will look at sat a little bit satisfiability which is a very well known problem in computer science all these problems are configuration problems you just want is there a state which satisfies the goal description that we want the other kind of problems we will call as planning problems where the goal is either known explicitly or described and the path is sought so we saw that in the waterjet problem you can either specify that 440 is the goal or you can describe it by saying that some some jug should have four which means the four should occur somewhere in that list essentially so either known completely or described and the path is short for example you might in a real world you might say that i want to go and have some good chinese food or something like that you're not specifying which place you should go then your algorithm should find out where the chinese this thing and which is the closest one to you and give you a path of how to get to there so the goal may be either described or it may be known and we have seen that these are the problems river crossing problems root finding problems the rubik's cube in all these we want to know how to solve it or how to reach there or how to you know achieve what we want to achieve cooking is also like that essentially you know if you want to make some good sambar uh you want to know how to make it what are the steps that you should do if you could solve it if you could represent it as a state space problem okay so we want the paths in planning problems and we just don't want to return that yes we have reached the goal state we also want to find out how to get to the goals state from the start state which moves to make on the way or which states to visit on the v anyway so we have the state space which is modeled as a graph with each node being a state we need to keep track of parents of each node during search so that we can reconstruct the path when we have found the goal node we will see that there are different ways of doing things but essentially it's like keeping a parent pointer for every node essentially one way to do that is to modify the search space to store the entire path in the node itself so you must distinguish between the search space and the state space this is the state space is what is shown to you on the right of this slide the source space is what is generated by the search algorithm where every node has a certain representation you could very well keep the entire partial path as a representation and just test for the last node in the partial path and if that's a goal set you can represent the return the paths that's one way of doing things but another elegant way of doing is to not just remember the partial part or the path but just to remember the parent and we can do that by keeping node pairs instead of single nodes so that we have this idea of a node pair where we have the current node that we are interested in which we want to test for being the goal but we also know what was the parent node of this node how was it generated so if we were to modify our search space or search representation to include node pairs then you can see the every node in the search tree is now a pair use s has parent nil as you can see because that's the starting place but if you were to go from s to a then that is captured in the fact that s is the second element of that pair if you want to go from a to d from there for example that would be captured in the fact that the parent of a is parent of d is a that you came to d from a so every node has a second element which is the parent node once we do that and if this was the search tree that we saw recently when we had removed we have generated only new nodes and this was a tree explored by that now we have the mechanism to reconstruct the path from what we know where is the path it's somewhere inside the closed list somewhere in fact at the point when we find the path we see that the closed list has four node pairs in it and we can trace back the path from goal you can go to e because now we are representing node pairs so we know that e is the parent of g but e d is stored in the closed list as a pair so we know that from we came to e from d so we can look for a node d why look for d because d is here and we are looking for something which begins with d that can be done by looking through closed and then from d to a and from a to s from s you don't go anywhere because its parent is nil essentially and so in this way you can reconstruct the path and return the path and that's what we want in planning problems so in general the search algorithm will explore the search space it may try different options eventually it will find a path to the goal and that is what you should return as the output of the search algorithm now so far we have used a non-deterministic kind of a algorithm which says pick some node n from open non-deterministic in the sense that we have not specified which node to to pick essentially not not not non-returnism in the sense that somehow the algorithm knows what is the correct choice like for example when you study non-deterministic finite automata you assume there's an oracle which knows the correct choice not in that sense but in the sense that we have not specified which node to pick and the other thing we have done is that we have created open and closed as sets and we would like to modify them to list because eventually we have to represent things in some computable format which is and list is a convenient option here so we use a list data structures instead of a set and replace pick some node n from open and we will do this uniformly in our algorithm that we will always pick the node at the head of the list essentially in our search algorithm so that makes it easier to implement and instead of adding new nodes to open as a set we will insert them as a specified place in the list so that will determine which will come to the head and instance in that sense it will determine how the search algorithm will behave essentially so before we do that i should introduce some list notation because we will use this in the algorithms so the empty list is is is represented as square brackets and if we write something like s uh something is empty then it evaluates to true it can be used as a test in the algorithm so it's like a predicate whether a list is empty or not essentially and we will write it as follows if you write asking whether this is empty then obviously it will return false essentially and remember that our test will have so we have said that till open is empty or something like that we might want to say this list functions will allow us to do that then we can add a new element to a list and that will be done using this colon operator this is sometimes called as concatenation in the language like lisp we used to use the function cat but we will assume this list notation here and the colon would be used to add an element to the head of a list and we can write this as this as well the head of the list is the first element and the tail of the list is the remaining part of the list but we are talking about lists here essentially and list can be enclosed in square brackets so we just said that we can so this is the assignment operator here that we are we we started with list one here we added a new element to the head of the list and what we get is list 2 so this is like an assignment operator we can say head and tail instead of element and list it doesn't matter so these are equivalent if you write a list like this with one inside it's equivalent to saying that we are adding one to the empty list here let's see by the same operator which is represented by the colon essentially so if we talk about the element one you can say that it's the head of one so this head is a list function that many languages give us we will use this as a as a given function if you apply the head to a list it will return the first element to that which is equal to writing that head of one concatenated with the empty list if you want the tail of a list if you take tail of 1 then you remove 1 from that list and return the rest and that is equal to the empty list decision if you want to say tail of empty is true it is a test the system will return true to you remember the test will be used as tests in an algorithm essentially so a list like three two one list of three elements three followed by two followed by one can be written as three concatenated with two a list containing two and one or three concatenated with two concatenated with the list containing one all all these c elements explicitly concatenated with the empty list they all are equivalent representations for the list essentially so this will return true the three is the head of the list two one is the tail of the list it is a list 3 is an element but the tail head is an element but the tail is a list so if you want to extract 2 for example you can say take the tail of this list which will give you 2 1 then take the head of that and that will give you 2 if you want to extract one you can say head of tale of tale of this list containing three to one so these are list functions that we will assume as we write the algorithms essentially if you want to append two lists then we you use the plus plus operator here so you can take two lists and join them together to give you a new list if you append two empty lists you get the empty list if you append a list with an empty list either ways you get the same list back if you append a list containing ou 2 with r u n you will get a list containing the six letters there and if you were to do them in a different order you would get a different list which is run out instead of outrun and if you are if you have these two lists are you and if you extract the head of the first one which is r and then concatenate it with the rest of the list you will get r here and the rest of the list here out if you take the tail of the tail of the list that will give you a list containing n and append it to the tail of out which is o ut you will get this list containing nu all these are list functions that we will use as we uh use this algorithm the other kind of structures that we have one doubt yes uh sir we are using two kind of operations one is a colon and one is a double plus uh operation yeah so the colon operation is specifically used to add an element to element to a list okay this thing so the append operate operation takes two these things two lists and puts them together the colon operator and in list you used to call it as a cat function will take a element and add it to the head of the list and return the list which is made up of that so the nature of arguments is different recently so it it will treat it as so if i say something like this for example if i take a list of a b c and use the colon to add it to a list which has got d and e what will this give us this will give us this is not append this is the colon or the concatenate operator this will give us a list in which there is a list a b c which is the first element in a list and the second element is d and the third element is e so it treats the first argument as an element that's the difference between the two okay and just to confirm the head operation will give you the first element whatever is the first element in the list you will remove the head and give you the rest okay okay okay got it thank you so apart from list we will also use tuples essentially so for example we talked about the node pair we could represent the node pair as a tuple instead of a list the tuples are represented using this parenthesis this these curved brackets and you can see that there is a tuple of two elements one zero one one zero two and it is uh assigned to a tuple where a and b may be variables essentially so so if you're writing a program and you want to access the first element and the second element we'll say this tuple the first element is a the second element is b essentially you could also call it as a pair and then we can even assign that pair to a b by saying essentially but when we are looking at tuples we do not have to look at all the elements if you are only interested in the first element we can use underscore to represent an unknown element and or don't care element kind of thing we are saying we are interested in the first element and we'll call it a essentially we could also access the first element by using a function called first because the tuple is an ordered set uh the first built-in function will return the same thing as doing so this so we it will give us the element that is the first element of the tuple we could also identify the second element by only saying that i'm interested in the second element you can get the second element also by a built-in function called second essentially so this is for double so we can have triples and you know any number of elements in the tuple and you may have for example a tuple in which the first element is a number the second element is a string and the third element is again a number all that is allowed and if you want to access only the number 4 then you can specify it like this but you could also say it by saying give me the third element of this tuple or you can do a test so this is this is assignment and this is a test equality is a test the left arrow is an assignment we are saying that you know give me the third element i am going to call it c this is testing that the fourth element is the third element is four essentially it will return true or false so you can see that if you want to extract this element and this is part of the second element of the triple or tuple is a list and you want the first element of the list so the second element is extracted using the built-in function second and from that using the head we extract the first element of that list essentially so you can write head of second of this tuple if you write tail of second of this tuple what you will get is this list which is returned as a list essentially again you can see that the distinction between head and tail is shown here head is an element one zero one list the tail is a list containing one zero two and one zero three essentially we can do things like uh this is a head and this is the tail so we can say okay give me the head and give me the tail of that essentially list but of course the head and the tail argument apply to list structures the first and the second and the third apply to tuples so we have to be careful while writing your programs whether we are talking about tuples or whether you are talking about lists essentially it just makes it life is convenient so if you look at this particular example a refers to the first element refers to the head of the second element which is one zero one t refers to the tail of the second element which is this remaining part of the list and c refers to this element null essentially okay so we have all the machinery now for writing real search algorithms we have decided to move away from this non-determinism of pick some node from open to saying that pick always the node which is at the head of open we have not stated where the new nodes will be added and we will do that when we meet next which is to talk about these algorithms death force and breadth first search and then other search algorithms