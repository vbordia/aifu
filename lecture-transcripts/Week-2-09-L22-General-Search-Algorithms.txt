okay so let's begin with our study of general search algorithms and by this we mean that our goal is to find domain independent problem solving algorithms in a domain independent form essentially so the approach that we would take is that we will develop a set of algorithms for solving uh different kinds of problems so for example algorithm 1 algorithm 2 algorithm 3 is shown here and this is the kind of stuff we want to do when you're doing ai essentially but if you want to solve real world problems which means you are doing applications then what you need to do is to develop the user defined functions of the domain description and the move gen function and the goal test functions and then plug it into one of the generally available domain independent solutions so you have domain functions for domain one you may choose which algorithm you will use and so on so that's the only thing that you would do but the task of developing those algorithms is what we want to do in ai as we said that we are talking about a state space search which is implicit there's a set of states there's a start state and there's a goal state and somehow you want to reach the goal state we have also said that the state space search is implicit the graph is not given to you you have to generate the graph on the fly using the move gen function so what's only given to you is the start state and the goal state sometimes not even a goal set but just a goal description for example when we talked about the waterjet problem we identified there were three different goal states in which you could measure four liters so you could give a description or you could give a state we have seen that the move then function is a domain function that returns the neighbors of any given node n so you give a node as an input and it returns to you the neighbors and we expect that the user will develop this move gen function we have already mentioned that the maze for example can only be seen you can only see the immediate objects whatever the moves and function shows you are the options available to you physically in some sense we will see that there's a difference between local search and global search and local search it's as if you are in a person in a maze but in global search we will see that we can keep many options together in parallel then we have the goal test function which tells you whether a goal whether a state is a gold state or not and that should be given to us by the gold test function we will distinguish between in our representations uh two kinds of nodes one is the nodes that we have visited and tested for gold and so that's going to be the algorithm go to a node and see whether it's a gold node or not so the nodes that we have already visited we will call them scene nodes and we will put them in a list or a set called closed and the other nodes are the ones that we have generated by the moven function but not visited or not picked yet these are the candidate nodes and we will put them in this set or list called open which is a set of candidates so we have two kinds of nodes once we have visited once that we want to visit or we which we have generated but we have not yet visited so search is like a treasure hunt in in some sense then you are going from node to node looking to see if that's a gold node or not this approach is called generate and test you generate a node you traverse the space by generating new nodes you generate a node and test whether it's a goal or not essentially and test each node so this is how this general search algorithm will work and this this approach in general is called generate and test the key question is going to be which node to pick so as i said these purple nodes that you see on the graph these are nodes that are on open or the ones that are candidates which have been generated but have not been inspected which one should you pick next that will obviously decide how quickly you find the solution or in some cases even whether you will find the solution or not as you will see as we go along but the key question is going to be which node to pick so let us look at a tiny search problem which is a graph where you have this small number of nodes s is the start node g is the gold node and a b c d e are the other nodes that you can visit essentially in our diagrams we have represented the set of neighbors as a list returned by the move then function so if you look at s for example from s you can go to a or to b or to c that's what the move then function should give us and we have represented this as a list returned when we call the move gen function with the input as s if we give the input as c for example then you can see that you can from c you can either go to go back to the start node s or you can go to the gold node g so its output the movian function will return s and g to us so the moved in function implicitly represents the graph that we want to traverse and this is a problem that we want to solve now we must distinguish between the state space and the search space or the search tree in our case the search space is going to be your tree so what do we mean by that yes we are going to start from this the start stage so the root of our search space is or the search tree is s from s you can go to either a or to b or the c that is a choice that you have to make so remember that i said that the key thing is which node to pick from there let's say you pick any one of them you go to from s let's say you go to b essentially what can you do from b you look at the move gen function and it says you can go to either to s or to a or to d so in the state space those are the three neighbors of b but in the search space they will come as three successors of b so we are talking in the most general way right so you can go from s to b from b then you have a choice of going to either s to a or to d then again you have to choose and again you have to choose and that kind of stuff essentially that's a general idea that the search space is different from the state space source space is where the algorithm is searching for the gold node so here's a our first algorithm let's call it simple search algorithm we start by adding s to open here we are treating open as a set but as i said we will eventually treat it as a list so we can alternate between that and the algorithm is as follows while open is not empty and it's not going to be empty because initially you put s into that pick some node from open and let's call that node n pick some node n from open and remove it from open that okay we are going to look at this node then we said the algorithm is for generate and test so you pick the node n and now you are testing whether n is true if n is true then we return n okay we have found the gold node essentially otherwise what do we do we call the movement function to generate neighbors of n and add those neighbors to open essentially we'll keep doing that either till we exit here by returning n or if open has become empty by returning failure so we will see that in certain situations the way we write the algorithm it is possible that the open can become empty in particular if the state space is going to be finite then you should not visit you should eventually end up visiting all the nodes and then there will be nothing new to visit but the algorithm has to embody that we will see how that can be done but this is a simple algorithm which embodies this generate and test approach keep adding candidates to open keep picking some node from open and testing whether it's a goal or not and keep doing that till you have found the goal note or till your till open has become empty in the graph that we do the search tree that we drew it did not look like it's going to become empty because you could go into cycles essentially and that's a problem with uh search space the simple search algorithm one that we said and this could be the graph explored by this algorithm remember that this is a non-deterministic algorithm you said pick some node from open so this could be one such run of this algorithm you start with s you go to a then you go to s then you go to b then you go to s as shown on the left hand side with the colored nodes essentially on the right hand side we have seen how the open if you are maintaining it as a list will be maintained you start with s you remove s from open you add its neighbors abc then you remove a from open here for example and then you add its neighbors which are which will be sbd and so on so you keep removing a node from open and testing it for gold of course and keep adding it to open but as you can see here this program is going into cycles it's going from s to a from a to s from s to b from b to s so it's you know kind of stuck in this this part of the search space essentially and as you can see open is growing and going it's never going to be empty and it's also possible that this algorithm will never find the gold node it could just keep going into the cycle forever that's a problem how do we address that one simple way of doing that is to maintain another set and we have already mentioned it before called closed essentially so closed will be the repository of scene nodes essentially so this algorithm is just a modification of simple search one except that we have a new set being maintained here called the close initially it is empty because we have not seen them but we again pick the node from open remove it from open and add it to closed that's the difference that we are doing here again the same thing test whether n is the goal or not if it is the goal then you return n otherwise again call move gen of the neighbors of n but from the neighbors remember it is a set we remove anything that is already in close decision so we are not going to generate those nodes again which we have already visited basically and we have a new algorithm so what will this algorithm do so when a node is picked from open it is added to cloud closed and and if a new node is on close it is not added again to open so that's what we are doing here how does this algorithm perform for the same problem it does not add nodes on close to open so the search space the search space is smaller than the search space for simple search one so that's how it is happening you can see for example when we generated the children of a you went from s to a then you picked a and you children of a then that s that could have been there because from a you can go back to s is not added because now we have this test that before adding check whether it's on close or not and because s is on close already and this is the closed list so since s is here it's not added to a so this algorithm will explore a much smaller space and you can see that in this example it would have found the goal node essentially so from s to a a to b b to d every time it goes only to a newer node at termination the search tree still has some extra copies of b and d and c as you can see so b is here but you have already seen b and supposing we have not found the gold node essentially for some reason then it would have gone back and tested to test it for b again maybe why should we add this node b when it is already present in the closed list we can avoid doing that and later we will see whether there is hidden cost there or not but that's a possibility so let's assume that we will add only the new nodes so whenever we generate the neighbors of a node n called by move gen function we will not select nodes which are already on open or already on closed then you can see that the search space generated is even smaller now because b has already generated b is already on open and b is a neighbor of a but now we are saying we will not add b to the child of a so the only child of a that we will add is d why is that because s is already on closed we won't add s as a child of a remember that a has three children s b and d s is already enclosed so we won't add s as a child of a b is already on open so we will not add b as a child of a so the only child we will add is d and in this manner this search will go forward and find a solution so this is a different search space so what we are trying to emphasize here is that for the same state space depending on what algorithm that you are using it will generate it will explode a different search tree or different search space here each node is added exactly once in the search tree now one problem with this is you wanted to go to the gold node g but this algorithm is returning ed so we we said that you do a goal test and if it is yes then it only returns that node n essentially so it will only give you the node essentially so if you look at for example the the man goat cabbage and lion problem it will say yes you can reach the other side it doesn't tell you how to reach the other side so that's what's missing from this this thing it it should return the paths to the goal node what it wants what you want is the solution you want is the sequence of states that will take you to the goal node the doubt is if there is no edge between e and g in the same presented state spacecraft and will the symbols search to will fail simple search is not guaranteed to okay simple search to will succeed because we see what will simple search do it'll start with s add abc to open then it will go to a let us see and then it will add d as a add dn it also add b again because we are only looking for closed in simple search two then it'll add b and d and then e and then it'll essentially after is it has visited a and b and d and e it'll pick c from open at some point and then it'll find the path