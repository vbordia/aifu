foreign Minimax we saw algorithm Alpha Beta And we saw algorithm SSS stock so let us run them on some randomly generated game trees and see how they perform on the same ah graph essentially so let us say we take this randomly generated game graph and the leaf values will have randomly generated evaluation function values and we will see how the three algorithms perform on the same graph so we are running the Minimax algorithm first so remember Minimax does a complete sweep of this entire game Tree in that first fashion and you can see that that is what is happening here it is going down the leftmost branch here so let's speed up the animation a little bit so that we can finish it faster so this will not do any pruning at all this is simply brute force it searches the entire game key there are small values of nodes which are written here I am not sure whether they are visible but in any case that is this is a path that of the game that this algorithm has found so let us now run alpha beta on the same game tree and you will see that it will not search the entire tree but only a part of the tree so you can see that the black are the ones that this algorithm has searched and it has done some amount of pruning but it has found the same game path that Minimax found so Alpha Beta thus give us the same value except that it searches faster because it does some pruning essentially and then but Alpha Beta also is is line in the sense that in the sense that it always searches from left to right the third algorithm we saw which was SS star has a sense of direction so let us run that I will slow it down a bit and then we can see it again so here it is first generating the clusters so maybe a little bit faster would help and you can see that this is the search tree that this algorithm is going to explore essentially and when we compare the three we will see that SS star does the minimal amount of search again it has found the same game three this is the search tree as explored by SSS star this was the search tree as explored by ah Alpha Beta they look a bit similar in this example and this is the search lead that was explored by Minimax which explored the entire game tree so let us look at another example to get more of a feel of this ah we will start with Minimax again we will choose denser graph so this is bhaskaran's idea of drawing the graph with the root node in the center so it is a tree with a root node in the center and the leaf nodes are all along the boundary essentially so ah let us run Minimax first but as you know this will search the entire tree so we can do this a bit faster and it will find some Minimax value and it has found that this is a game that will be played over this game tree let us record that and let us learn alpha beta on the same problem so bit slower Maybe so you will see that at some point it will start doing pruning it is skipping some part of the tree and exploring only some part so let us speed it up a bit I think and you can see that towards the end it's skipped a large part of the tree it also found the same game path so that we already know that the performance of all three algorithms is that they always find the same Minimax value all three are correct algorithms let us look at SSS star so as you know SSH Star first first builds the Clusters and then it explores the rest of the game tree so now it is exploding it has finished building the Clusters now it is exploding the tree and we will see that it is focused more on this part of the tree and eventually we will terminate with a smaller part of the game three explode so the so the Minimax as we know explored the complete Game tree and found the Minimax value which happens to be 59 in this case and its coming down from the node Leaf node which is at the end of this path which is shown in red essentially Alpha Beta also finds the same value 59 from the same node but it has explored ah less of the game tree as compared to Minimax algorithm and SS star being a best first version has explored even fewer nodes so if you look at the bottom of the screen you will see that there are certain nodes for example here which have been seen by Alpha Beta but those nodes which are there on the bottom and bottom left are not explode by SSS stock so this is Alpha Beta and this is a system the game tree that is found the the Minimax value that they have found and the paths that they have found is the same it's just that in order Minimax such as the entire tree Alpha Beta searches a much smaller part of the tree and SSS Star Search is even more smaller part of the tree so I hope that gives you an idea of how these three algorithms perform and basically the idea is the mode that they can prune the more they can do further look ahead so they can look ahead little bit deeper than for example Alpha Beta can look ahead more than Minimax and SSS star can look at more than Alpha Beta given the same time ok so we are done with that