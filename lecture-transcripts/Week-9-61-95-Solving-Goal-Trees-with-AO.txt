foreign trees which are also called gold trees and we have said that we can associate some heuristic values with nodes which estimate the costs are solving that node so now let's look at this algorithm which is also a well-known algorithm called AO star algorithm a stands for and an O starts for all ah which is used for solving gold trees again the fact that we have used the star here says that under certain conditions you will always find the optimal decomposition or optimal solution the problem so let's look at the algorithm first with a small example ah at any point the algorithm maintains the graph that is generated so far this is as opposed to the open and the closed list that earlier algorithms were generating this one works with the graph itself every choice point in the graph has a marker at every choice Point U Mark which is the best choice known to you as known at that point of time essentially so the algorithm follows the marked path leading to a set of live nodes and it will refine one of those life nodes by expanding it in some way so it has this forward fees again ah you should try to compare it with algorithm SSS stuff which also had a similar flavor essentially the forward space and the backward face in algorithm aostar once it has expanded a life node it backs up the cost of the best hyper Arc and propagates its upwards which is the backward face essentially if the best choice leads to a solid node then the parent is also labeled solved essentially so if there is a node here and let's say there are two choices and if let's say for argument's sake it's just an or node and if this is a solid node which we can represent by a double circle and if this is also the best choice then we can label the parent to the solved noise instantly so that is the in the backward piece and the algorithm will terminate when the root is labeled solved or if there is no solution to the problem essentially again this is similar to how SSS are terminated essentially so let us look at the behavior of a u o star this was ah given to us by martelly and Mountain Ali 1978 it's also mentioned in Nielsen's book in 1980 and has the following cycle as we have just outlined starting at the root Traverse a graph along the marked paths till the algorithm which is a set of Unsolved nodes U or live nodes U pick some node from that and let's call it n and refine it further propagate the revised estimate of n up via all its ancestors so if they it had more than one ancestor remember we are talking about a graph here propagate the revised estimate up if for a node all and successors along the marked path are marked solved then Mark that node also solved so we just saw an example so it does not have to be just an or successor it can be an and successor so if let's say now we are talking about and nodes and we have this is the node n and we have defined it and we have found that there are two ways both of them are and arcs if one of them at some point happens to be solved so we that's what you said if a node if for a node all and successors so these are the and successes are marked solved and of course if this is also the better choice then you mark this also solved essentially so this similar to what we saw in the previous slide if a node has all edges emanating from it and the cheapest successor is marked solved then Mark the notice all we have already seen that terminate when the root node is marked as solved essentially so this this solved label also propagates from Leaf nodes upwards and eventually if it propagates to ah the root node then we say that the algorithm can terminate and remember that it only propagates when the best choices are marked sold essentially that means a node like n here will be marked solved only if the better choices are marked solved so if the I one of the and part and tree is solved then will the algorithm go to the next part next sub entry yeah so this is if you remember the SSR algorithm let's say this one is solved and this one is solved and this is the better choice which in SSS star would have meant that it has come to the head of the queue essentially and if it was a Min node for example then we knew we said that there is some cutoff which happens for the other this thing so in this case also it is a similar phenomena if if this two children are sold and the other better choice plus the condition that I have not mentioned which is similar to a star is that the heuristic function must underestimate the value of the this so if the value of this heuristic value of This is 40 for example and this is 30 for example and let's say The Edge cost is 10 and 10 and this is a solid node let us say this is 30 and this is 30. and again it costs are 10 and 10. so this branch the right branch has a cost of 30 plus 30 plus 10 plus 10 which is 80. the left branch has a cost for T plus 30 plus 10 plus 10 which is 90. but this 90 is an estimated cost and you and our condition that we have I haven't stated yet but we will state it later is that if the estimates are underestimates that means this 90 can only go up essentially when we refine those nodes earlier so it can never be better than 80 and therefore you can label this n as solved which is very similar to what was happening as a system them so here is an example of how the algorithm operates so let us say we have a gold node and we have some estimate H is equal to 45 for that we Define that gold node and it has two possibilities either A or B and C as we had started let's say the costs are as shown that the cost of solving a is 42 the cost of solving B is 22 the cost of estimated cost of solving C are 24 and the H costs are let us say 4 and 3 into respectively which is the next node that you should expand next that's the question if you are doing a star like an algorithm you would always pick the node with the least cost so if you are doing best first for example then you won't even look at the backwards you will only look forward and then you would say B is the best Note but the the fact that b is the lowest cost of 22 is misleading because B is part of a solution whose cost is 22 plus 24 which is uh 46 plus 3 plus 2 which makes it 51 whereas the left hand side a node is 42 plus 4 which is 46 and this is a better option and so it's not the heuristic value which determines which node to expand next it's the backed up values for every option that we see so in so this star that I have drawn ah all this exclusion that I have drawn over 46 says that this is a better choice and go along that direction so note that the choices between the two values 46 and 51 are not the heuristic values of the individual nodes so this is where we are 46 and 51 and we refine the one with 46 and after node a has been refined let us say this is a situation that it has two two choices d e is one choice and F G is another choice so these are and nodes so I have just written them like this and again we compute the the values for them and we see that the right choice is has a value of 45 and it is marked with the next node so what are the markers the markers will be saying that at G you go along this direction at a you go along this direction and then the set u that we set of live nodes will be f and g so one of them will be refined next essentially also notice that as we progress the estimated cost can go up so this has gone up from 46 to 49 essentially so if you compute this value then you will see that this 22 plus 18 is 40 plus 5 is 45 plus 4 is 49. so the cost can get you revised as you go along so that was a flavor of the algorithm and now let's look at the algorithm itself it takes the start node as a or the gold node which is a goal node as an argument and it takes some number which we call as futility and this is as as given to us by the authors of the algorithm which is a cost Beyond which we are not willing to go essentially we are saying that if the cost is going to go higher than futility then we are not going to solve this problem and what does it do so in the forward phase it says you add the start node to the graph G so G will stand for the graph here and compute h of start so we assume we have a hue stick function and we say that this value of solved start is false to begin with and then we go into this while loop half of it is shown here the forward face and the backward phase I will show in the next slide so this whole thing is a while while loop which includes the forward face and the backward face and till when do we stay inside this while loop as long as the start node or the root node is not solved which means the labels all is false the predicates all says is it solved or not solved and it says false it's not solved so we are treating it as a predicated and as long as its estimated cost is less than what we are willing to pay which is futility then in the forward phase it trace the mark paths in G as we just saw to a set of unexpanded nodes which we called as U speak some node n from this set u which one should we pick that's a matter of um efficiency a little bit um at one level it does not matter which node you pick because you have to solve all of them they are part of some and this thing but at a different level if you pick the most expensive one first then you are likely to know whether that is feasible or not earlier essentially so from that point if you we can pick the most expensive note but in general we will say the algorithm says that pick some node from this set u generate the successors of n so we assume that we have some way of generating successes if there are no successors then say that there are no solution that for this problem then also check for looping as I said that there can be Loops that you can transform problem a into problem B and it's possible to transform B back to a so avoid doing that sort of stuff so that this no looping if there are any looping members in the children you will move them that's here and then for each child s belonging to the children remember children are the successors of n add s to the graph and compute its heuristic value if s is a primitive node which means it is a solved node then say that solved s is true essentially and label its all in other words all right so having done this now we go to the backward face which is the this thing so let at any given point ah n b the set of modified nodes the children of the expanded nodes are the ones that will come in here to start with while m is not empty this set of nodes which are modified so we have we are wondering whether to propagate this change up or not essentially remove the deepest node from M so remember that M may have nodes at different levels in the graph as this propagation is happening and remove the deepest of them compute the best cost of D from its children essentially so that we have seen already that if two children have two different costs you mark the base path so so compute the best cost and mark the best option at DS marked essentially then we have already said that if it all nodes connected through the marked arcs are labeled solved then say that D is also solved essentially now if D has changed how will D change either it will get a new estimated cost and or it will get a new label which is solved or not if anything has happened then add all parents of D to the set M so this set M basically is kind of you know bubbles up the graph and till some point essentially if at some point it a solid node reaches the root node then we will terminate actually which is what we see that if solve start is equal to true then return the marked subgraph from the start node essentially else you return null so this is in general the aostr algorithm and here on this slide um we have just put the whole algorithm in one place so as you saw there was the initialization phase and then there is this Loop so let us say this is a loop and then in the loop there was a forward face followed by a backward face in the forward face you follow the mark path pick a node Define it and then you go to the backward face you adjust the estimates of different nodes starting from the bottom up remember that ah starting from the lowest nodes from the set which are changing and percolate the changes up essentially and eventually if this happens with the and the root node that the solid label has gone to the root then we know that we have found the solution essentially ok so as I said this label star on aostar stands for the fact that it is admissible and by this we have already said earlier that admissibility means that the algorithm will find the optimal solution if there is a solution so like a star the algorithm aoster is also admissible it finds the least cost solution and it does that when the following condition is true that the heuristic function underestimates the actual cost this was the case in a star as well and the reasoning is similar essentially so what is you can see that this algorithm is also fits into our general scheme of Hero 6 search algorithms which say that we find the best looking partial solution till the best solution is fully refined so till the two conditions it must be fully refined and it must be best because if you were kind of let's say implementing this by maintaining a priority queue or something then it should come to the head of the priority queue the explanation is the same as we said for aoster we will not give a formal proof here but we will just do this informally that as long as the partial solutions that exist in the graph essentially what do you mean by partial Solutions then there are certain sub graphs which whose label who who which have not gone to the solved level essentially so this there are some sub graphs which have not gone to the Salt level but maybe there is one sub graph which has gone to the Salt level which means it's there is once one partial solution which is completely refined but as long as the partial Solutions are looking better it will keep exploring them so you have let us say five options A B C D E A is fully refined b c d e are not fully refined they are partial if any of them has a estimated cost which is lower than the cost of a the algorithm will not pick a it will pick one of those whichever has the lower value so as long as the partial Solutions are looking better it will keep exploring them the same thing happened in all the earlier algorithms that we saw for example branch and bound ah for tsp if you remember and if they are underestimated so the partial Solutions and that's where the day comes in are underestimated if their cost is underestimated and it is worse than a refined solution they can never become better so we saw this argument informally is that because they their costs are under underestimate underestimations ah when you get refine them they will actually go up essentially and if this this partial cost is itself worse than a fully refined solution then there is no point in exploring the partial solution again so this is just a repeat of the same argument that we have been making uh for administ algorithms which are admissible which are the three algorithms which are admissible that we have seen one is uh branch and bound then a star and its variations also of course and now AO star essentially and of course the fourth one is SSS stuff for which is the best first approach to solving a game tree all of them follow the same you might say a design pattern and all of them have the same conditions on estimation or similar conditions on estimation and all of them when they pick a defined solution they terminate and we can argue that they terminate with an optimal solution so again we illustrate this by trying to vary the cost of edges so we will look at one graph ah one problem graph with and the only thing we will vary is the cost of the edges and what we will see is how that affects the way that the algorithm searches that curve so we will do that next