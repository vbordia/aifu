foreign we are finished looking at that first search breakfast search and dfid these are known as blind search algorithms because they do not have a sense of direction irrespective of where the goal is they always search the space in the same manner today we move on to heroistic search which has a sense of direction and we will see how that is acquired so this was the basic approach to depth first search ah you always extract a node from the head of open and then it tests whether it is a goal or not and other if it is not the goal then you call the move joint function and after doing some checks like removing scene nodes we add them back to the head of the list as a result of which the search behaved in a particular fashion which is which we call as depth first search I think and we saw that what happens is that it dives into the search space till it reaches so dead end or something and then it backtracks a little bit and again Dives back into it essentially the red note here is the gold node and you can see that it is not paying any attention to that the other algorithm we searched looked at was bath first search in which the new nodes are added to the tail of open making it behave like a queue and the consequent behavior of search was that it stuck as close to the source as possible and we saw that this also resulted in the fact that it would always find the shortest path essentially whenever it found a path so these algorithms are known as blind or uninformed such methods both the first search and the first search are oblivious of the goal irrespective of where the goal is in the state space both the algorithms set out on the same predetermined trajectories every time essentially what is needed is a search algorithm with a sense of direction it should be in some way aware of which side the goal lies essentially and try to head in that direction so let us first look at something from nature which is the pull of gravity so this is the scene from lahul and spiti where you can see a stream flowing down a Mountainside essentially the gradient of a mountain side gives a sense of direction to the water flowing down a stream essentially water always takes the steepest gradient descent path so you can see that here its following this path and eventually merging into a liver below that we can simulate the sense of direction in search algorithms and that is what we would like to do imagine a physical agent wanting to reach the bottom of a valley essentially and let us say that the physical agent is blindfolded so you cannot see have a global view of the the space what you can do is that you test the landscape by taking a step in each Direction and choose the direction which seems to be going down the most or the steepest gradient essentially the question that we would have to of course address is what would Define the landscape that we are traversing our search algorithm is we assume that it is traversing some kind of a landscape but how is this gradient defined essentially so this is what we will look at in this session so here is a example of how this gravity pull pulls the influences the movement so imagine that you there is a stone that is rolling down a two dimensional landscape ah and you assume that this Stone has mass so that it can feel gravity and it is pulled by gravity but it has no momentum so it cannot overshoot or something like that it does not have any momentum so where will it finally rest essentially so as you can imagine if there is no momentum and there is only the pull of gravity then the stone will roll down this path and come to rest in a place from where all gradients are pointing upwards and there is no gradient pointing downwards one can say that this is what it is doing it is testing the surroundings and then follows the steepest gradient path essentially it is also an example of local search it looks only at its immediate neighborhood essentially we will see how this manifests itself into a search algorithm essentially but this idea of steepest gradient descent is one of the approaches that we will ah study in the next few videos so we have this idea of a heuristic function essentially testing the neighborhood and following the steepest gradient identifies which of the neighbors is the lowest or closest to the bottom of the valley so this notion of closeness to the goal is what we are trying to be capturing the idea of a heuristic function is that it takes a state as input or a node as input and computes a number which is an estimate of the distance of that node to the goal essentially if you could do that if you could have an estimate for every node in the search space as to how close it is to the goal then that could be used to guide search then the search algorithm can pick a node from open which has a lowest heuristic value physically typically we call the heuristic function as h of n and typically it is a user defined function because it depends upon the domain that you are operating on and we will look at the example of a few heuristic functions very shortly so there are three functions that a user is expected to provide to a general purpose search algorithm we have already seen the move gen function which kind of navigates this space and the gold test function which helps it to stop navigating and now we have a heuristic function h of n which estimates the distance to the golden 16. so traditionally we do not mention the goal in the heuristic function we just read it as a function of one node but in practice of course it will have to look at the goal as well now since the heuristic value of the goal is going to be zero we can say that effectively we are trying to minimize ah the heuristic value so you can see some analogy now between a ball rolling down a hill and a search algorithm following the gradient towards the lowest point in that surface essentially so the Helix function estimates the distance to the goal now if we had a perfect heuristic function which would be like an oracle which would be able to tell you that what is the length of the pass to the gold then of course our job would be very simple and the search would get guided without any looking this way or that way essentially but in practice what happens is that such perfect heuristic functions are hard to find and that is why we have to end up with some kind of an estimate essentially so if we have such a heuristic function how will we modify our source algorithm we will call this algorithm as best first search so instead of depth first or breath first you are saying that look open the best node on open next how do we Implement that as before we pick the node from head of open as before we test for gold and if it is not the goal we generate the new nodes but now the first in the new nodes are inserted into open which we can assume is sorted on the heuristic value essentially so it does not go to the head of the open it does not go to the tail of the open we assume that the open is sorted on heuristic value the lowest heuristic value is at the head and we have sorted it in increasing order and the highest is at the tail and depending on the heuristic value of the new nodes generated we will insert them in an appropriate place in the open and essentially so with this small modification we have changed our Behavior to be a little bit more informed the behavior of our search algorithm essentially in practice of course we do not keep a sorted list we will maintain open as a priority queue so this is a change that we are making in our depth first search algorithm as you can see here We have replaced the name depth first search by best first search everything else is the same in this algorithm except that this line shown in red is replaced by the line below that shown in blue essentially what is the line shown in red it says that you take the new nodes and add it to the head of tail of open because you have removed that head of open what we are saying now is you do that but in addition you also sort the open list essentially um so that means this best nodes will always come to the head of the open essentially instead of doing this thing which is less sufficient we might say ah first sort the new nodes and then merge them into the tail of open because sorting is an expensive operation and merging is a linear time operation but as I said earlier in practice open is maintained as a priority queues actually we are still using the same terminology of calling our constituents of open and closed as node pairs but in practice we will have to mod modify them into triples because we want to include the heuristic value with every node essentially so this is how the new algorithm looks so everything else is different except that now we have a triple instead of having a node and its parent we have a node and its parent and its heuristic value essentially and we assume that we can manipulate this triple everything else is the same because we extract ah the we take the node from head of open we extract its first element and we can do that using that built-in function first we get n then we do the same thing we will test whether it is a goal or not if it is a goal we reconstruct the path and if it is not then we generate the children and make the new nodes and add them this thing here we have written it as again you append the new pairs to tail and then sort it so as I said in practice you would want to do it as a priority queue this is only for efficiency reasons correctness why is this is fine so let us look at how do we Define heuristic functions essentially this is an problem that we had seen the eight puzzle earlier so if if you recall ah ah in the start node ah so there there is a blank at the top and either the two can we move to the left or six can be moved to the right or eight can be moved up essentially so three moves are possible as shown on the right hand side and then so on and so forth and eventually we have some gold which is described on the right part of the screen how do we Define a heuristic function for this problem remember that heuristic functions are going to be domain specific and we would expect the users to Define them for us our job is to write the high level algorithms which will in which you can plug in any problem and then search will take over so the question we are asking is of the three successors that the start node has which of the three is closest to the gold node essentially so here is one heuristic function ah we will call it h one for every any node n it simply counts the number of tiles out of place essentially this is a bit like Hamming distance essentially you just see which which tile is in place which tile is not in place and add up for all the eight tiles a second function which you will call as a Manhattan distance will measure will for each Style it'll sum up the distance to its destination essentially so ah for example ah if you are looking at this tile three in this in this middle state then you can see that it needs to move one step to the right and two steps to up up so it is three moves away from its destination that is the Manhattan distance from its current location to its destinations remember the destination is in the top corner here so in Manhattan distance we add up these distances in Hamming distance we simply count how many are out of places so here is a state that we are looking at ah you can see that the Hamming distance is 2 why is that because as you can see one is in place two is in place 7 is not in place four and five and six are in place eight is in place and three is not in place so only tiles seven and three are out of place so that is why the Hamming distance is two essentially and if you look at the distances of how much three and seven need to travels three needs to make ah two moves to go to its correct position and seven needs to make four moves to come to its correct position so this magnetic distance is six so these are the two heistic functions that we have defined essentially now in this given start State there are two moves possible either you move the tile four down or you move the tile eight to the left essentially what are the heuristic values of these if you compute them you will see that their respective values are three and seven in both the cases essentially what does this mean it means that both the states that we are generating appear to be further from the goal than the starting state but of course we do not have any choice we can either move 4 or we can move eight so we will have to choose between one of them but this is what I mean by saying that heuristic functions are not perfect essentially and remember that we were trying to kind of create an analogy with gradient descent and we wanted to go to a state where the heuristic value was 0 but in both these with both these functions ah we see that the starting state is a local minimum in the sense that all the neighbors are higher than that and gradient descent would have stopped there itself physically so we need something more than simple gradient descent so either move increases the estimated distance to the gold remember these are all estimated distances and what do we mean by distance here we mean the number of moves that you need to solve the heat puzzle from the given state if you go to this website which whose link I have given below you will you can do this eight percent online essentially and they have two kinds of representations one is a picture and the other is the number that we just saw essentially it does not matter essentially you have to get them into the right place what I want to emphasize here is the fact that when we look at these things we can we are not thinking in terms of similarity instead of distance essentially so you want eventually the goal State the state which is most is closest to the gold state is what we are seeking essentially of course but this is the same example that we saw in the last slide except that instead of numbers we have ah this picture of this dog here essentially and you can think of distance function as an inverse of similarity in some sense of course you have to normalize it to one because we want maximum similarity to be one and minimum similarity to be zero but they are in some sense inverses of each other essentially and what you are trying to do is to is to move through this search space towards states which are more similar to the gold node essentially and Visually you can see that the start State here is more similar to the goal than the two states that we can go from there essentially and that is what we mean by saying that ah the heuristic function is not perfect and if you have been solving the Rubik's Cube then you would probably to be thinking in terms of similarity how how similar is my proposed move taking you closer to the goal stage and one of the gold States could be that every phase has one unique color so here's another problem ah this is geographical route finding ah I have drawn this on a small grid where ah every vertical grid is let us say one kilometer or something like that ah and the horizontal grid and the start state is on the top right and the goal set is on the bottom left and if you can see the graph then that is a load connectivity for this particular place you can imagine that the bluish or cyan line is a river which is flowing through the city essentially what can we use as a heuristic Function One function is simply the distance you just compute the euclidean distance between all the candidate States and the gold state and choose the one which has the lowest essentially so we have three successors of the start State and you can see that one of them has the lowest heuristic function so the best first search algorithm that we will see or this gradient descent algorithm that we are looking at will move to this node with a value of 7.21 you can also use the notion of a Manhattan distance again here so basically you count how many hops you are going in the vertical or the horizontal Direction going from one state to another and you see that now you get round numbers here so for example the same state which had seven point something now you move one two three four five six seven eight nine ten steps you are ten steps away from the goal if you were going in a grid essentially so it is called Manhattan distance because Manhattan is a city where the roads are very nicely laid out as grids essentially of course in our graph they are not great but we can still use magnetic distance as a heuristic function so the algorithm that we are looking at is going to be or we have looked at is the best first search and how does that operate in this graph I have shown the state of search when the algorithm terminates the numbers that you see in red for example the first noded of it opens is the value with the one with the value 10 then the second node it opens is with a value 7 because that of all the candidates that 7 appears to be closest to the gold node essentially then it goes to ah the third node which has a value seven then it rise eight value seven but you can see that from there there are no paths going directly to the gold so then it change shifts its attention and goes to a new pass and this is how best for search will behave it will Ex it'll pick up nodes in the order that are shown here in in red color so one two three four and goal node is the ninth node that is picked up by eight essentially what I have drawn in red arrows are the back pointers or parent pointers and this is the path that base first search would have found on this particular graph so here's ah similar problem ah with a real map outside IIT address and item address is at the bottom of the screen here so let us say you are starting had the gate in IIT Madras and you want to go to a place called LM Alumni Club of Anna University which as you can see is across the river here and this is a Alternatives that Google map has given to us of course underlying this search we will convert all this pictorial information into a graph and that is what we are talking about we have said that our search is going to be Graph Search essentially so again we have the start State and the goal State and we want to find the path to the gold state essentially and we are looking at this algorithm called best first search is so here I superimpose the graph on the map and you can see that what is going to happen with best first search as we just saw in the example before also is that it is going to drive search as close to the goal as possible till it realizes that you cannot cross the river there in this example there are two two Bridges one is on the left and the other is on the right and they correspond to the two parts that Google Maps is showing to us essentially so again this is to emphasize the fact that heroesic functions are estimates they do not know what's happening in the completely what's happening in the domain I mean they can see the map they can see the coordinates but they cannot see the liver essentially and that is why it tells you that those nodes in the red circle uh appear to be the nodes closest to the goal but in practice they are not essentially and it is very difficult to find perfect heuristic functions so how do the search Frontiers look like for the three algorithms that we have seen I have not included DF ID here because dfid is Search Frontier is a bit like breadth first search anyway because remember that dfid is basically depth first search masquerading as breadth first search essentially so we had seen this earlier for that first search the Search Frontier grows linearly shown in pink nodes on this graph ah for breakfast search it it grows exponentially shown in yellow nodes in the bottom row and the row before that in practice for best first search the Search Frontier is not very well defined it really depends upon how good your heuristic function is if the heuristic function were to be perfect then it would be linear just like that first search but in practice its not what about the other qualities that we looked at completeness and quality of solution will this algorithm find ah paths to the goal again it depends on the heuristic function but because its a systematic search remember that the only thing we did which is different from that that first search is to sort the open list essentially and if the search space is going to be finite then it is eventually going to Source the entire space and if there is a path it will find the paths through the gold the quality of solution is different and one of the reasons for that is that our measure of quality so far is how many steps to the goal essentially we are not we are not given in any weight to the edges so we are simply counting how many steps are there to the goal so as you can see this best first search algorithm will head towards the goal because it is using an estimate of distance but in practice there might be a shorter path which is a two step solution to the good and best first search is not guaranteed to find that essentially