foreign open so in general if you are talking about search spaces then it is open which tends to grow faster so we saw this for example if we have four children then we have four successors at this level then we will have 16 at this level and then 16 into 4 which is 64 at the next level and so on essentially it's the open list which tends to go in general faster and we saw that that's why dfid when we first looked at dfid we said that it doesn't do too much extra work we said that it does B divided by B minus 1 times because the number of nodes the internal nodes in an exponentially growing tree is is typically much smaller than the leaf nodes at any level essentially so pruning option open is also something that we are interested in and the simplest way to prune open is to use a beam search some with Beam with w we looked at VM search as a variation of field slamming which when we were optimizing the H value the only difference was that hill climbing we we said that choose the B best successors but only if they were better because we were trying to to measure the value of the hill climbing and the termination criteria at that point was that there are no better successors and you could get stuck in local Optima but in the context of finding a path to the goal node which is what we are interested in ah we are really optimizing on the G values of the goal node essentially we want to find the shortest paths to the Google node essency and nodes chosen based on F values essentially so if you remember the recursive best for search algorithm for example it always picks nodes based on F values but the problem with f values is that they are known to increase with depth we saw this in the example also and we showed that specially when the monotone condition is satisfied it becomes a property of the ground so you can't choose this only if better Criterion which was there in the earlier hill climbing and beam search algorithms we will say that even if not better then you always keep the W best successes where the blue is the beam base and that's why that's a variation of beam source that we will look at essentially the termination criteria is going to be gold found so we hope that the heuristic function will be reasonably good so that even in an infinite graph it will be formed for a finite graph anyway it will find the path to the goal it not may not necessarily with the optimal path because the heuristic function may not be the best but it will find the paths to the gold node the total space required by beam search will be wields into depth because at every level we are storing the blue nodes and we are going up to some depth D cycle so it is a linear space algorithm but it's not necessarily optimal and we have already saw a figure of beams as the same figure applies here obviously so the total nodes that you are going to be keeping is w at every level and because the depth is D so W Times D eventually but we are going to be searching on F values and we have already observed that F values tend to increase with depth essentially so our termination criteria is going to be again that the goal has been reached essentially if you wanted to find an upper bound cost the solution the lower bound cost is H of s because H is an another estimating function and we know that it will be at least that much but we are just in Upper bound because sometimes we want to do some pruning based on that we could find some path starting from the goal using beam search and use that cost as an upper bound cost so uh we just spoke about the beam search so what is the beam search algorithm the beam search algorithm maintains a beam of width w in this diagram the width is two but in general it can be larger because nowadays memory is cheaper and we always choose the nodes with the best F values at each level essentially and we keep doing that till we reach the goal so we assume that it will find the paths to the goal we can't use the older criteria of move only to better nodes because we know that F values increase as we go towards the goal and now we are working with f values not with h values as we were doing earlier so very often we will see that if you can estimate an upper bound to the cost of a solution to the goal then that upper bound can be used profitably to prune the third space further essentially and we can find an upper bound on the cost by just using beam search and so some paths it will give us so we know that we at least have a path of the cost given to us by the beam search so that will become an upper bound we do not have to go for a longer path so let the cost of the path be U then this can be used as an upper Bound for other source algorithms so it's a upper bound on the cost of the optimal path because we have a cost of path of Cost U so the optimal it will only be better than that essentially so basically what you are saying is that we did not look at nodes whose F values are greater than U this U is an upper bound cost that we have found using beam search foreign then this U gives us a kind of a boundary which says that you need to search only within this range essentially don't look outside this range if an value F value of a node becomes greater than U then you don't have to explore it any further because we know that F values keep increasing and any path going through a node for example here Will if F of n is is greater than U here any path that goes through here will be longer than this this upper bound value so we do not need to exclude that so such nodes we will not explore so a the the Search Frontier for a star is always going to be within that we have we have already made this observation that for any node that a star picks its F of n is always less than equal to F star of s so which is always going to be which is which is less than the optimal cost cost in any way this optimal cost we may not know but it is going to be less than that so it is definitely going to be less than or equal to the upper bound basically so for a star this property already holds essentially but we can think of other algorithms where we can exploit this fact essentially so one of the algorithms that we look at is called breadth first heuristic search it is also given to us by Zao n Hansen in 2004 but there was a spurt in in a star and variations around that time and quite a few algorithms were proposed so if this is your start node and this is the goal node and if you had done breath first search then breadth first so our algorithm is going to be breath first essentially which is what go level by level deeper into the source speeds breath for circles blind and its open is not influenced by the goal at all essentially what if we use breadth first search and use this upper bound to limit which nodes in the breadth first open we will search essentially so limit that first search to F values less than U so we had said that U is like a plot of f values which is all those nodes whose F value is less than this U what is u u is some path we have found to the goal so we know that we can only look for a better path we don't need a path worse than that so any node which is outside this boundary we don't have to search essentially so this bed first search best first heuristic search the name sounds like a contradiction in terms is but it's actually a blind search but it works within the boundary U that's why we call it hero stick in some sense and the search space the Search Frontier for b f h s or breath for civilistic search is only a small part of the Search Frontier that breadth first search would have had remember that breakfast was not had no sense of direction it would go off in any direction and that's what branch and bound also did but now we are saying OK you do blind search but stay within this boundary which is given to us by this upper bound U which we have calculated by finding some paths to the goal node now this will also find us an optimal path essentially and it has been empirically observed that the Search Frontier of depth first specific search is smaller than the Search Frontier of a star algorithm and this is an empirical observation beam search we have already spoken about it what does beam search do that uh instead of maintaining this open of breath first heuristic search beam search only looks at us fixed number of nodes at every level essentially so this diagram shows in this green curve the open list for left first heuristic search and inside that which is in this region here is the closed list that best for Series 6 search would have maintained essentially beam search by definition has a constant beam width this thing so it will only look at some subset of the nodes that breadth first seriously search will do that and we have already observed that beam search is not admissible in fact the solution found by the beam search algorithm we are using as the upper bound which means that we know that it's not necessarily the optimal that we could find better Solutions but this is how beam search would look like it's open and close we have already mentioned its ah if this is w and this is d then the total number of nodes kept by beam search is W into d which is linear in nature and remember that our focus is on saving space at this moment of course with the warning that beam searches inadmissible but it's it's got linear space that's the plus point now you can use this idea of divide and conquer uh here as well and zowon Hansen did that so they have this algorithm called device and Conquer breadth first seriously search which is remember that this is the boundary U within which the breadth first so it says and it maintains in their style of smart memory Graph Search it maintains a list of open nodes and a list of boundary nodes the open nodes are in green or cyan and the boundary nodes are in yellow but because we need to reconstruct the path it also maintains one or more relay layers essentially so that is divide and conquer breadth first heuristic search essentially we can extend this idea to divide and conquer beam search and it keeps only three layers of constant width so what are the three layers one layer is open the other layer is boundary and assuming that there is only one relay layer the third layer is relay but in in principle you could have more than one relays so we are talking about divide and conquer beam search so it is beam search so it started off with linear search but now we are saying of those all those D levels keep only three levels one for the open one for the boundary and one for the closed so which is why they have constant width essentially so which means divide and conquer beam search has constant space requirements it will keep B not B's you know yeah yeah if if B is the beam bit then it will keep B into three layers essentially or if W is the beam builds it will keep W into three layers essentially it is a constants constant space algorithm the only thing is as we have said as the warning that it is not admissible it will not give you the optimal solution necessarily code but it may not essentially so beam searches are in admissible which means divide and conquer beam search is also inadmissible and we terminate with a non optimal path so the next algorithm which is also given to us by zavon Hansen Hansen was the supervisor and there was a PhD student working on this problem is called beam stack search it's a very interesting idea so let's look at that beamstack search is like beam search plus backtracking so a little bit like what we did with recursive best first recursive best first search essentially you you followed the eurasic function but you can backtrack essentially except that in recursive best first search we kept only one best successor in beam search will keep many essentially so backtracking is explicit and it is Guided by something called The Beam stack because remember that we are not storing everything and they introduce a data structure called The Beam stack which is as follows that at every level it kept two values two numbers one is f Min which is the value of the node with the lowest F value in that beam so remember that a beam has some number of nodes whatever the width is so this is f min the let's say that these nodes are ordered in increasing H values and so one is the lowest F value is is the F min and the highest F value which is not in the beam is f Max which is why we have an open interval here and a closed interval on the left hand side so what does the beam stack contain it contains two numbers one says that in the beam at that level so this these pairs of numbers are stored at every level in the in the beam stack and it says that at that level the cheapest node in the beam has a value of f min and the cheapest node which is outside the beam Which is higher than F Min has a value of f Max essentially and these two numbers so it is a list of numbers that it it stores and it uses them to guide the beams as search when it is backtracking the pair of values in the beam stack they store the identity of the current nodes in the beam essentially used to guide the Regeneration of nodes when you are doing backtracking so beamstack search orders nodes on F values and it slides this F Min F Max window when it backtracks but it always says within this upper bound of U is actually so this is how it looks let's say the beam width is three so at every level you are storing three nodes so we have seen two levels here level K and level K plus one and the leftmost node and these are assumed to be sorted with increasing values F values here the leftmost node is the cheapest node and just outside that is f Max which is not in the beam build so when it backtracks it knows where to start the next level from so if you want to backtrack from level K plus 1 you go back to the level K and then generate all the nodes which will be starting from here essentially and that's what is happening is that you know at at Next Level which are the nodes which will go into the beam width so using this as a guide this beam stack search can ah search the entire State space so if you assume that the F values are sorted from left to right here then beam stack search essentially maintains this beam bits as you can see in the nodes on Blue which sweeps from left to right searching the space and these red nodes are the upper bound Beyond which it need not search because we know that that that we already have a solution of that cost so the last thing that John Hansen presented was called divide and conquer beam stack search so if you remember divide and conquer beam search it's a similar this thing it maintains three layers of constant width nodes except that it will now backtrack and search this entire space which is within this red boundary which is defined by the upper bound U essentially how does it do that that's a interesting part divide and conquer beamsex search has deleted the parent nodes because it's maintaining only those three layers so how does it work it answer to that is that it regenerates a node from the start node that's the important thing every time it needs a backtrack instead of going to the parent and then coming down to the next node because the parents have been deleted except for those three layers it regenerates from the start node and at each level it is Guided by the values in the beam stack so it knows that where to look at the next level essentially so in that sense the space complexity of divide and conquer beam stack is of the order of 1 which is the space complexity of divide and conquer beam search which is same as beam stack search essentially because it maintains three layers of of constant with w so those three layers that we spoke about the relay layer the boundary layer and the open layer but here we are ignoring the memory needed for the beam stack because beam stack is of depth d and at each point it stores two numbers okay so if we if we ignore that part then it is a constant time constant space algorithm so we started with a star which required exponential space then we saw how to prune the closed then we saw how to prune the open with beam stack search and then we are saying we divide and conquer beam stack search we are going to work with a constant space algorithms so with that we end our study of a star