foreign now we start looking at this topic called Rule based expert systems this topic is mole in the area of knowledge representation and reasoning but our interest is in how that is implemented because there is an interesting algorithm that is sitting behind such systems so we begin with this idea that we are looking at patterns in the domain essentially so earlier what we have done is that we have looked at transition and search in state space we started with that then we also said that we can do Transition and search in the solution space after that we moved into problem decomposition we saw that goals can be broken down into sub goals and this gave us this idea of gold trees and the algorithm aoster now we look at the notion of patterns inside the state so what's really happening behind the scenes so to speak what is inside the move joint function how does how do you implement a move then function and we will see that it is basically a collection of pattern action responses so we are interested in this pattern action pairs and we will make that explicit now essentially so in some sense we will dismantle the move gen into individual moves like we did in planning algorithms essentially so we we say the pickup is one move put down is another move and so on in a similar fashion we will look into a general problem of reasoning and and search so this area is also has been known as pattern directed inference systems in the sense that you look at a pattern which is a part of a state and do something with that and then you modify that part of the state essentially so a rule and we will look at this notion of a rule here ah looks at the a part of the state which matches a pattern as shown on the left and modifies it to result in a new state this idea is very similar to the idea of a rule that we use in logic because in logic also we have a set of antecedents and then we have some conclusion essentially but when we look at this language that we are looking at today we will see that the right hand side will be not just conclusions which are based on the antecedents but may have other ah actions included also amongst them would be the action of deleting a statement essentially which is not the case in ah first order logic in logic once you say that something is true then it kind of remains true forever so the Pythagoras theorem for example is always true so there is no notion of time in mathematical logic you know statements are true or false essentially but and we saw that to get around this problem ah when we looked at planning we had to explicitly add predicates or delete predicates a similar idea is followed in ah rule based systems as well so earlier this was our notion of a move then that you were in some start state or some any state n and a move gen function told you what are the neighboring states but what's inside what's underneath the hood of the movement function so a state is essentially a set of sentences in some Language by now we have this notion of logic and language and we saw that in planning also pattern is a subset of sentences and modifying the state in a piecewise fashion so you look at the pattern and modify that so A Move is basically spot a pattern and do or suggest the corresponding action so a state may have many patterns and each pattern may trigger some action which will show up in the move gen function as one move so this four moves that we see on the screen are essentially four patterns that can be spotted in the state and resulting in changes with that which gives us the four neighbors this idea of rule based system extends Beyond simply doing search for problem solving and it extends Beyond ah reasoning itself we have already seen earlier or we have mentioned that ah Kowalski had said that logic can be used for programming that logic subsumes computation essentially so that is how we got the idea of logic programming declarative programming is a general term which talks about programming where you specify the relations between things and do not specify the sequence of actions that have to be done in the program which is typically done in imperative programming languages for example C that you do action a you do action B and so on and so forth in declarative programming we do not worry about which action is to be done we worry about the relation between the input and the output essentially and we see that a rule Associates an action with a pattern in the state the community also called it a production so sometimes people call it a rule based production system unifying format for heuristic knowledge business rules and actions so a rule can represent an action also like in the planning operators pickup or something it can also encapsulate heuristic knowledge and this is the key that we want to look at here is that human experts may give us a whole plethora of rules ah which they feel are useful and in some sense in those rules we are capturing their knowledge which we will use as heuristic knowledge and we will see that business rules is going to be one of the subsets of this heuristic knowledge for example ah Bank may have its lending rules essentially here as to you know when when do you lend to somebody and so on everything can be implemented as u rules so the rules in a production system there are unifying unifying format not only do they capture the sequence of moves they want to do but they also capture heuristic knowledge and ah other related things so because of which declarative programming is also during complete programming language and by Turing complete we mean that anything that can be computed by a turing machine the turing machine is the most general form of of an abstract machine essentially so you might have studied that in the course on formal languages but even if you haven't basically a turing machine is an abstract computer which captures what can be computed essentially and any programming languages any programming language which allows you to do that computation would be a touring compute programming language and the language that we will look at today call Ops 5 is one such language the key thing about declarative programming is that the user or the programmer only States the rules we already said that the rule is a connection between a pattern and an action it's very popular in business environment for example I just said that lending by Banks you know bank manager only needs to specify what are the rules under which you somebody is eligible for a loan the programmer only specifies a pattern action Association she does not specify when that action should be executed which is what happens in imperative programming that you say okay do this action then do this section and those actions may be conditional statements if something is to do do this or something is not to do that which can be extended to things like Loops so for example they are also conditional actions while something is true do the following set of actions and so on in declarative programming we do not do all that we just say what is the relation between patterns and actions and we will leave the decision of what rules to execute to what we as we will see what is called an inference engine and which incorporates a search strategy as to which rules are applicable which ones to apply so the user is typically given a choice of strategies by ah production system implementation and we will see that today so this is a notion that we are looking at rule based production systems it has the following three components one is the working memory so if you think of that ruleless production system as an agent which is trying to solve your problem then the working memory represents the current state of the problem solving so in some sense if you remember what was happening in the planning domain is at any given time we knew which predicates were true you know block a was on top of block p and Block C was being held by the robot arm and so on everything that is represented is the current state and that we will call as the working memory so it contains a set of records of statements known as the working memory elements so working memory elements are Elements which reside in the working memory so each of them will be called a WME and we tend to think of this working memory as a short term memory of the problem solver because it is dealing with the current situation what is true at a given state or a given point of time then we have a set of rules essentially and each rule which we also call the production it represents a move it has typically a name or an identifier has one or more patterns on the left hand side which we will call as LHS or left hand side and has one of one or more actions on the right hand side which we will call as RSSI so the rules is the problem solving knowledge of the agent it is independent of the current problem that you are trying to solve it is independent of the situation the state of the current problem which is captured in the working memory so in that sense it is a long term memory of the problem solver the long term memory is the problem solving knowledge that the agent has and then finally as I said we have something called the inference engine and it is the task of the influence engine to first match patterns in all rules so each rule has a set of patterns we have said one or more pattern on the left hand side so it is the job of the influence engine to look at all the rules and all the patterns in all those rules and match them with the working memory which tells us whether the rule is applicable or not in a given state after matching all the elements it has to pick one matching rule so we assume a sequential form of execution in which one rule is fired at a time essentially and it has to execute or fire these are the terms we use interchangeably that we fire a rule or we execute a rule and we will see what all the details of all this ah different kind of actions we can do and essentially it repeats this process till some termination criteria so what is this that is being repeated its a cycle of match where you match all the rules with all the working memory elements then you will resolve as to which of those matching rules you want to select essentially and the third is fire that having selected the rule fire the rule which means that you may be making some changes in the working memory as a consequence of that and this happens till some termination criteria is used so as shown on the right hand side in influence Angel basically Works in this cycle match resolve execute till somehow it gets out of that essentially so the first phase of this cycle is match the match takes as input all the rules that is a long term problem solving memory of the agent and the working memory which is the short term memory which says what is the problem we are solving and what is the state of solving that problem and it produces a set which traditionally has been called as a conflict set so the mass algorithm takes a set of rules of rules and the set of working memory elements input and generates the conflict set each element in the conflict set is a rule along with the identities of the matching working memory elements we will see how they are identified in a moment but basically every element in the conflict set is a rule and the data that it is matching in the working memory essentially now we have to select between the rules in the conflict set and the matching data and in that sense there is a conflict as to which rule to fire and conflict resolution which is the next stage all these all decides as to which of the rules to select from the set of matching rules what are the set of matching rules these are the ones which have been identified in the conflict set so the first state the match state results in the conflict set now if you think of this problem ah little bit you will see that supposing you have a hundred rules and let us say each rule has five ah conditions on the left hand side just for the sake of argument so there are 500 patterns in in those hundred rules and if the working memory has let us say 10 000 elements ah then all these 500 patterns have to be matched with all the ten thousand elements to see whether there is a match so it so it has been stated that match is the most time consuming part of rule based system so having got the conflict set the next stage is resolved and what does resolve do it selects one rule along with the matching working memory elements so it selects rule to be executed or fired plus the data which is matching that rule essentially because that is what is stored in the conflict set essentially and we will see that it is resolve which captures the strategy that will be used for search the rules for conflict resolution will will give you what is the strategy of search that this agent is doing so it is also doing search its you know all these rules are matching just think of it as forward chaining in fact the influence engine that we are looking at is also called a forward training rule based system because you have the data you match all the rules and you in some sense you are doing the move Jain function and then you select one of them and that selection is the Crux of whether you are solving the problem efficiently or not essentially and that selection is done by a con module which does con resolution which is called a conflict resolution module and the last stage of the influence engine is that you execute the rule Plus data that you have fired and actions that are executed they may make changes in the working memory which is of course then updated and then you have a new working memory element and if you are adopting The Brute Force approach then now again you will take all these working memory elements let us say there is ten thousand and five or something like that you have added five due elements with those 500 patterns in those 100 rules you have to do all over again so of course one of the things we will see ah is an implementation an efficient implementation in which this Brute Force work is not to be done but we will come to that later so there is a language which started this all a production system language called ops 5. it was devised by Charles 4G in Carnegie melon University in the 1970s remember from our history of AI that CMU was a very prominent Center for AI you know with Simon and Newell uh working there and many of their group Ops some people say in courts stands for official production system language so PS5 but there were later versions of Ops language and much later there is a system called soar you should look it up on the net with this was implemented by ah somebody called John layered also hcmu and it is a cognitive architecture whatever I am writing and by our cognitive architecture we mean a set of modules or program which does what some people call as cognitive Computing which basically tries to do Computing like we do essentially and remember that that was the goal of Simon and Newell the first book was called human problem solving and they try to see how humans solve problems so this overalls basically eventually evolved out of op swipe it contains all the elements of off swipe but it also contains other elements so nowadays if you go and download so you will see that it has a reinforcement learning component and various other components essentially so it kind of system which is tuned to solve problems but coming back to Ops 5 in Ops 5 our rule has a simple form that you start with the initial p p basically stands for production then you give a name to the rule which is the rule name then you specify the left hand side and then you specify the right hand side separated by n right Lo essentially we often use the term export system at least in the 80s we used to use the term expert systems because these were programs which had knowledge acquired from domain experts and the whole idea was the domain expert would Express their knowledge in the form of rules and then we will put those rules into ah production system like Ops 5 and low we would have the knowledge to solve problems essentially one of the earliest systems was called Arwen and we will look at a example of a rule from Alban inner moment so let us talk about the syntax of this language so this Ops 5 is a language which is a turing complete language and anything that you can do you can do in Ops way the data structure used in offside is a structured record which starts with the class name and a set of attribute names attribute names are marked by the Hat sign essentially the data is a collection of attribute value pairs bunched together so there may be a set of attributes and we will see examples and each attribute will have a value and so we have this attribute value Pairs and they are all bonds together the way that we Implement these influence engines the order of attributes does not matter so you can State them in any order and the system will work well which is different from for example matching in in in in logic where the order matters essentially and we saw this example of backward chaining earlier the default value of an attribute is nil if you do not specify a value while creating a working memory element then its value will be given as nil essentially so here is an example of a working memory element the class name is student then there are attributes name is an attribute whose values sudhir age is an attribute whose value is 19 semester three discipline math degree BSC for example this is an example of a working memory element so you have a set of attribute names and you have a set of values for those attributes essentially and then there is of course the class name in this case the class name is the student or the record name if you want to use that term so the working memory is basically a collection of working memory elements as we have said and Simon and Newell model the working memory as a short term memory of the problem solver that's what we have already said that it contains a data that the solver is currently working with essentially it is a collection of working memory elements that we have stated and these are indexed by a timestamp so every working memory element is given a timestamp indicating the order in which they were added to the working memory we will see that this will have a role to play in conflict resolution that you may want to use the timestamp for resolving the conflict in the conflict set so here is an example of what a working memory could look like so there are six records of six working memory elements each of them has a timestamp given to this each of them has a class name so next or total marks or array or so on each of them has a set of attributes and each attribute has a value essentially so the first working memory element here says that the class name is next and the attribute is called Rank and the value is 1 essentially we will see how to use this because we will see examples of rules which use these particular working memory elements essentially the second one says the class name is total marks then there is a student who has marks and whose rank is nil essentially so you can imagine that we are writing a program that we have marks of all the students and we want to assign them a rank essentially and what this working memory at this point says that the next slang that you want to assign is one essentially and then of course we will choose a student with the highest rank in this example it would be Sangeeta we will assign the rank 1 to Sangeetha and then this next slank will get modified to say 2 essentially so the next person to get a rank will get two so we will see how this is done with rules essentially the other example that we will also see is if you have an array of elements where ah using the syntax of of swipe this array can be stored by a set of Records where there are two attributes one is the index of the array and the other is the value stored in that index essentially so here we have index one value 29 index 2 value 12 essentially should be a closing bracket here now let us come to rules essentially ah the left hand side of a rule is a set of patterns that conform to the syntact of the working memory elements because after all we have to do matching essentially the value field in patterns can have variables and they can also have Boolean test so we are talking about the values of attributes in a rule they you can have variables and you can also say that do some tests on whatever is stored in that particular attribute in the working memory element essentially there may be even negative patterns too which will say that ah this pattern this working memory element with this pattern should not exist in for this rule to be matching remember that the left hand side of a rule tells you when is the rule applicable or when does the rule match essentially so here is a example of a rule in English if the next rank to be assigned is R so notice that we will use this angled brackets to represent variables essentially and that is a convention used in Ops 5. and there is a student ah who has some marks which we have written as M which is a variable we do not know how much is the mark but who has not been assigned a rank as yet and there is no student so this is the negative pattern that we were talking about that there should not be any student who has higher marks than M and whose rank is also unassigned so basically the highest the person with the highest marks who has not been assigned the rank is captured by this left hand side and in Ops 5 it would look like this let us say we call this rule as ranking that's the name of the rule remember that the syntax of a rule says P followed by rule name followed by a set of patterns there are three patterns in this and we have said that next slang to be assigned is something which is a variable called r and total marks there is a record of total marks for some student s we do not know the name who has got some marks M again we do not know what is the marks but who has not been assigned the rank so the rank is nil and there is no record of the kind total marks where the marks are more than this variable m and the rank is also nil essentially so you would also see that patterns do not have to conform to the whole structure of the working memory element you can only select table it choose attributes that you want to talk about and then there would be some right hand side so again to repeat ah this rule says the first condition is that ok what is the next slang to be assigned this tells us that then it says who is the candidate for this rank that candidate must be some student who has got marks M and there is no student who's got marks more than them so remember this Boolean test we had said this will return true or false essentially so there must be no one with marks more than m so here is an example of a rule from this system which I mentioned R1 which is also called xcon it was written by John McDermott and John McDermott if you remember was an author of one of the early good books on AI which is written by him and Eugene charniak so charniak and Magnum what is how we typically refer that book to this system called R1 internally it was called xcon for an expert configurer program was the production rule-based systems written in this language Ops 5. ah in 1978 and it was used to assist in ordering of deck wax machines computer systems by automatically selecting the systems now 1978 was the time which many of you may not have been around ah when there was no personal computers I mean not to speak of mobile phones or smartphones they were nothing of that kind the only computers we had were called as mainframe computers and they were kind of machines which occupied a large amount of space in the room and the company deck also produced this series of very popular machine called wax computers and this program was used to try to configure as to what is the configuration that a particular user wants to buy essentially so this is just an example of a rule from r one we do not want to go into the details the rule has a name as you can see it's a long name assign UV modules except those connecting to panels four so this is a series of four such rules this is the fourth one in the series and there are various patterns the current context is assigning devices to Universe modules and there is an unassigned dual Port disk drive and so on and so forth and then you of course do this assignation so this is just to illustrate a kind of rule ah that Ops 5 uses of course this rule is not in offside language this rule is a ah expressed in English just as we express the ranking rule in English first but it can be converted into Ops wave so let us go back to the syntax where the conditions in the left hand side what can be the conditions the value field in patterns remember that a rule is a set of elements which we call as each is a pattern in the left hand side they can have as we have said variables and Boolean tests essentially so a variable ah Boolean test for example can be equality which says that they are the same type same type as and also equal so for example x equal to 3 if you say that means that it will match only when x equal to 3 essentially or you can also just only take for same type that you know both are integers for example for integers or for floating point we have this ordering relations less than less than equal to greater than greater than equal to we also have conjunctions that two test must be satisfied that it must be greater than three and it must be less than three point five and there is an implicit and and this is represented by this curly brackets for saying that I am talking about the conjunction if you use two angle brackets as the one shown in the bottom ah then it is a disjunction or an or essentially so the supposing we are talking about the day of the week then it it then the rule will match either if it is Monday or if it is Wednesday or if it is Friday so what about the right hand side the right hand side of a rule is a set of actions that we have set the simplest action is to make or create a new working memory element which is called make in Ops wave and other is to delete one which is called remove so this is a little bit like the add list and the delete list we saw in split in in the strips programming language or pddl 1.0 modify is just a short form for removing something and adding a different one which is slightly modified but but there are other actions like read from the screen or from a file right to a file or from a screen load a file or halt all kinds of things which are necessary for making it a complete programming language so here is our ranking rule that we started looking at it this was the left hand side that there is the next slang to be assigned is r and there is some student who's got M marks and there is no student who has got more than M marks then what we want to do is to assign for this student the rank which is given to us in the first pattern so this is expressed by saying that modify the first working memory element of modify the working memory element which matches the first pattern in the rule to R plus one which basically the next rank will become whatever R was plus one and modify the second element to the value R now I purposefully written this in this order ah because if you are a imperative programmer then you would say that oh the all the value of R has changed before you have assigned the rank that's not the case in ah off swipe actions can be thought to happen concurrently essentially which means you are not saying that the first action happens first and then the second happens the values that the variables you use are the values they got from match essentially so basically this R and this R is the same even though this R says that you modify this to R plus one but it does not affect the fact that the current student will get this rank or essentially so if you have found the record with the highest marks then assign rank R to the student and increment R also there is no sequence that the kind of confusion that would happen in imperative programming that if you say R is equal to R plus one and a sign line r two the student that means a incremented rank would be assigned but this is not the case the r that is used is the r that the inference engine has got in the match phase essentially we can also use an alternative syntax instead of identifying the working memory from the matching pattern that pattern number one or pattern number two we can actually give names to patterns so we say this is the rank and this is the total and then we can say modify Rank and modify total it is just an alternative syntax which you may find it useful especially if you are going to be playing around with your rules and modifying them and then you don't have to keep track whether it's the first pattern or the second pattern you can remember them by the names essentially so just a very quick example of how rule based languages can be quite powerful ah so let us look at the problem of sorting we have already seen that you know there was some working memory elements in a class called array which had attributes index and values essentially stored with every index value the objective so let us say sort the values in increasing order essentially so we have a set of Records which is array index one value something array index 2 value something in Array index three value something and you want to sort them in increasing order and there is a simple rule which as you can see is very similar to what we did earlier what are we saying here we are saying that for some index I if the value is X and if for another index J which is greater than I so J is greater than I the value is y Which is less than the value in X what does it mean that there is a higher index a higher meaning numerically larger index but whose value is smaller so we basically do not want it essentially and modify we simply say modify the first value to y and modify the second value to C so what have we done we have swapped this record which was at ith index so for example a nine actually I should write let's say I will just use index I here 9 and value six let us say this is one record and there is another record whose index is more than nine so let us say its eleven and whose value is so ah less than six which means it is three let us say for example so if we have these two in elements in the working memory so the ninth record has a value six the eleventh record has a value three but we want to arrange them in increasing order so essentially we are going to swap these two elements essentially and that is what is happening here that whatever was stored um ah in the second element now thats got into the first element and whatever was stored in the second thing has gone into the second element so basically we have soaked and this one rule is enough to sort this whole array essentially it will just repeatedly fire Till There is no mismatch somewhere of course this is not going to be an efficient algorithm to solve this thing we know that sorting has to be done much more carefully but this simply shows that a rule can capture this whole process essentially if you are working with Ops 5 you can actually write algorithms like merge sort or quick sort and so on which are more efficient but this was just to illustrate this idea so we will take a break and we will come back to looking at how conflict resolution happens yeah is the one and two referring to the index the modify one yeah not the index it refers to this is the first ah pattern and this is the second pattern so that is what we saw ah here here also you can see that ah it is the identifier for the working memory element and which is why we said that maybe you will find it useful to give them names and then say that you know change in those names