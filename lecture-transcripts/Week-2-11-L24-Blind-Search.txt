so now here we are uh ready to write our first two algorithms for doing state space search uh these will be the building blocks on which we will build the rest of the algorithms so let's get on with that here is the first algorithm called depth first search and let us go through the steps one by one here the first line says that we initialize open to contain the start node and remember that we are keeping parent pointers so null is the pointer null is a parent of the start node so we have created a list of a pair and there is only one element in this list so the list looks like s comma null this is the starting point and we initialize another list called closed which we will use to store the nodes that we have already visited and remember that this was done for two reasons one was to avoid going into loops and the second was that to store information about the parents that we had visited in the past so that we can reconstruct the paths back to the start note so as before we continue by saying that as long as there is some some candidates in open as we say in line three we extract the first element from open now remember that that element is a pair and we'll call it a node pair essentially in particular we are interested in the first element of that note pair which is a node that we want to inspect and we have called it n here obviously then we apply the gold test function to n and if it is true then we will call a function called reconstruct path to which we will give the argument as a node pair that we just extracted from open and also closed which contains the node pairs that we had extracted earlier so we will look at this algorithm in a little bit detail later if it's not the goal node then in line 8 we add it to closed and we have used the concatenate operation there after that we generate the children of this node n by calling the move gen function from this set of children we remove anything which is already on open or already on closed and we do this by a function called remove scene which we will describe shortly so now we have a set of new nodes new nodes in the sense that these are nodes that we generated but which were not present in open or enclosed but we want to work with pairs now so we call another function called make pairs and what this takes is that it takes the new nodes and to each of them assigns n is the parent of that node and this function also we will see shortly and then we append the new pairs with the tail of the open so what we have removed one element from open and whatever remains we append with that and we have appended it at the beginning of the open list and that will make it a stack data structure so if you have studied data stack data structure it has a property of being last in first out so the last node that we insert into open will be the first one to come out of open and add that influences the behavior of search essentially the other option would have been to add it at the tail of at the end of open that would have been first in first out a queue and you will see that that will lead to it behaving in a different way which you will call as breadth first search so anyway this is the main program our main algorithm for doing that first search let's look at some of the ancillary functions that we want to use so one of the functions was remove scene it takes a node list what is this node list this is the set of neighbors returned to us by the move gen function from that node list we want to remove anything which is in open or anything which is in closed essentially so this is a small recursive program if the node list is empty then you don't have to return anything you just return the empty list otherwise you take the head from the node list remember that this is one node and this thing and we call a function called occurs in which says ask does the node occur in open so it will answer yes or no and likewise whether it occurs in closed again that will answer yes or no if any of them are answers yes because we have used the or conjunctive or the all connective here we will not add that and we will make a recursive call instead to remove scene with the tail of node list so the first element has occurred either in open or in close so we won't add it and we'll go and look at the rest of the nodes if this whole thing returns false which means it occurs in neither then we go to this step which is comes because of the else clause here and then we keep that node and add it to whatever the recursive call to the tail of the node list gives us essentially so in this manner we will scan the node list from left to right keeping only those nodes which have which don't occur already on open or on closed and we have used the function occurs in there what does a cursing do it takes a node and a list called node list of node pairs remember this node page could be either open or it could be closed the same function is called twice basically now if there is nothing in the node pair then obviously occurs in will not occur doesn't occur so we return false but if the node page is not empty then if that node that we are interested in is the first element of the head of notepad so what is note pairs notepads is a list of pairs so let us say n1 p1 n2 p2 and so on what we are doing is first first we are extracting the first element from the this list which is done by the list function head and from that we are extracting the first element which is the no first node in that node pair essentially so we want to check whether this node is equal to the node that we are interested in if it is true then we return true essentially otherwise we do the same thing recursively by looking at the rest of the note pairs or the tail of the load pairs list so some some some books if you look at data structures they would use head and tail for list operations some people some books might say first and rest but as long as you understand the meaning of these functions we can use them so we are using head and tail here and so in this way we look at all the note pairs first in open and then in close remember that the we we made two calls there one was with open as an input the other was with close as an input so at the end of those two calls we know whether we have seen that node or whether it is present in open or not if it is not present in any of them then we will add it to the set of new nodes essentially new nodes meaning the nodes that we have not seen earlier okay so the next function that we have used is called make pairs what does makepass do that for all the new nodes that we have generated we want to add the node n which was their parent as the parent of the node list so what this makepass function does is simply that it takes a list of nodes called node list it takes some node who we are calling as parent because that is a parent when we call the move gen with these nodes were generated if node list node list is empty then we have finished our job otherwise what we do is we take the head of node node list the first element in the node list and make a pair so this operation of putting it in brackets is making a pair with parent which has been given as an argument to that and then recursively call make pairs with the rest of the node list which is the tail of the node list essentially so in this manner we generate these pairs so there is an example in the bottom if you give if the node list is a list of nodes a b c d and the parent is s for example then what this function returns is us a list of pairs so it should be a list so there's an error here let's not pairs that's a list of so list is something which contains an arbitrary number of elements of the same kind pairs or triples or tuples in general are of our collections of fixed number and they can be also of different kinds so in any case so what this has given to us is this new node pairs or new pairs as we have called it in our earlier program and we are adding it at the head of open and that's what that first search is doing so that leaves one part of the ancillary function which is the reconstruct path function we already saw reconstruct path when we were looking at the more general description of a search algorithm but let us go over it again essentially what this does it the parent that we are looking for we skip to that note pair in the list of notepads um which has parent as the first element if parent is the first of the head of node pairs then we return node test else we skip to parent in the tail of the node pairs essentially so we are looking for some list at the beginning of which is the parent of the node that we have passed as the first argument so we are looking for the parent of the parent in some sense and then we start constructing the path we begin by taking the node that we had started with remember that this is the node pair that for which node for which the gold test function said yes and we append it to the empty list so basically we are creating an empty list with this node and as long as so we keep looking at parent and as long as parent is not null we add the paths parent to the path or append the or add or concatenate the parents to the path remember that parent is a new parent we have found and path is that and we now go to that part of closed which contains this new parent as the first element and then we will extract the parent of that parent essentially so i hope that's clear the other algorithm that we mentioned was called breadth first search and the only difference yes so before you start this dfs so is it possible to explain these functions in a graphical manner so example we draw one we have one trees or somewhere on the left hand side and on the right hand side we are going through these functions and you can explain to the drawing but yesterday we had a graphical description shall i go back to that maybe here so here uh we have written we have we have done gold test with this node g and at that point closed contains these nodes and then what we do is we now trace back the parents from the nodes to its parents so from g we know the parent of g is e as you can see here then when we go and look for e in closed we know that the parent we can find out that the parent of e is d so we add d so basically what's happening here is first we add g to the path then we add e then we add d then we add a and then we add s and in this manner we reconstruct the paths that we are interested in and that's what this reconstruct path function is doing essentially so maybe it's good that we came back to this diagram so hopefully it will make things clearer so essentially that's what we are doing we are tracing the parent pointers which are all stored in the closed list somewhere essentially well you are calling it a pointer but it's just a set of node paste in the later version of the algorithm ah we will actually use the notion of pointers essentially when you look at the algorithm a star okay so that was depth first search a variation of depth first search which only changes in this statement is that instead of putting new new pairs at the beginning of open we are adding it to the end of open therefore converting open into a queue and the behavior of the search algorithm changes how does it change because the first element that is extracted is uh was generated earlier essentially maybe we will have a look at how these two algorithms behave so it's basically it boils down to how are you treating open what kind of a structure is open and the two possibilities that we have seen are that either open is a stack or open is a queue so we have this new nodes which are generated how are these new nodes generated they were first generated by move then function then we recall the removes remove scene function which removed anything which is either on open or closed already then we call the make pair function and that gives us this set of new nodes essentially and now we are adding them to open because that's what our idea is that if the node is not a goal node add its neighbors to open and then at some point you will inspect the neighbors one by one we have all we have already said that our neighbors that we inspect will always be from the head of open so where we add the new nodes we'll determine which neighbor we will inspect next so search always picks a candidate from head of hope and that we have frozen essentially the only thing that is variable is where do we add the new nodes so one option is to add them at the beginning of this open list in which case it behaves like a stack and we will see its behavior how how it influences the search in the tree the other option is to add it at the end of open so it's like a queue because you know everybody queues up in a disciplined fashion and that its behavior of the search algorithm will see so what do we mean by behavior of the search algorithm by as to what is the order in which nodes are picked up from open that basically determines how it will traverse the tree essentially so depth first search it turns out is dives into the search space so if we started with the start node and generated its neighbors we move to one of its neighbors then when we add the children of this new neighbor which let's say are possibly these we are going to move to one of them because these children have been added at the head of open and we are always picking a note from the head of open so maybe it's it's this one as we see and then we add the children of this one the children of that one may be this of course not all will be added because we have the remove scene function so essentially the behavior of depth first search which you might be familiar with is that you generate children and without loss of generality we always pick the leftmost node because you know it all depends on what is the order in which you are generating neighbors then you move to this node and generate its children then you move to this node and generate its children then you move to this node and generate its children then you inspect this node let's say this node has no more citizen left then you go back and inspect this node and this has no more children left let us say and then the third one also has no more children left then you backtrack from there and come back to the next node and this is where search will go so this is the general behavior of depth first search and it is achieved by taking the by adding new nodes to the to the front of open this thing and as a result of which it kind of dives into the search tree by looking at all the newest children first so we can write this as a strategy newest children first it is traditionally called depth first search because what we are saying is deepest nodes first so if you look at the search tree that i have drawn on the right that at any given choice point you are choosing the node which is deepest in the tree and inspecting that and of course at some point because it may be a finite search space you may have to backtrack and try something else if the search space was infinite then maybe you will never backtrack and you will get lost into some branch and that's as we will see one of the drawbacks of depth first search so that first search is achieved by treating open as a stack which means the last nodes that you put in are the first nodes that we take out remember a stack is described by this property of lifo last in first out and in that sense it kind of dives into the search tree essentially and it's like follows its nose and without looking left or right breadth first search on the other hand uses open as a queue that is characterized by first in first out essentially so let's look at so this is a diagram which is showing how the how that breadth first search would explode the search tree but if you were to look at it from and this diagram is in the state space in the search space you start with this node s and you generate its children so let me number these so this is the first node that you inspect then this is the second node that you inspect you inspect one child on this thing so inspect it it's not the goal so we added its children to open so in this diagram that i have drawn double circles are closed and single circles are open and you basically want to select one node from open for inspecting and expanding next what does brett first us do because it puts these new nodes that we just generated behind the older nodes the node that will be generated would be this one three so you can see it goes down level by level it will generate this it generates children but it will not inspect them that first search would have generated the children and immediately gone to one of the children between search generates these children but keep them pushes them in the queue behind their turn will come later so it will inspect this third node then this fourth node and i think i have some diagrams coming up which will describe this then this node and only then it will shift to the next level so let first search goes level by level so it traverses the tree first nodes at distance so if this is a start node and let's say first it inspects all the nodes which are within one hop from the start node then it will inspect all the nodes which are within two hops of the start node and so on and so forth which will transcribe to uh which will trans which will be described by it going layer by layer so you can see that basic difference between the two search algorithms you might say that depth first search is impetuous it sees a new node and it goes to that breadth first search is conservative that it says okay i have already generated some nodes earlier let me look at them first then i will go to the new nodes that are generated so the fifth node here is the first new node at level 3 that it sees then it will go to 6 and then we'll go to 7 and so on in that manner it will go down the search tree level by level and its behavior is as illustrated in the state space on the left where where we are shown the order in which the nodes are expect inspected and as you can see this is in direct contrast to how that first search space looks it goes down till it hits the boundary or the edge of the state space then backtracks and keeps trying the newest children first essentially so these are two search algorithms and this is how the behavior is different this difference is achieved simply by where do you add the new nodes to the open list essentially if you add it at the head or the front then it behaves like a stack and it always rushes down into the search tree or dives down into the search tree if you add it at the tail or make it a queue then it goes systematically farther and farther away it tries to in some sense stay as close as possible to the start node essentially whereas that first tries to be as far as possible from the start node essentially and we will see that this property will uh result in breadth first search always finding the shortest paths essentially