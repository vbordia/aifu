okay so we have described these two algorithms depth first search and breadth first search what is the difference between these two algorithms the first search treats open as a stack last in first out breadth first search creates open as a queue first in first out and we have a feel of how they generate different search trees and explore them in a different fashion we already saw in the last session that breadth first search seems to have an advantage that it will not go into infinite loops even if you're not checking for nodes enclosed that it will eventually find a path to the goal so let's do an analysis of both the search algorithms and try to understand um how they fare in terms of com various properties of search algorithms that we will be interested in so we have seen this behavior i think if it's a small uh search tree as you can see it's on the left hand side there's a branching factor of three and every every node has three neighbors and the search tree as you can see grows exponentially that is something that we will be worried about the first level has one node which is only the start node the next level is three the third level has nine the fourth level is 27 it grows exponentially and we will see that that's going to be something of concern for search algorithms but at the moment we are more concerned with what is the order in which they inspect nodes and the order is shown on the right here both for that first search and that first search essentially and we have kind of informally discussed this already so when the two searches pick the fifth note so there we have seen already fourth note have been put and the fifth note is shown in green in both the diagrams one is here and the other is here you can see that the fifth node is at different levels to start with uh for breadth first so it is closer to the source and for the first search it is farther from the source and they have explored the earlier nodes in a different order by the time that first search picks the picks the fifth node it has already finished with the previous level so it has seen all the nodes on the previous level that's not the case in the first so in that sense their behaviors are different we already said that that first search dives down into the search tree and buffer search is conservative staying as close to the source as possible as as close to the start as possible so how what should be the running time how long will it each of this algorithm take to find the the goal node essentially so in this schematic diagram i have drawn this as something which looks like an exponentially growing search tree and if since you know the behavior of these two algorithms by now we can see that the left hand side side shows a goal node which it will that first search will find faster essentially so supposing there are two gold nodes in the search tree it's possible that they occur in this thing two different locations in the graph there are two different paths to the to the goal node that first search will one find one find one path if it is closer to the left hand side of this search tree whereas between search will find that path to the goal node which is closest to this start node essentially so remember that how does that first search work it goes down this path then it backtracks tries it and that's the behavior of depth first search it goes down when it runs into a dead end it backtracks so we are assuming that you know the search tree is a constant uh total depth this could happen for example if you are solving the n queens problem because you the the depth would be n you try the first queen the second queen the third queen and so on and the last one would be the nth week i'm just to illustrate that point breadth first search on the other hand starts from the root goes down the first level then goes down the second level then goes on the third level fourth level and so on and eventually it will find this node earlier if it is closer to this start node essentially irrespective of what or see the order in which we are generating exploring the nodes depends on the order in which we are moved then function is giving to us nodes essentially because that determines the order in which you add them to open essentially but in general we can observe that if it's closer to the start node breadth first search will find it closer but if it is generated in a branch which occurs earlier then that first search will find it faster so let us worry about let us try to discuss this little bit more rigorously in terms of time complexity so we assume that there is a constant branching factor so we are looking at the search trees at this stage of b so b is a branching factor and assume that the goal occurs somewhere at depth d essentially so the time complexity can be measured in terms of the number of nodes inspected by each search so because it spends some time looking at each node it takes it out from open inspects it adds its children to open and then takes out the next node so the number of node it impacts can be seen as a measure of time complexity and we will call these two numbers uh n for dfs or n for bfs essentially so they let let there be the number of nodes inspected by these two search algorithms so if the node and we have assumed that the goal occurs somewhere at depth d this is just for the sake of analysis if the node occurs at the left most edge then you can see that depth first search would have inspected the nodes this the start node the node at level 1 level 2 and so on and up to the level whereas breadth first search would have inspected this entire tree before it would have inspected the entirety up to the previous level remember that it goes like this it would have finished doing all those things and then it would have picked up this node and so the number of nodes that it would have inspected is whatever 1 plus 1 plus b at this level b square at this level b cube at this level and eventually that will come down to this expression which is for the summation of from 1 up to b raised to n minus 1 b raised to d minus 1. what about the other end supposing the goal node was at the rightmost end of the this thing in which case both the search algorithm will inspect the entire tree and the number of nodes in the tree are b raised to d minus d plus 1 minus 1 whole thing divided by b b minus 1 so which one is better so on the average if we sum up for depth first search we have two components one is d plus one if it is on the left most end and the other is this figure which is on the rightmost end and for large branching factor and large depth you can assume that this approximates to b raised to d by 2 essentially so it's exponential the important thing to observe is that it will ex it will look at an exponential number of nodes essentially for breadth first search also we do the same thing we take the expression for the left hand okay that should include that and the expression of the right n and then simplify it remove constants and so on and so forth and what we get is something not very far from there which is b has to do into b plus 1 divided by 2 into b minus 1. so if you look at the ratio of the 2 the number of nodes inspected by breadth first search is more than the number of nodes inspected by depth first search but only by a factor of b plus 1 divided by b minus 1. how much is that factor supposing branching factor is 10 that means 11 by 9 times more essentially so the larger the branching factor the closer their time complexities will become but the important fact is that they are exponential in nature and as computer scientists we do not like algorithms which run into exponential times so we will need to do something about that what about the size or space complexity so if you measure space complexity as a size of open just as an approximation then you can look at this tree where blue are closed nodes and the blank nodes are open nodes both of them start with the same kind of search tree but as that first search explores it you can see that the number of open nodes is not growing very rapidly as you go down deeper and deeper why is that that's because as you go down the depth at every level dfs adds v minus one nodes it generates b nodes and picks one of them and the remaining b minus one i added to open so with this branching factor of two you can see that it's added two nodes here it's added two nodes here it added two nodes here out of which it picked that one and at every stage it is adding basically two nodes essentially and as in fact as it sweeps from left to right then that two will also get reduced later to one and so on so what what do we see here we see that the size of open for that first search grows linearly with that while linearly because at every level as it goes deeper into the subspace it adds a fixed number of nodes which is b minus one number of nodes and therefore the size of open grows linearly on the other hand breadth first search finishes one level before going to the next level so we have already seen this right at level one there are one node then there are three nodes and there are nine nodes and then there are 27 nodes essentially what is breadth first is doing it multiplies by b as it goes down every level at every level initially it was 1 then it became 1 into 3 and then it became 3 into 9 then it became 9 into 27 and as you go deeper it multiplies the number of nodes and you know that if you keep multiplying this then that's where the exponential growth comes wrong with this so as far as the size of open is concerned that first search is definitely a good algorithm it's it the space required is only linear in nature whereas breadth first search will always require exponential space so that's a negative point for breadth first search and in fact as i mentioned earlier as the search sweeps from left to right the size of open keeps decreasing for depth first search because we have finished all those nodes in so initially for example we had four nodes here and then even eventually when we backtrack and come back there will be only one left essentially so the size of open keeps also decreasing what do the search frontiers look like by search frontiers we mean the open nodes because they define the set of candidates that you want to inspect the frontier for that first search as shown by the pink nodes here grows linearly we have seen that it adds a fixed number of nodes at every depth whereas the frontier for breadth first search grows exponentially and that also we have seen essentially that's shown by the yellow nodes essentially and that is in some sense a measure of space complexity so let us compare the two algorithms on four parameters time space quality of solution and completeness we have already seen the time complexity for both the first search and breadth first search is exponential so they are in equal in that so remember the time complexity we are measuring as the size of closed and space complexity we are measuring is the size of open essentially these are just you know estimates when it comes to space complexity we see that that first search is a clear winner it only grows linearly whereas breadth first search grows exponentially in terms of quality of solution there are no guarantees by that first search it may find a path which is longer as opposed to breadth first search which will always guarantee the shortest path because it is always generates a nodes closest to the source first essentially so whenever it finds a gold node it will find it at the shortest distance from the start places actually so that's a plus point for breadth first search essentially what about completeness by completeness we mean that if a goal node exists or if a path to a gold note exist then will it will the algorithm find it that's what is our notion for completeness so we also use the term systematic sometimes for this and by systematic we mean that will it explore the entire state space eventually which means that in other words what this would mean is that if if there is a path to the gold node will it find it now if i give you a graph which is something like this s a just a trivial graph c d and let us say there is a gold node which is not connected to any of these nodes essentially so this is a situation of no path to google node so obviously whatever you do you can't find a path to the goal node because there is no paths to the cool node but when you talk about completeness or search being systematic what we mean is will it systematically explore the entire space by this we mean okay it looks at the node s then it looks at the node a then it looks at the node b then it looks at the node c then it looks at the node d it has seen all the nodes that can be reached which are reachable from s so entire graph which is connected to s it has explored and if it does that and only then it will say that i can't find the paths to the goal node so it can report failure essentially so but the corollary of this is that completeness means that if there is a path to the goal will the algorithm find it essentially and we have already seen in that small example that that that first search could go down in infinite path especially if the search space is infinite essentially whereas if there is a path to the gold node and we have already seen this earlier then breadth first search will find that path because of the fact that it it looks at parts of increasing length essentially first it looks at paths of length 0 if the start node itself is a good then you can return that otherwise it looks at the first level nodes paths of length one then it looks at second level nodes parts of lens two and so on so if there is a path and if there is a path it means it will be a path of finite length but for search is guaranteed to terminate you know with the path essentially whereas breadth first searches that for search is not not guaranteed to do that for in especially for infinite graphs so you can imagine infinite state spaces for example number systems will have problems which you know uh have infinite nodes because there are infinite numbers and you are looking for some numbers with certain properties maybe you know numbers which satisfy which which which don't satisfy uh formats last theorem are there three numbers such that you know the format's last theorem is not true for them or something like that uh the subspace is infinite and if there are three such numbers then therefore search may get lost into an infinite path whereas breadth first search being conservative will will find that path the condition is there must be a path of finite length from the start to the goal node essentially so for example if there is an infinite search space and the goal node is here this is the example that we had seen earlier that first search will just go down some path here and keep going down this path and it will never find the path to the goal node so it's not it's not complete it's not guaranteed to find the path if there exists one whereas breadth first search is guaranteed to find a path if there exists one so our next sir one doubt so there are two factors uh for this difference right one is finding the path and one is whether the search space is infinite or not yeah so for depth first search see if the search space is finite okay then both algorithms will explore the entire search space and and say that path exists or doesn't exist okay so here's a small example this is the start node this is the gold node this is a v c d let's say in this case there is a path to the gold node so if the if that g node was not there the g node was separate as down on the top then both will explore that s a b c d and both will say no path that's for the case of finite listing even in the case of a finite graph for the one which i have drawn here at the bottom breadth first search will give you the optimal shortest path why because it will look at s first then it will look at a then it look at b then it will look at g whereas what will depth first search do the first search will go from s to a from a it will go to c from c it will go to d and from d it will try b and then it will try g so it will find the path s a c d g it will give you not the shortest path so that's that's a third property quality of solution that first search guarantees the shortest path between search as this example show will not find the shortest path in the case of infinite search spaces if if there is no path to the goal then both will go into an infinite loop because you know they'll keep searching but if there is a path of finite lengths to the goal space then best for such will find that path because it goes down level by level recently and because of power and the goal node is at some finite level finite distance away from the start and brethren search will eventually look at that whereas that first search as shown in the middle diagram here which i do can go down a path and never come back from there so both algorithms have their advantages and the fact that we are fighting against exponentially growing trees is is not something that you know you should take lightly and which is one of the reasons that some of the algorithms that we will see will try to address that issue that can we you know work with smaller space complexities and time complexities but for the moment we will turn our attention to an algorithm that uses linear space for open which means it got the good property from that first search and guarantees the shortest path which you know good property from breadth first is there an algorithm which combined these two things so i will ask you to think about this and we will take this up in the next session