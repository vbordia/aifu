and one more question uh if we consider that the search space is infinite but in that infinite space we know there is a solution so in that case and we are not uh concerned about the solution how we got that solution so in that case should we use dfs or bf if you know that the subspace is infinite then it's best to use breadth first search i think because that that will guarantee you finding the solution this thing if you know that the surf space is finite then you could have a choice between the two and then you have the trade-off of how much space complexity is required and you know whether you get the optimal path or not if you want to guarantee if you want completeness then you know you have to go by breadth first search essentially but these are not traditionally used for problems traditionally we use heuristic search and when we come back to that we will again return to this issue okay and for that end queen's problem you said we are uh most likely to use uh depth sources right no i didn't say that uh yeah i mean you could use that for search i think see the advantage of that first search is uh and i will point it out when we talk about heuristics the advantage of that first search is that for example in principle you can maintain only one copy of the board supposing for this n queen n is 25 000 or something like that that means every board position will have 25 000 rows and 25 000 columns in it so you have to store that thing right but if in depth first search in prince instead of adding everything to open as a stack you can explicitly say okay generate this move generate this move and then undo that move undo that move so uh maintaining only one copy of the this thing but keeping track of what moves we have made this thing and in that sense that first search is more relatable to us in the sense that it's it's physical that you know as if one person is going in search of something whereas bethesda says oh we have this option this sort from this weapon this option you know it's like parallel essentially okay so we have finished looking at that first search and breadth first search and we want to look for an algorithm which combines the best of both these algorithms which is linear space for open and guarantees of a shortest path so here is an algorithm which we will work towards the best of both the worlds so to speak we begin with an algorithm called depths bounded depth first search this is not our final algorithm but it's a step towards the final algorithm and the basic idea is simple that you do that first search so the basic idea is that you do depth first search which means you are guaranteeing linear space but you have a bound so there's a bound beyond which you are not allowed to search and you will do search only within that so let's say the bound is d so it's a depth bounded that first search what are the properties of this algorithm you are doing that first search with bound d and therefore you need linear space because you are doing depth first search but it's not complete as you can see from this diagram if the goal is outside those bounds it will not find a path to the goal even if the search space is finite and secondly it will not guarantee the shortest path so for example if the search started off in this direction it could find one gold node here when there was another gold node there essentially so if that's the case then it will go down here backtrack backtrack backtrack and find this cool node so it may find the shortest path so it doesn't guarantee you the shortest path it doesn't even guarantee you a path especially if the goal node is not within the bound so these are the properties of depth bounded that first search the the plus point the redeeming point for this search is that it's linear in space because basically it is that force search so how do we what do we do next so this is an implementation of the depth of depth founded the first search you can see that it's not very different from our earlier algorithms its open is maintained as a stack so it is behaving like that first search but instead of node pairs we have a node triple now we are still calling it a node pair but that should not deter you from thinking about it as a triple and what is the third element in the triple as before the first element is the node itself n the second element is the parent of that node that that's what the original node pair was but the third element is the depth value is how deep the node is from the start node essentially so essentially if the node is not a gold node then when we come to this else part we check whether the depth of the node remember the depth of the node is now stored along with the node it's less than the depth bound which has been given to you that's given as an input argument this thing that search only up to this depth if the depth is less then you go through the motions of generating children removing seen and all that kind of stuff that you do and then add it to that but when you add it to that you make sure that you have increased the depth of the children by one essentially so you will only explore up to depth form so this is basically depth first search modified to function within the depths bound which is given as a parameter to this algorithm now there is a variation that will be of interest to us in which we will also return the count of the nodes visited by a call to this algorithm we have called it depth bound dfs 2 the difference between this and the the previous algorithm is that we maintain a count so initially we start with count equal to 0 and as we progress we add as to how many new nodes we are seeing in every cycle inside it's still doing that first search but it keeps track of how many nodes that you are visiting so this new pairs every time you add you add that length of that new pair so length is a function we will assume which will take a list and give you the length of the list so the list is new pairs that we have generated as before and length will tell you how many elements are in the new pair and we add it to count essentially so this is just a variation and we will see why this variation was needed where you need to count those nodes essentially but let's first get to see whether we can achieve our target goal so this is a question we'll come back um to in a moment i think so here is an algorithm which is built upon that it does depth first it's called depth first iterative deepening so the first thing to observe is that it's a depth force algorithm and the second thing is that it increasingly looks at increasing depth bounds essentially so here we have just initialized count to minus 1 just for the sake of correctness uh and we don't have a path yet and depth bound is initially zero and what we are going to do in iterative deepening is iteratively increase the depth bond so initially we start with depth problem of 0 and then we do we make a call to depth first that's rounded that first search 2 which also returns to us a count and it returns a path to us essentially and if we have found a path then we return the path if you have not found the path we go back and so so this repeat until work still either you have found the path so if the path is not empty or the previous count is equal to count now why are we doing that well so that's a question that let's address that first is that supposing we have this search space of the kind that we have been looking at a simple subspace but in which there is no paths to the goal node let's say this is the simple part and then we have a path to the goal node now imagine if you were not maintaining this count what would happen uh iterative deepening would say what does it what is it saying in in it's saying first look for a path of length 0 and then it goes into this cycle so this is deepening next time increases that bound by 1 then look for path of length one then look for path of length two then look for path of length three and so on and so forth but in every such cycle it is doing that first search so the property of linear space we are maintaining but we are also now by this process of deepening we have also adopted the behavior that breadth first search would have done that look for parts of length 0 looks of paths for length 1 then look for parts of length 2 and so on and keep increasing that now supposing we did not have this previous count is equal to count this thing that means that when it looks for paths of length let's say in this particular example that i have drawn initially parts of length 0 we don't find the paths to goal that means s is not equal to goal then you look for paths of length 1 then you don't find a path which means that a b or c neither of them is the goal then you look for parts of length 2 and you don't find a path because it is d is not a goal then in the next time we will look at paths of length three and four and five and so on and every time it would say gold not found gold not found gold not found essentially and it will get into this infinite loop essentially so to get around this fact that you are exposing paths of increasing length but only till the extent that in every cycle you are adding a new part of the state space to the search this thing when will that not be true when if in one cycle for example in this example you have inspected five nodes in the next cycle you again inspect five nodes in the third cycle you again inspect five nodes that means you are getting into a loop and you can cache that by saying that previous count is equal to count so that's the reason we want to maintain count of how many nodes we have seen in this cycle of depth first search and in the next cycle of depth first search if they are the same that means there are no new nodes to be inspected and we can say that we have finished this thing that's the reason for adding count essentially so the other thing that's happening is that because we are doing search of increasing depth we are essentially doing behaving like breadth first search but because we have implemented that first search or a series of that first searches our space complexity is going to be linear essentially so in that way you can see that it combines both the good features of breadth first search and that first search so that question of why i think we have just addressed but there is still a doubt and this doubt was raised in a class that i was taking in iit dhargar in 2019 by a student called siddharth sagar so we must give him credit he asked this question that does dfid always return the shortest path that's the claim we have made okay so why is this claim that we have made we have said that we are looking for paths of increasing length and the first time you find a gold note it must be the shortest path to the gold mode that is true but what siddharth pointed out was that because you are using close to proven nodes this property may be violated so let us look at an example to understand that here is a state space where you can see that there are several paths to the goal node and on the left hand side is the move gen function so by now we are familiar with that how will dfid work with this state space so remember that the order in which you explore nodes is given by the move gen function so when you generate a and b as the children of a of s the fact that you look at a first comes because in the move gen function a has been written first essentially so that's a simple thing that applies to all the all all the nodes so what happens remember that we are maintaining this closed list and we are not adding new nodes which are present in closed essentially and also in open but that open doesn't matter in this example so at depth 0 we will look at start node s then at depth 1 we will look at this sub subspace of paths of length 1 from s to a and s s2b and so on at depths 2 we will look at parts of length s a b s a c and s b d essentially now at this point maybe you should observe that the shortest path to the goal node goes from s b d to g essentially but what happens when you look at paths of length 3 or depth equal to 3 what is this tree is search space explode you go from s to a which is path of length a one then you go from a to c which is path of lens two then you go from c to e and to d which are parts of length three then because this is none of them is a goal you go back and go to the node b but b has no children why because all its neighbors s and d are already enclosed so remember that s is enclosed then a went into close then c when it took close and e when it took close and d went into close because d is already on closed you will not generate this path and dfid will miss finding the shortest path so here we look at it again if you are checking loops with closed then this is how dfid will behave first it will look for parts of length 0 then it will look for parts of length 1 then it will look for parts of length 2. remember it is doing that first search essentially so from a from when it's looking for parts of length 2 it will go from s to a from a to c and then it will see i've done parts of length two then it will go back and go to b and then try this path to length d then it will go part to lens d that's what we saw in the previous slide also it finds the path to node d which is of length 3 from s to a to c so this is the fine node it finds to path it finds a node d but because it has found this path to node d it does not generate d as a child of b because d is already on close by that remember it's doing depth first search and going in that particular order so it does not find it it does not generate d as a path of b and it finds the paths of the goal only in the next round in which case it finds the longer path as you can see s a c e g essentially so what is going on here who's the culprit the culprit is the fact that we are not allowing nodes which are already on closed to be added to open again that's what happened here essentially so because we have d already on closed it's not generated as a child of b at all and which which is why in the next cycle it is generate goal is generated as a child of d remember this is doing depth first source so it will first go down this path and only then it would backtrack essentially so here also it first goes down from s to a to c to e to d and then it goes to b but after that b will not generate d as a child so we cannot afford to do this checking with closed in dfid essentially so without close dfid finds the shortest path so if you don't check for close how do we find the shortest path so we will find the shortest path but how do we reconstruct this is the question so have we thrown the baby out with the bath water that you know in this effort to find the shortest path we have done something which doesn't let us find the shortest path so we are undoing that and that undoing is that we will not prune nodes which are already enclosed we will allow them to be added but then how do we reconstruct the path because one of the things that closed was doing for us was reconstructing the path essentially so here are some points i will leave you with and we still need the parents node nodes to reconstruct the path essentially so we should still maintain closed as a data structure but just not use it for when we are adding new nodes to open essentially so we can still store the node pairs and close and use them for reconstruction but have to be careful to choose the right node pair because you know there will be multiple parents from which a node can be generated so we saw d for as an example in the previous case from s you could find the path from b to d but you could also find the path from a to c to d essentially and then eventually when we find that that g is a child of d we have to be careful not to choose this parent but to choose this parent because because both will exist in the closed list d with b as parent and d with c as parent so we have to be a little careful that by reconstructing the path we construct the shortest path it has found both the paths but we should find the shortest path perhaps one simple way of doing is to store the entire path as a at each node so what what would that look like it would say that okay this is s then this is s comma a which means from s going to a this is s comma b then this is s comma a comma c and this is s comma a comma was there a link from a to d no okay is that not there so s comma b comma d and then in the next cycle it will find s comma a comma c comma e i think so we are storing the entire path and there you would have found the path s comma b comma d comma g so if you are storing the entire path at the point when we pick g as a goal node we already have the path because it's part of our representation itself so we don't have to do all this reconstruction business and simply return the path so maybe that's one option essentially otherwise you have to figure out how to store all parents and but choose the right parent this thing if you're looking at configuration problems so if you look at for example you know end queens or sat or map coloring then the path is anyway not important so does dfid make sense that's a question you should ask yourself it could help in some sense in some cases by finding the solution faster because shorter solutions are there but path itself has no importance except from the time complexity point of view path is important when we are only looking for planning problems where we want to return the path and in particular if you are interested in the shortest paths finally a comment that dfid was actually invented by chess playing chess programmers people who write programs to play chess so in the game of chess the rules say that you are allowed a certain number of certain amount of time to make a certain number of moves so typically it may be something like 2 hours or 30 moves but the player is free to choose how much time he spends on the first move he or she makes the second move and so on and so forth so very often what happens is that you come on the time pressure in which case you don't have too much time to find a move when we study game playing we will see that essentially chess playing programs do some variation of that depth first search up to a certain depth so that's called ply that th they look six moves ahead or eight moves ahead or things like that depending on how many resources you have instead of keeping this depth constant if we implement something like dfid iterative deepening then after the first cycle you know what is the best move after the second cycle you know what is the best move you keep going till the calling program says give me a move essentially and that calling program is keeping track of time how much time is left of so it gives you this flexibility of making moves quickly or taking more time thinking over the move and this dfid allows this anytime move generation facility that you keep searching and when i tell you give me a move you give me a move so it was really invented by chess playing programmers so this is a summary of that first iterative deepening you start with depth bound one and you keep searching till greater depths so you do this depth founded dfs i have not mentioned the fact that you are keeping count of number of nodes in those special situations but the general idea is that you keep searching deeper and deeper using that first search and because you are doing depth first search you will require linear space essentially but you must also be careful not to check in closed when the path through the goal is found it must be the shortest path because otherwise it would have been found in the previous iteration essentially so you are looking for paths of length one then lens two then three and whenever you find the goal node that means it must be the shortest path essentially so that's an informal argument the question is is there a catch are you getting the best of linear space and shortest path what is the extra cost you're paying now remember you're doing a series of searches you're not just doing one search you're saying depth search up to depth once also to depth 2 and so on what is the extra cost that you are paying so we can do a quick analysis of that is that for every new layer that tfid searches it explores the entire tree up to that layer all over again so if you are looking at some layer at depth d where there are l leaves then whatever the internal nodes which are there you are visiting them all over again see if breadth first search would not have visited them again but it would have inspected this leaves in this order the fid is actually that first search but it's masquerading as a breadth-first search because its behavior is like a breadth first source first nodes at level 1 then nodes at level 2 and so on but in order to do that it has to do all the internal nodes all over again so when when breadth first search would have seen l nodes dfid inspects l plus i nodes what is i i is the internal nodes of that tree remember we are assuming that the constant matching factor b how much extra work is it doing is the question that we want to ask now it turns out that this is the extra work instead of looking at l nodes it is doing l plus i nodes so the number of nodes seen by dfid as compared to the nbfs is l plus 1 l plus i divided by l essentially so how much is i and how much is l what are their relative sizes and this should give you some insight into exponential growth what does it mean for a tree to grow exponentially now if there is a full tree with branching factor b then the number of leaves in the tree are b minus 1 into i plus 1 essentially in other words the extra work that and you can work out the details because we have we have said that this ld is something which is going exponentially the ratio of the node seen by dfid to dfs is simply just b over b minus 1. for large b essentially and you can see that this extra cost is not significant at all essentially so again if b is 10 for example then this is 10 by 9 little bit extra about you know 10 extra or something like that surely you are willing to pay that price for this advantage of saving on space essentially so we are paying there is a trade-off we are spending more time but we are saving space but the amount of time we are spending more is not too significant so we can afford it essentially so that's an advantage so i would like to introduce this monster as they say in western mythology called comebacks the complex that ai fights is combinatorial explosion the search tree is growing exponentially essentially here is a searchly with branching factor three and you can see that you know it's really growing exponentially so it reminds me of this great mythology you know where hercules was fighting this dragon hydra or something and every time hercules would cut off one head of hydra some three four more heads would appear that's what's happening in our search theo so you inspect one node and it adds three four more children essentially so in that sense i called it a monster because greek mythology calls hydra is a monster but that's what we are fighting we are finding combinatorial explosion that an exponentially growing search tree we cannot afford to spend exponential amount of time or exponential amount of space searching this so this is an example with branching factor 3 and you can already see that the tree is growing very rapidly so we do need other approaches to be able to solve real problems uh especially if those real problems have you know branching factor which is larger and you can imagine that in any typical situation the number of choices that you may have is likely to be definitely more than three and so the plan the search tree is going to grow exponentially how do we tackle that one observation we can make is that the first search always behaves in the same fashion irrespective of where the goal is so in this diagram i have drawn the goal at three places it doesn't matter which of these three is the goal that you are looking for the behavior of depth first search is going to be the same it is irrespective of where the goal is it is oblivious of the gold totally it just goes about its way in a predetermined fashion so obviously that's not a good strategy the same the same is true for breakfast search also irrespective of where the goal is breakfast search goes about as business in the same fashion it you know gradually moves away from the start node to the gold node essentially so all three algorithms which includes dfid are called as blind search or uninformed search they are blind in the sense that when they are searching they are not aware of the goal where is the goal and we also call them as uninformed whereas a good search algorithm should have some sense of direction you know it should realize that okay the goal is in this direction so i must search that part of the search space or state space and that we will see next as the topic of heuristic searches actually