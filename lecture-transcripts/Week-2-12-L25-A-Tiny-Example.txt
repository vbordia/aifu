search is conservative that it says okay i have already generated some nodes earlier let me look at them first then i will go to the new nodes that are generated so the fifth node here is the first new node at level 3 that it sees then it will go to 6 and then we'll go to 7 and so on in that manner it will go down the search tree level by level and its behavior is as illustrated in the state space on the left where where we are shown the order in which the nodes are expect inspected and as you can see this is in direct contrast to how that first search space looks it goes down till it hits the boundary or the edge of the state space then backtracks and keeps trying the newest children first essentially so these are two search algorithms and this is how their behavior is different this difference is achieved simply by where do you add the new nodes to the open list essentially if you add it at the head or the front then it behaves like a stack and it always rushes down into the search tree or dives down into the search tree if you add it at the tail or make it a queue then it goes systematically further and further away it tries to in some sense stay as close as possible to the start node essentially whereas that first tries to be as far as possible from the start node essentially and we will see that this property will result in breadth first search always uh finding the shortest paths essentially and that first search will not be to that so here's an example of a small search space so i hope you are familiar with this notation by now on the left hand side we have the state space and we have the moves all the moves are reversible if you go from s to b you can always go back from b to s and likewise so s is the start node and g is the goal node that is the tradition that we will follow just for the sake of agreement so that we our discussions are clear on the right hand side is the move gen function that we have so from s you can go to abd from a you can go to cbs and so on and so forth we also said as to what will the goal test function do the goal test function returns true only for the node g and for all the other nodes s a b c d it will return false essentially so we have said that our search algorithm is generate and test generate a candidate test whether it's a goal or not essentially so it terminates when it when when when it finds a goal node so we had looked at three cases of writing this search algorithm so let's look at them individually the last case we saw was when only new nodes are added to open which means that the remove scene function that we had written removes nodes which are already on close and already on open and they are not added again in this example both depth first search and breadth first search explore the same search tree the order of exploration is different essentially so what's happening here if you just lower it again fresh you start with s then you generate the children of s in which order do you generate the children the order as given by the move gen function d so in the first step both will generate the children of s and in in the first step both that first and best first would pick up a next they'll pick up a so if we are finished with s then we pick up a and we generate the child c of e both will generate the child c of a but depth first search will go to c now because of its strategy of picking the newest nodes first breadth first search will go to b at this stage essentially right so this is let me write notation third node for depth first search and this is the third node for breath first search anyway so depth of search will look at c and then there are no no children of c no new children of c c has children as you can see here b and a but but b is already on open and a is already on closed so no new children of c are added so that first search will finish looking at c and then backtrack and the next node that is there in the open is b essentially and then it will look at b b also will have no new children because all its children which are s s a and c are either on open or closed so it will backtrack from b and go back to d and then generate g after that that is depth first search what does breadth first search do it starts with s goes to a then it goes to b because that's its nature then it goes to d by the time it is inspected d they have both generated g but both of them will inspect c before g p because that is always ahead in the in the open list essentially whether it's a queue or a stack essentially so both will generate the same search tree but they will explore it in a different order and i would encourage you to do this whole simulation yourself that was case one when only the new nodes are added to open one case that we looked at was that if a node is on closed then you don't add it to open but if a node is on open then you are allowed to add it to open and in this case and we have seen this kind of these cases earlier except that now we have have the added restriction that we always pick the new node from the head of open and the two search algorithms put new nodes at different plus parts in the open so as you can imagine first the first search picks s it generates the three children abc abd then it generates it picks a what are the a's children is is neighbors are s b and c s s is on closed so it doesn't add it generate s as a child of open b is on open already but we are only pruning nodes which are enclosed so b is now here added as a child of a so because we are not pruning nodes which are on open the search tree that will be generated and i will encourage you to verify this yourself is the one as shown on the left likewise breadth first search will also start with s then it will go to a generate the children again only it will keep c and b then it will go to b now when depth first searched inspected b at that point it had already added c to closed but when brett first search inspects b for the first time so this is the first time when breadth first search inspects b essentially this is the this is the first time when node b is inspected by depth first search so when when when depth first source inspects b it has already put c on to close so it doesn't generate any children but when breadth first search inspects b for the first time c is not been c has been generated but not on open but not on closed means it has not been seen it has been generated but we are allowing that and therefore this new c is is can be added to the source so what we are trying to see here is study as to how different variations of our search algorithm what is the search tree that they generate and in what order do they inspect the search tree so the case 3 which is where we add the most number of nodes all new nodes are added which means the remove scene function is not even called essentially anything which is generated as a child of a node is added to open where is it added that depends on whether it's that first search or breadth first search so because we have this unrestricted addition of nodes to open as you can see that first search will go into an infinite loop it generates a abd initially that both the searches do it picks a it generates children of a which is cbs so it adds all the children essentially including s even though s is already on closed so remember this is on closed this is on close but it adds everything so then as i as as as as the children of a it will add cbs then it will go to c and c has got two children b and a it will add both of them and you can see that it's next going to go to s because it is allowed to add s to open and this essentially means it started with s went to a went to c went to b and it's gone back to s so it's gone into a loop and it will just keep doing this all over again so that's one of the reasons why we had introduced the fact that you want to check nodes which are unclosed so that it doesn't go into loops that was the primary purpose of adding the closed list to the algorithm so that first search as you can see even in this small graph it's got just six nodes is not able to find a path to the goal unless you explicitly avoid loops essentially if you avoid loops and and don't put nodes which are enclosed then you can see that this s will not be added to open and then it will have to shift its attention to in fact not only s this a also will not be added and this c also will not be added because by then it has seen all of them so it will even this a will not be added because it's already unclosed by the time so you can go through this that if you are pruning closed it will find the path and we had seen this earlier as well what about breadth first search breadth first search as you can see grows breathwise now we said it goes level by level and things like that so brethren also generates its three children a b and d and it inspects them in that particular order it first impacts a then it inspects b and then it inspects d this is also not constrained it's allowed to add all neighbors to open list so that's what happened when it generates when it inspects a it adds c b and s to open that's allowed then it goes and inspects b it adds s a n c to open then it inspects d and it adds g to open but it'll not inspect g now after this so what is the order this is the first node little c this is the second node this is the third node this is the fourth node then it will go and inspect c because that's the order of breadth first so it's level by level finish the first level first then the second level then the third level so it'll do five then it'll do six all the time it'll keep adding these new children here seven but when it comes to this nine ten eleven and on the 11th turn it would have found the gold node that is a property of the fact that it is not impetuous like depth first search it doesn't dive into the search tree that it is generating it says let me finish all the notes that i have generated first level by level and it finds a path to the goal node so that's an advantage of breadth first search over depth first search essentially so we'll do this analysis in a little bit more detail uh in the next