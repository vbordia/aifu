so welcome back ah we just looked at west first search and hill slamming we found that best first search is a complete algorithm which will find the solution but it requires exponential amount of time in ah space typically then we looked at local search algorithm which is hill climbing which simply follows the gradient and we saw that it is computationally efficient the only problem is that it doesn't always find the solution essentially it gets stuck in local optima essentially whether it's if it's a maximization problem and a local maximum otherwise in local minima so let us look at some approaches to get around this problem of getting stuck in a local optima or optimum using hill climbing so this is a recap it is a constant space algorithm it only looks at its neighbors it moves to a better node if it cannot then it terminates so time complexity is also good its termination criteria is different it stops when it has reached what it thinks is the optimum however that may not be the global optimum that we are interested in essentially so we want to see how to escape from this local optimum that hill climbing can get stuck in and how to move towards a global optimum so we had seen this with the eight puzzle example that hill climbing would have got stuck at this local minimum and we said that we need algorithms that can do better than hill climbing and we will look at these three deterministic algorithms next we also define this notion of solution space or plan space search to begin with so what do we mean by solution space search by solution space search we mean a formulation of the search problem such that when we find the goal node we have the solution and we are done essentially we do not have to reconstruct the path configuration problems obviously fall easily into this category every node in the search space is a candidate solution and a candidate is a solution if it satisfies the goal description but we will see later that planning problems can also be solved in the solution space and then we say that they are we are solving them in the plan space every node is a candidate plan and these algorithms are known as plan space planning so we need to observe the difference between synthesis and perturbation synthesis methods are constructive methods and starting with the initial state we build the solution piece by piece so for example if you were doing the n queens problem then we would place the first queen on the board then we would see where to place the second queen we would try different options if you are doing that first search and eventually find a position for that and then look for a position for the third queen and the fourth queen and so on essentially on the other hand look at the following representation we have an index which represents the row number on the board and we have values for those indexes which are the column numbers for on that board essentially so when we say that the first element in this list is a we mean that the first queen or the queen in the first row is in column a as you can see which is on the diagram on the this thing then when the second the queen in the second row is in column c as shown here and the rest of the thing says that the third queen is in column e the fourth is in b and f and d and so on essentially so this is a representation of the end queen's problem as a singh one dimensional array where implicitly the first element is the queen in the first row the second element is the queen in the second row and so on and what we have stated is the column in which the queen exists now if we take permutations of this representation of this one dimensional array then we would end up generating new candidates so this is what we mean by perturbation that we take a candidate solution so for example a c e b f d it may not be a solution but it is definitely a candidate solution so what is it saying it is saying that the third queen is on e which is here the fourth queen is on b which is here the fifth queen is on f which is here and the sixth queen is on d which is here so this is not a solution obviously because you can see that these two queens are attacking each other essentially but nevertheless it's a candidate solution and if we did some amount of perturbation either you know taking ah shuffling the array or something like that then we would generate new candidate solutions and that's the idea of perturbation so let's come back to the sad problem given a boolean formula made up of a set of proportional variables each of which can be true or false or one on zero to find an assignment for the variable such that the given formula evaluates to true or to one essentially so for example here is a formula ah written in proportional logic or boolean this thing can be made true by giving these values a is equal to true c is equal to true d is equal to false and e is equal to false and there may be other evaluations also very often sad problems are studied in conjunctive normal form of c n f so the above formula is not in c n f because it has things for example the implication sign and so on but in the c n f form the main connective is an and and inside every clause as this is called so this is called a clause we have only a disjunction or a negation sign essentially so essentially we can say so many clauses are there in the formula so let's see how solution space and search and perturb methods perturbative methods can be combined the solution space is a space of candidate solutions um search method generates neighbors of a candidate by applying some perturbation to a given candidate so that's a moved and function and in some communities we also call them as a neighborhood function so a sad problem with n variables has two raised to n candidates because there are n variables each variable can be either true or false or one or zero so there are two raised to ends possible candidates where each candidate is a n bit string because if there are n variables and there are n bits essentially so when n is equal to 7 for example as shown here a neighborhood function we could choose a neighborhood function or perturbation function which changes only one bit essentially so these are all the neighbors for this particular candidate where the bit that is changed is shown in red essentially so a search algorithm would navigate this space this space is a solution space where every node is a candidate solution and of course we can evaluate whether it is a goal node or not essentially the other problem that we would be of interested in is the travelling sales person problem or the travelling salesman problem which is considered by many to be the holy grail of computer science so this is how the problem is defined that given a set of cities and given a distance measure between every pair of cities remember every pair of cities the task is to find a hamiltonian cycle visit each city exactly once and in addition we have a con constraint that we want a tour with the least cost in the traveling salesman problem when the underlying graph is not complete for example in the real world not every let us say these are flights not every city has a flight to every other city so we will assume that we can connect them we can connect the missing edges we can add the missing edges and with a very high cost essentially and the basic idea is to maintain the fact that any fermentation of cities and by permutation here we would mean the order in which you are going to visit the cities is going to be a possible candidate so you can say that you can go from this city to this city also even though there is no edge between that there may not be an edge between that or something like that now this tsp is a much harder problem than ah sat ah and it falls into the category of being an np heart problem ah which means it that it needs at least exponential time and what is more is that when you are looking at a candidate sat solution you can look at the candidate and decide whether it's a solution or not when you look at a candidate tsv solution you can of course decide whether it is a tour or not but you cannot decide whether it is the optimal tour or not so in that sense it's a much harder problem than sat essentially now if you go to this website tsp live they have a collection of problems which they have got together from various sources and some of them are like real world problems how can we solve the tsp so first let's look at a greedy constructive methods the simplest is the nearest nearest neighbor heuristic it says let's start at some city and move to the nearest neighbor as long as it does not close the loop prematurely we will see this in a moment this is a greedy approach you start at the some city and move to the nearest neighbor as you can see it will not work with this problem so for example if i were to start with this then i would go around like this and then i would probably go here and then i would probably go there and then i would probably come here obviously now clearly not the optimal two essentially so it's it's a np hard problem so it's not easy to find optimal load especially it's not easy to find towards using greedy methods even for problems which have additional constraints so for example one of the con constraints that we may want to add to tsp is that the distance metric the distance measure is a metric essentially which means it satisfies the triangle inequality and so on and for such problems its typically not so hard to find problems but in the general case it is a hard problem a variation of nearest neighbor would be to extend the partial tour at either end essentially why just go in one direction you could do go in both the end another approach is called the greedy heuristic ah and it works like this it's a little bit like kruskal's algorithm for finding the minimum spanning tree you sort the edges add the shortest available edge to the tour and you keep doing that as long as you are not making smaller loops essentially so here is a nearest neighbor approach just to get a feel of what it does you start at some city and move to the nearest neighbor as long as you are not closing the loop so let us say we choose this as a starting city and essentially we go at every step to the nearest neighbor and we will see that this is a kind of tour that would be constructed clearly it is not optimal again ah for example for visiting this city it would have been better if you had come here and then gone from here to here and things like that essentially but it is a greedy approach and it can sometimes give you reasonably good tools essentially the algorithm known as the gradient six is that add the smallest edges first so for the same problem here is a snapshot of what are the edges that you might have added before you continue from there so from there we will look at the next smallest edge we cannot add this edge because that would close a loop there but we can't add this edge also because you can visit the because that would mean that one particular city is visited more than once so one of the properties of tsp tour is that every city has one incoming edge and one outgoing edge so you cannot have three edges on tower city but you could have had added this edge next and maybe you could have added this edge next and so on and so forth you keep adding the smallest possible edges and till you have some sort of a solution maybe you should try it out try to complete and see what will happen with this particular problem instance there is another approach called the savings heuristic which is quite popular and the way it works is as follows that you start with n minus 2 of length 2 anchored on some base vertex and then you perform n minus 2 merge operations to construct the tool what is the merge operation you remove two edges connected to the base vertex from two loops and as the add the edge to connect the two hanging vertices essentially which two the two edges are chosen such that there is maximum savings of total cost in the resulting graph that's why it's got this name savings heuristic so here is an example you start with this constructing this so there are five cities in this so you can see that there are four tours of n minus one four four tours of length two essentially starting with the anchor city which is the dead city so every red city is connected to every other city and that forms a small loop then we remove these two red edges and connect those hanging edges with a new one then we remove these two red edges again and connect the hanging cities and in the last step we remove these two red edges and connect the remaining hanging cities and what we have is the two is actually so just to illustrate how the savings heuristic works it is a very popular heuristic use for solving tsp so this is something which i had downloaded from this website but at the moment i could not find this link again so anyway so this is a sample run created by these algorithms the greedy tool the nearest neighbor tools the savings tool and also we have the optimal tour essentially so you can compare how the different greedy approaches perform with respect to the optimal tool you can see that the greedy tour and the nearest neighbor tour has lot of long lines thrown here and there like for example in the example that we saw earlier and the saving tools seems to be performing a little bit better it may or may not be optimal so now let us look at perturbation operator we have we have looked at greedy constructive approaches for constructing tools now let us look and see that if you are working in the solution space where you are given some tour how can you perturb that to produce new tours so one of the simplest operators is called the two city exchange where the two shaded series that we have seen so assuming that the original tool was going from left to right and there are some edges which i have not drawn so they would have been here and here and here and here but if we exchange these two shaded cities then what do you mean by exchange we are saying that instead of going from this x to this y you do not do this instead you go go to the other other city that you are exchanging instead of going to this city which we have called y go to this city call which you call z so we exchange y and z essentially and we have changed the two so if this was a starting tour and these are the two cities that we were exchanging then this is how the tour would look after we have exchanged the two cities what do you mean by exchange because you know we are assuming that the tour is represented by what is called as a path representation where it is a sequence of cities going from city one to city two to city three and so on essentially so and we are simply changing the order essentially the two cities can be selected in nc two ways obviously so that would be the neighborhood function sometimes people prefer edge exchange instead of city exchange why is that because it is age which contributes to the cost of the tour and maybe if you are using edge exchanges then you can try to remove longer edges essentially so we have removed this edge and this edge and we have added this edge and this edge so we have got a new tour again edges can be selected in nc two ways three edge exchange says that remove three edges from the tool some three edges may be some three long edges that you do not like and replace them with some other edges so that we have a tour so what you see on the left is that we have removed three edges so we have broken the two or two into three separate components so we want to reconstruct the tool and this can be done in four different ways as can be seen here and those would be the neighbors essentially so the three edges themselves can be selected in nc three ways so you can see that there these are different neighborhood functions what is the state space the state space in the travelling salesman problem is all permutations of all the n cities and what the neighborhood function does is connects some candidate to us to some other candidate to us and these are the two different kinds of things we typically do as city exchange or edge exchange let's talk a little bit about complexity both sat and tsp problems are hard problems we have already said that both have huge search spaces and hence we are looking for local search methods sat is np complete in fact it was the problem that was used to define the np complete set by gary and johnson it requires exponential time to solve but can be verified in polynomial time as i said earlier tsp is np hard it requires factorial time to solve because you know you are choosing different permutations of cities the first city you can choose in nba is the second city you can choose in n minus one base and so on and moreover it cannot be verified easily because you may construct a tour but how do you know whether it is optimal to or or not essentially so these are the this is a gives you a feel of exploding search spaces so the number of candidates in sat is 2 raised to n and this basically shows that if n is equal to one there are two candidates if n equal to two there are four candidates and so on and so forth as you go to 100 n is equal to 100 then we have something like 10 raised to 30 candidates which is a huge number of the space is huge essentially so if you have a 100 variable sat then this is how the space would look like tsp is even worse initially of course if there is one city then there is only one tool there is no tour in fact if there are two cities then you can think of two tours essentially in fact actually you can think of only one tour if there are two cities what i have written here is n factorial n factorial of 2 is 2 essentially but in practice if i choose this as a first city and if i choose the other one as a first city so let us say this is p and this is q so that tour p q is equal to the q p essentially because it's just a the direction that you are changing essentially so in in general if there is there are entities then you can rotate them and you can reverse them and they will represent the same tool but we have not written that we have just mentioned here n factorial so in in practice it should be n minus 1 factorial divided by 2 this 2 comes because of the fact that the reverse of a of a string is the same tour essentially in any case the point is that they tend to grow as a factorial and the factorial function you should study is grows much much more rapidly than ah the exponential function so for n is equal to eight for example there are only two fifty six candidates but in the factorial function there are already forty thousand candidates essentially and as you can see with uh n equal to 50 it goes to 10 raised to 64 and then for n is 100 10 raised to 157 the last column shows the ratio between the space state space of tsp versus sat and you can see that for n equal to 100 it is 10 raised to 127 times more than the sat state space so these are basically huge problems and we cannot afford to solve them by using brute force methods we cannot look at every candidate and say whether it is a solution or not and that is why we are looking at local search methods essentially how much time would you need to solve sat of course you may strike the solution quickly in the sad your first guess may be a good guess or so on but in the worst case how much time do you need if there are if there is a 50 variable sat problem then 2 raised to n is this number which is given here and you can see that what i have done is you are saying that if a million nodes in a second that means we just chop off those six smallest significant bits there and whatever remains is the number of seconds that you will get then i assume that there are 100 seconds to a minute so i have chopped off those two zero six then hundred minutes to an hour so chopped off this 99 then 100 hours in a day dropped off this 89 so you have one one two five days and assuming 1000 days in a year so that leaves you this 1.1 for 100 cities it's quite different and we are talking about sad problem here so we started with 10 raised to 30 and we have reduced it to 10 raised to 24 because we are doing a million nodes a second and then we you know gradually keep removing that many zeros two zeros two zeros and so on and eventually you will see that it is a large number of centuries that you need to solve hundred variable sat problem and we are not even talking about tsp here essentially so clearly brute force is not going to work and what we need to do is to look at some approaches which will be local search methods but which will not get stuck on local optimas actually so we do that next we will start by searching in the solution space or in the plan space