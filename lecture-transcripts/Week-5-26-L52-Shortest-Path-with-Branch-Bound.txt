foreign looking at algorithms to find Optimal Solutions when the state space has Edge costs involved we saw an example of solving the tsp ah using this algorithm called Branch inbound but let us come back to the problem that we originally started with which is that you are at some start node and you want to find the paths to the gold node and the difference is going to be that now those edges will have caused and our goal is to find the optimal solution so let's see how branch and bound works there we have studied with no h cos we have looked at blind search algorithms so that first search the deepest candidates are the best and dies had long into the cell space optimistically and does not guarantee the optimal path essentially we saw that breadth first search treats the shallowest candidates as best stays as close to the start as possible and Wades into the search space to find the solution with the shortest number of hops simply because it stores stays at as close to the source as possible and the same strategy is going to be applied in branch and bound in state space search stay as close as possible to the source node depth first iterative deepening also found the optimal solution but it was basically simulating how breadth first search works so it was the first algorithm masquerading as breadth first essentially then at some point we looked at heuristic functions and the idea was that to find the solution faster because we look at estimates through the goal and then pick the node which appears to be closest to the cool essentially so pick the candidates with the lowest heuristic value in the hope of finding the solution sooner so the objective was to find the solution faster now our objective is to find the optimal solution so branch and bound works as follows that organize a space that does not preclude any solution so we do not want to protect any solution because we want to guarantee optimality that is going to be a necessary condition so local search for example used to always preclude all possible solutions so we just found some solution if we did the service Place could be the state space in which a partial sequence of moves is extended or the search space could be a solution space in which an abstract solution is refined we saw that for in the tsp example but now we are going to come back to the fact that we will have a partial path and we will keep extending the best partial path we will continue looking for the solution whether we are extending it or refining it until the complete solution which known cost is available and no other possible solution with a smaller cost exists again this condition of lower bounding will have to apply and the basic idea behind plants and bound is to prune those parts of the search space which cannot contain a better solution and the reasoning that we did by saying that you should have lower bound estimates will apply here as well so let us say look at this tiny space search problem that we might have seen earlier ah the move then function is given on the left so from s you can go to a b c from a you can go to SPD and so on and so forth and the state space graph is shown on the right and there are Edge costs involved now as you can see the placement of nodes is not necessarily accurate its just a schematic so I have tried to draw them so that roughly they correspond to the length of the edge but it may not necessarily be so I think just look at the edge cos the question we want to ask is what is the shortest path from s to G essentially and when we say shortest we now are thinking of those Edge costs may be as distances so maybe from s to b is three kilometers from B to D is four kilometers and then what would be the shortest path including the edge course not in terms of the number of hops that you make in the state space so as we have been doing each candidate is tagged with an estimated cost of the complete solution in this algorithm that we are looking at which is branch and bound this estimated cost of the full solution is going to be the actual cost of the parcel solution that we have found in other words we will not look ahead towards the goal we will only look at the current node as to how much cost we have incurred up to that node essentially and then we will follow our same strategy refine the best looking parcel solution till the best solution is fully refined and it is best seen with an example so what does branch and bound do it extends the cheapest partial path essentially so we start with the start node and there is only one node so we extend that path and we have now three successors for s ah we use the following ah notation that nodes in yellow or closed nodes in without any color they are on open and this node in cyan or or greenish is the best node amongst the nodes in open essentially so s has three successors as you can see you can go to a with a COS 6 or you can go to B with the cos 3 or you can go to C with a cost eight and the best one is B so we extend that this thing what happens now we are not doing Loop checking here we had seen when you were looking at DF ID that Loop sucking can have its own drawbacks so we are just looking at the algorithm how it performs essentially so we Define B and B again you have three successors you can go back to S ah by this path s b s and that has a cost of six because three plus three ah or you can go to a h from B which is got a cost of five as you can see which is better than the direct Edge going to a which had a cost of six so we have found a better path to E but our goal is the G node so then we Define that best node a because that is had a cost of five and we have added these three neighbors to that as you can see that as you keep going you may be going in Cycles but ah your costs are going up so the costs are going to S is include so one simple Improvement we can do is that if the same node occurs in the path then exclude that path so that will be one simple Improvement to reducing the search space that in any path every node should be included only once essentially so for example you must not include this and you must not include this s and you must not include this B Because B is already in that path that we are following and so on and so forth and that would make the search space a bit smaller but we are just trying to understand the algorithm at this stage we have two nodes A and S who have a cost of six and we expand them in some order to get the three that you see on the right hand side then after the expansion we see that there are two nodes which have the lowest cost which is C and B which cost eight and we would refine them then without going into the details you can see that the algorithm will look at all these nodes whose costs are shown in in yellow because they are all cheaper and eventually it will find this path goes to G which is of course thirteen so you should verify that after all these nodes which whose costs are shown in yellow when it picks G then at that point G would be the cheapest path from start to goal this for example is more expensive but before it takes that this will pick D so the same principle that we saw applies and this is the lungs and bound algorithm applied to State space searches actually we can of course improve it by removing duplicates so as I said you can remove this B from here you can remove this s from this Source node you can remove this s which means you will remove all of these from the search node ah and what else so you can see what we are doing is we are exploring partial paths so what are the partial paths so for example this path says that you go from s to C and C to G and you just calculate the COS s two c is eight and C to G is eight so that sixteen you go from s to A to B that cost is eight you go from s to b directly and that cost is three so basically all these are partial these are paths so what this algorithm is doing is simply keeping track of all parts that it has found and extending the cheapest one and that's why every node has a cost associated with which is the cost of reaching that node so for example this node d at the bottom how does it get the value 11 it gets a value 11 because from s you have gone to B from B you have gone to a so I am just following the path in the search tree from a you have again gone to B and from B you have come to D so this is the path s b a b d of course B you have visited twice but that is why this is not going to play a role in the final solution essentially what is the cheapest cost of going to D is from s to b to D that is three plus four seven so from s to b to D this would have been the cheapest way of reaching the so obviously as we will see further ah we will next we will look at the ashes algorithm and the extra algorithm ah once it has found that the path to D is by a 10 it will just simply prune the other possible path in fact it does not even represent possible paths so that is what we are going to do next I think focused here we are the diastas algorithms are very well known algorithm the only difference between what we are trying to do and what disaster algorithm is designed for is that this has algorithm says that you are given a source node and you have to find the shortest paths to all other nodes essentially we are not interested in all other nodes we are interested in a particular goal node so in this particular example it turns out that you end up visiting the entire State space anyway essentially so how does the algorithm work so you might have studied this in some course basically it begins by assigning an infinite cost estimate to all the nodes except for the start node it assigns the color white to all the nodes initially so this is how the algorithm has been described by di straw ah it picks the cheapest white node and colors it black essentially saying that ok I found a path to that particular node essentially and then it relaxes it impacts all the neighbors of the New Black node and it checks if a cheaper path has been found to those neighbors and if yes then we update the path so we will only keep one path from start node to any other node essentially unlike the algorithm that we saw in which there were many different parts ah that were maintained but that obviously was wasteful work so if you have found the cheaper parts then update the cost of that node and Mark as to which is the parent node essentially so the Esters algorithm keeps track of this parent pointers so let us look at the algorithm in this same problem that we are looking at ah as you can see that here all nodes have been assigned an infinite cost or it could be some largest possible number that you can think of or whatever the case may be and we start with the source or S and that is the only node that you have so you pick that node and color it black ok so when I say black I have not drawn black so Orange is the New Black as you can see and then what has happened is that you have found paths to A to B n two c so these are represented by the back arrows and the estimates of those nodes have come down from Infinity to the shortest path that can be found to them which is a single hop so we follow the same convention saying that we will Mark the cheapest node or cheapest white node actually because the Isla call them black nodes and white nodes cheapest white node and we are going to Define that all relax that essentially so what is that the node is B because it is a cost of three the other two have cos 6 and 8. so we Define that and then we find that we have found a better path to a essentially so what diasterized algorithm does is that initially there was an arrow like this it removes that arrow and adds the arrow to the parent B from where the cheapest path through a comes so the cheapest part to a is by a b so the back arrows point to that now at this point a is the best node it has a cost of five the other nodes ah on open remember B is already on closed and S is already on close or they have been colored black in the Islas terminology so out of five seven and eight a is the best so we Define that we color it black and the value for path to D has not changed because the best part today is still from B and that is maintained in this back pointer so that has not changed so so coloring black a black did not change anything then of the open nodes the best one is between C and D the best one is d so we Define that and we have found one path to the goal which is can be followed by the back pointers that you came to G from D you came to D from B and you came to B from s but of course we have not yet we are not going to pick goal at this moment because the algorithm says pick the cheapest white node and color it black the GPS white known that this stage is C with a cost of eight and we color it black and as far as the goal is concerned nothing change because the paths to the goal wire C which is as you can see eight plus eight is sixteen whereas the path that we have found to go is thirteen which is by a b and d so that does not change basically if if for example this Edge cost between C and G instead of eight if it was let us say four ah then ah this would become the better path if but only if this was four that's not the case so in this example ah nothing changes e is the next cheapest note so we color it black and finally we pick up G and we have found the cheapest path to the node G essentially so the same path that of course 13 which branch and bound found disaster algorithm algorithm also finds it is a little bit more elegant in the sense that you do not have this hugely growing search space search tree and you are only modifying the graph in some sense and this is what we will do ah or this is what we will ah inherit from DIYs algorithm when we look at this algorithm a star which is going to be of interest to us now branch and wine has no sense of direction essentially so it only does on the exercise algorithm which is the same thing ah it extends the cheapest path at any point essentially so starting with the start node you can see that runs and bound will first explore all the neighborhood ah or at least most of the neighborhood before moving on to this node or this node or this node and so on essentially that's because it like breadth first search in some sense it tends to stay as close to start as possible and branch and bound will first explore that that part essentially so just imagine its like if you are in IIT Madras and you want to go and visit the ah let us say the show Temple in mahabalipuram and you want to use branch and bound so branch and bound will first explore the entire Chennai City before heading off South essentially so it has no sense of direction but it does guarantee you the shortest path because it always stays as close to the source as possible like breadth first search best first on the other hand only looks ahead essentially so if there are two nodes on open shown in the blue here you can see that it looks like the shortest path is from here but somehow best first has come to find this node and that appears to be closest closest to the goal so best first will not necessarily find the shortest path essentially that we have already discussed essentially what we need is an algorithm which will behave like branch and bound but also have a sense of direction like best first search that is going to be our next algorithm and this is the algorithm a star which combines the best features of both so we will do that next