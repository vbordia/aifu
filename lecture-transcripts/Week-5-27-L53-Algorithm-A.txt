foreign shortest paths or finding Optimal Solutions and we looked at branch and bound which was a blind or uninformed algorithm in the sense that while it did guarantee that you will find the shortest path or the optimal solution it had no sense of direction that it could go off in any Direction this brings us to probably the most well known algorithm that you are going to study in this course which is algorithm a star which combines the properties of the algorithms that we have seen earlier a star was given by heart and Nelson and Raphael in 1968 so it is quite an old algorithm they were at that time Stanford Research Institute which is now called SLI International it can be seen as an extension of diastrasal 1959 algorithm for shortest path all pair shortest path as it is called a star achieves before better performance by using heuristics which is what we will borrow from best first search essentially and of course the exercise algorithm was not concerned with reaching one particular goal state it said that find shortest paths to all the nodes and therefore it did not need a sense of direction but our task is to say that you are given in a star State and you want to go to a gold state and therefore making use of heuristics makes sense so this is just a recap of what we did with branch and bone that eventually it found this path to the gold node which came from here actually maintains distinct paths as separate nodes in the search space ends up expanding the same state space node again and again essentially and we had said that we could do a little bit of optimization there but then the Stars algorithm gives us a much better idea which says that you only maintain one graph and for all the nodes that you keep exploring you keep track of what is the best parent you came from so the parent pointers will tell you what is the shortest path but it had no sense of direction essentially it keeps only one copy of each node and adjusts the parent pointer when it finds a better path through the node so we saw that initially the better when we started by expanding a or inspecting a as a child of s we thought that s is that is the best path to S and if you remember that was the cost six but later we found this cheaper path which was via B which is of cost five this is the feature we will adapt into a star both branch and bound and digest algorithm have no sense of direction and we had said that once you start this then this is the the closest nodes is what they will keep extending essentially so in this but something like this order it will generate the nodes and eventually of course it will exhaust all those nodes and find the path to the gold node essentially to give you a sense of direction we borrow what we did in best first search which is to use a heuristic function which estimates the distance to the goal basically so we are going to combine these three aspects the aspects from this different algorithm according to base first search candidates which appear to be closest to the gold okay remember that this is only an estimate and then we choose the node with the lowest H value in the hope of finding the solution sooner but when we combine it with the features of plants and bound we will see that we will also find an optimal solution and we will see the conditions under which that happens so as we said earlier ah it only looks ahead and it is possible that best first would have found this path to the goal but which is not necessarily the shortest path that's because it only looks ahead it does not look behind a star as we have been saying combines the best features of all these algorithms so what is the cost that you will associate with nodes so look at this diagram so we have the start node on the left we have the gold node on the right and we have some open list in which we have candidate nodes so in this example there are three nodes for all nodes we will compute a value called f of n that is standard terminology in a star which will have two components G of N and H of n what are these G of n is the actual cost of the solution found from the start node to the node n so supposing this is the node n that we are talking about G of n says that this is the actual cost found to the node n this is what branch and bound or digest algorithm would have essentially h of n is our good old heuristic function which says that you estimate the distance to the goal and essentially F of n is the sum of G of n plus h of N and that is what we use in our search algorithm so this is algorithm a star each candidate is tagged with an estimated cost of the complete solution F of n is the cost of the complete solution passing through node n where as we have said g n is a known cost of the path found from start to the node n as used in the branch and bound algorithm or die stress algorithm and H of n is the estimated cost from the node end to the gold node as used by the best first search algorithm the surface as SL excludes the graph looks as follows then all these are the nodes which has already explored and you can see that every node has a back pointer like in the distress the extra valuesome to say which is the best parent it came from so for example for them this is the best parent and so on and so forth for every so you can find follow the trace the path back to the source from any node essentially and of course we have the heuristic values of the nodes which are on open that we will include in the evaluation which is f of n f of n remember is H of n plus G of n the algorithm is going to follow the same strategy that we have been talking about refine the best looking partial path till the best solution is fully refined maintain a priority queue of nodes sorted on their F values where f is equal to g plus h for every node pick the lowest F value node and check whether it is the goal that's our good old strategy that we have been following if it is not the goal generate its neighbors and compute their F values and insert the new nodes into the priority queue as you will see we will keep only one copy of every new node that is generated so for existing nodes check whether you have found a better path like we did in the lifestraus algorithm so that is going to be the strategy of a star this is a full algorithm ah you could see this version is from who's helping with this course and he has followed the style of diastereom by saying that initially every node has a value of plus infinity which is what diastereom did so initially whenever you generate a node you say that it has a g value which is infinity and then of course you revise it essentially like the dice algorithm it maintains ah a priority queue as we had said and initial when we initialize this so we can see that the parent of the start node is null we compute the G value of start node is zero because you know the COS G value is the cost from start node to that node and that node itself is s then we compute the F value which is G of S Plus h of s remember that h of s is a function that is a static function which somehow estimates the distance of any node to the goal node and we put it into this open list essentially we initialize the closed list to m t and while open is not empty we do the same so this is very similar to the algorithms that we had studied earlier ah we add into closed after we have inspected it and we apply the gold test function if we if we have it then we follow then we call a function called reconstruct path so this reconstruct path function in our case will not use the closed list we are not no longer keeping node pairs that we were keeping in best first and that first and so on instead we are keeping parent pointers and for every node we will have a parent pointer which will be used for reconstructing the path so if it is not a gold load then you look at all the neighbors m ah which are given to us by the move gen function and we test whether the parent node the value of the GE value of the parent node plus the cost of that edge from n to M if it is less than the cost of G of M that is existing what is g of M we have already said that for all new nodes G of M will be initialized to Infinity so obviously this will be less than that but it could also be less if you had already found one path to a node and then found a better path as we did for example if you remember the node a when we look at the tiny search example with diastere so if we have found a better path to the this thing then we update everything we say that parent of M is n and then we say that the cost of that node m is cost of n Plus cost of K of M this is the cost that we found was a better path remember in this part here and of course we add that to h of n to give us F of M essentially now there are three different cases that is the algorithm encounters and most algorithms most textbooks they mentioned these three cases explicitly but here ah they have been included in one long things case one is when this m remember this m is each neighbor that we are talking about if m is a name so this this is a neighbor M we are talking about if m is a new node ah as mentioned here then simply we add M to open so we have added M to open we know that what its G value we know what its H value because H value you know for any node and we know who is a parent of M which is n that is what we have done at this place here so this is case one when we are generating a new node case two is when the node was already on open and what happened was that it had some original G value but we found a better G value and we have updated the better G value in this lines here and then we can simply continue this is what for example diaster algorithm does essentially in the case of a star there is a third case which was not there in diysulars algorithm which is that the new neighbor is already on closed essentially so if you think about a digital algorithm you will see that when diastas algorithm takes a white node and colors it black which is a white node it picks it picks the cheapest white node and colors it black at that point it has already found the best path to that black node that it has created so it will never find A New Path to a node which is already black or in our case which is already on close essentially a star that does not happen and we will see this as we go along and the reason for that is because a star includes this estimate h of n which may or may not be accurate essentially so it is possible that the nodes that you found path earlier you found found and passed earlier you even picked it from open then you added it to close you may still find a better path to that that node and we will see examples that is a third case which is there in a star which was not there in foreign and that means that the node is enclosed and if m is in close then we will need to propagate this improved cost to Children of M is actually and we will see a small example which will illustrate this and why why is that because we have found a better path to a node which is on closed and the fact that it is on closed means it had been expanded its neighbors had been added and we had found some path to its neighbors now we have found a better pass to M so that Improvement must be propagated to its neighbors how does that happen so there is this algorithm called propagate Improvement so given this node M which is enclosed and which we have found a better path to then for each neighbor X of M which is given to us by the move gen function if we have found that this new path which is G of M plus the cost of going from M to X which is the H cost is less than what we had stored for that node X essentially then we revise that so we say OK we have found a new path so parent of X must be m g of X must be G of M plus K of m x f of x must be G of X plus h of x and if x also happen to be enclosed then we recursively call the propagate Improvement algorithm which is because this is something peculiar to a star algorithm in the sense that the nodes that you have already inspected first you can find better cos later and a little bit later we will see that under certain conditions ah we can try to be closer to the Esters algorithm where you have already found the better path but that requires an additional condition that we will need to look at so we need to now study as to under what conditions does a star guarantee you in optimal path and the reason that we need to study those condition is because we are using a heuristic function in our computation which is an estimate of the actual distance unlike the asteroid algorithm in which we knew exactly what was the cost of reaching to a node ok so we have discussed this if we found a better path to x x is on closed ah or it may be on open but if it is not close then you recursively propagate the Improvement to Children of x so here is a small example which we illustrate this so we have this node n that a star is about to pick ah that means that see what we have the numbers that we have in every node are the G values so as you can see on the top what we have written are the G value we have not mentioned the H values ah but let us assume that whatever the H values are this node n is the next one to be picked from open essentially so the nodes on closed are the ones with solid lines and the nodes which dotted edges are the nodes which are on open and Acer always picks the cheapest node from open so let us say that that is n essentially it has a g value of 55 essentially it will generate its neighbors so these are the neighbors that it has and all the three cases that we talked about will come into play here one is this first case means when it is a new node which means it was not on open this is this case number one this is case number two where the node is on open and nodes like this would be case number three which means they were already on closed but we have found we may have found a better path to that essentially so what happens after n expands this node 55 all its children are added to the graph essentially so as I said case one is the new node ah case two is node already on open and the other nodes are which where they are already on closed essentially what is the path to n that it has found the path to n it is found is this one because that is where the back pointers show that the parent of n is this node with G value 50 then it is this one and then but to other nodes it may have found better parts so after this node n is expanded what happens ah you can see that for this new node we have up upgraded or assign the G value to 62 where did this 6 2 come from that is 55 plus 7 7 is The Edge cost and we add it to open now for this node which has a cost of 62 which is now a cost of 62. initially it had a cost of 70 as you can see here and this cost came from this node with with at a cost of 50. now we have found a better path so we have revise the parent pointer and said that this cost is 60. the same is the situation for nodes which are already enclosed so this node for example its parent was the node on the top which is 30. but now we have found a node with 55 plus 2 which is 57. before it before this it was 60 how did this 60 come we have not mentioned the G values here or the H cos here but this H could have been cos 30 so 30 plus 30 would have been sixty now what's happened is its 55 plus 2 which is fifty Seven so we have found a better path so we will move the parent pointer for that and then we propagate so this is the propagation part this one and this one from from seventy we came down to 57 or sorry from 60 we came down to 57 so that Improvement of three should be propagated and then you find that when you are doing this propagation you find a better path to all these nodes which were already on closed and we have also revised their parent pointers so this hopefully explains what happens when a new node is expanded by a star it may find better paths to some nodes and it may add new nodes which is the original case first case next we will look at how a star searches the implicit graph Guided by the F values and generating the nodes which is thus on the Fly and we will also compare it with best first search so we will do that next