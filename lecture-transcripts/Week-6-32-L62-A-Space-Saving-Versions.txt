foreign there has been an interest in finding variations of a stuff where the space requirement is lower but at the expense of time obviously because you can't have a free lunch but we want to maintain the admissibility of the algorithm that we are still interested in the optimal path we are willing to spend more time and we want to use less space and the implication of that is that we will be able to solve problems which are larger and larger in terms of the size of the state space system so let us look at some linear versions of a star we had already seen that when a function is more informed when is it more informed when H2 of n is always greater than H1 of n for all n we had studied this that a more informed function explores lesser of this space essentially but of course there is a limit to how much more you can be informed and one way we try to do this first by using w a stuff so that the value of H increases but at some point it crosses the H star value and therefore it is no longer admissible but the more informed version thus explored less of the space essentially so we have seen this already earlier in weighted a star we can shrink this further but we lose on addressability as we solve the example where it found a longer path essentially the search Frontiers in general turn out to be exponential in nature and we saw that when we were talking about best first search the Search Frontier could look like this because it's an odd shape because the heuristic function May take you to different parts of the search space but in general it turns out to be exponential so we are trying to reduce on that so we also saw when we compared looked at this example that that best first has a smaller Subspace than a star then of course w a star was somewhere in between best first as lesser space than ah a star w a star has lesser space than a star also and all of them have more space requirements as compared to branch and bound so we saw those four examples so now let's look at algorithms which will save on space so the space complexity algorithm is often exponential because we can very rarely find perfect heuristic functions we could find the perfect realistic function any of these algorithms would work in fact hill climbing will also work eventually but we do not unfortunately so we prompted us to look at search algorithms like hill climbing beam search taboo search simulator running when we were looking at best first search essentially because we were trying to save on Space essentially remember hill climbing was a constant space algorithm we also looked at population based methods like genetic algorithms and end Colonial formation which had in some sense constant space Also but they did not guarantee optimality are there space saving versions of a star where they are admissible essentially perhaps at the expense of time complexity and we take a Q from the algorithm that we had looked at towards the beginning of the course which is depth first iterative deepening or DF ID and if you remember what dfid was that it was a series of that first searches and Its Behavior was like breadth first search because it went down into the space level by level which means it always found the optimal path so we will take look at a variation of that to begin with so this is a recap of what depth bounded depth first search did that you had a bound and you searched inside that bound but using only that first search to do DFS with a depth bound D it requires linear space because it is DFS remember that the open of DFS grows only linearly but it was not complete and does not guarantee the shortest path I mean it's not even complete essentially and then we extended that to dfid that first retrieved deepening and we said that we start with the depth bound of one and then increase the depth Bound By One in Every cycle so that you first look at parts of length one then you look at parts of length two and so on and so forth so we want to adapt this algorithm when we have H cost involved and that algorithm we will see shortly so we had already seen all this earlier so it is a series of Dash bounded searches that require linear space and when the path is found it it is the shortest path so this is shortest in terms of the number of ah hops in the particularly now we have to also accumulate Edge cos then we had seen ah this was done earlier as well that dfid the extra work is not significant it's only B over B minus 1 times the breadth first search time requirement the time requirement was only marginally higher but the space requirements the space savings were quite significant which is why dfid is a fairly popular algorithm the variation of dfid when we look at Edge cos is called iterative deepening Ester it is very similar to dfid it was given to us by Richard Koff who did a lot of work in search algorithms as part of his PhD it is designed to save on Space by doing a series of therefore searches exactly like dfid unlike dfid which uses depth as the parameter idsr uses F values to determine how far it should look ahead essentially so IDs are initially sets a bound of f of s remember that F of s is the estimated uh distance to the goal which is equal to H of s as mentioned in the next line and that is that is the smallest cost path that you can imagine because it that is a optimal or it underestimates the optimal cause remember that h of s is less than equal to X star of s so you can't you can't get a optimal path of length less than h of s so we start with h of s at best if the heuristic function is good we may find the PATH otherwise we will increase the H value a little bit essentially what it does is that it extends the bound to the next unexplored F values actually instead of adding plus one in like in DF ID we extend it to the next this thing so the F bound is increased to the F value of the cheapest unexpanded node on open essentially and otherwise it does a series of the depth first searches exactly like dfid and when it finds a solution it would find the shortest path solution essentially so this is how huh ideas are looks when you look at the search space at any given cutoff which is shown by this oval Gray colored oval it explores all the nodes that are in green in the depth First fashion okay so basically it goes down some path then backtracks and goes down another path then backtracks and goes down another path and so on so maybe we will see an implementation of this and see how it repeatedly does a series of the first searches of increasing depth essentially so the boundary shown in oval is here and in the next cycle what this will do is that let us say this is a node closest to the boundary it will extend the boundary to that much and then do another cycle of that first searches essentially another cycle of depth first search and it in this fashion iteratively expenses this thing the problem with Ida stuff is that even when the stage space grows quadratically which is what we will see in city like map it tends to grow quadratically the further you go away the number of nodes are like square of the distance foreign can spend a lot of time exploring these different parts in a closed list if a clause is not maintained and we had seen in the ID dfid that we do not want to maintain this closed list because it prevented us from finding the shortest path so we had seen this earlier and the problem with then is then it it it does a lot of repeated searches so it it looks at every node and looks at every different ways that you can reach that node that the number of Cycles become too large especially if the nodes all have different values so in every cycle it will extend the F value to the next node and then search the entire space up to that point essentially one option would be to extend the search Space by a certain constant amount each time with a some compromise on the cost essentially so we can see that by a diagram here that instead of X extending so the inner oval is the search that you have just finished and in the next cycle you are extending the boundary by an amount which is Delta essentially you can decide as to what is Delta that is the will determine the loss of optimality so instead of just extending it to the closest F value you are extending it by a constant amount Delta so that the outer oval is the new boundary and you can see that if you were doing that first search here it's possible that it will find this path because you know if let us say it is going on this direction and searching like this and find this path and it will miss out on a load which is actually closer to or which has a ah foreign cost or lower cost essentially but so we are paying a price essentially in terms of accuracy uh but we are limiting this extra cost by deciding what should be Delta essentially so that was our algorithm that Richard Koff has given us he gave us another algorithm called recursive best first search so IDs star has linear space requirements as we have seen being that first in nature but it has no sense of direction because it is depth first in nature recursive best first search is an algorithm given by cop in 1991 and it's a linear space that is the important part best first it is also has a sense of direction so it is a bit like hill climbing with backtracking it explodes new nodes in the best first order and expands fewer nodes than iterative deepening which is idea stuff so it's like hill climbing with backtracking and we'll see throughout example backtracking if no child is best on openness actually so remember that the open for hill climbing is only the successors of the current node it's a local search algorithm so this is also going to be like a local search algorithm so it's going to be like hill climbing but if none of the best none of the successes is a better node it can roll back essentially except that instead of backtracking as depth first search does RBF press rolls back search to a load so it kind of relates those nodes that it had visited but while doing so it backs up the lowest value for each node from its children to update the value of the parent so this is I think best illustrated by an example and this is a example that we are looking at we started so that's a start note here and we are searching this in a hill climbing like fashion except that we are maintaining a linear set of open essentially so which is what that first search will have done the difference between that first search and rvfs is that that first search is blind it always lets say number of figures always goes to the left side of the tree whereas this is like informed that it opens no nodes in the best First fashion so let's say it started with this cost node with f value of 50 and then it went to this node value of 59 which was the best successor so remember that it it is the best successor it is not necessarily better and we had kind of observed that F values increase as we go towards the goal so that is to be expected of f values so but it is still going to the best successor and what rbfs does is it maintains at all times the second best node that it could have visited so when it finds this node of 55 it marks 59 as the second base load essentially then it looks at the children of 55 and it finds that there is a node 56 which is better than the second best as long as you are better than the second base it goes down this path so from 56 and it goes to another 56 then 57 then 58 at some point it discovers that the three nodes that are the children of this noodle 58 all have a value Which is higher than the second best so what all BFS does is that okay I am going to shift my attention to that node with a value 59 and I will just delete all these nodes so which I have searched in the process you will roll back the values going from bottom to the top and this is what happens so so you roll back this value is become 61 then for the instead of 57 this becomes 60 because that is the best successor now so this went from ah 58 to 61 so it is not the best anymore this is the best and therefore that is what is rolled back here essentially so this value of 60 goes back all the way up and then when so which is about to re restart this value that was 55 earlier is now upgraded to 60 because it's a better estimate and now search goes off in this new Direction yeah so uh so if you see the Shaded arrows on the left hand side from every node which is the pink node is getting the best value from its children and we do this from bottom to top so this in this node with value 58 which is the lowest pink node its value becomes 61 because we know that if you are going to go through that node one of its children which is 63 61 62 the best is 61. now this is 61 its its sibling is 60 and another sibling is 64. 60 is the best so 60 gets updated to its parent which had original value of 57 and now it's become 60. now the siblings are 67 and 60 again so again the value of its parent which was 56 now gets updated to 60 essentially it could have come from this side it could have come from the other side it does not matter because it has now two children whose value is 60. then again its parent which of 56 becomes 60 and then its parent which was 55 and the other two children are 61 each so the best value is 60 so which is why this node is this thing so it has rolled back the search and then it's gone off in a New Direction the problem with rbfs was also similar to what was in Ida star is that especially in search spaces where the different parts are similar in nature it will keep repeating it will switch from one to the other go back to the other so for example if this 59 were to increase to let's say 62 somewhere down this path essentially so let's say 63 it becomes here the best node then again it will roll back and again it will start from here so this kind of thrashing Behavior was observed when we implement this rbfs which is why eventually we looked at newer algorithms so this is a small exercise problem some space being explored by our BFS we have seen that it has gone from 500 to 550 to 570 to 590 so just what show us what will happen after you do two more expansions of this thing and then which is the next node that our BFS will pick up essentially so I will leave this as a small exercise to be done to make sure that you have understood the word this thing so this this shows in some sense a larger part of the graph then that was actually explode the graph that has been actually explored so far is in solid circles the dash circles are things which have not yet been explored essentially so which is the next node it will explode which is the next node after that it will explode and what will this graph look like after those two expansions that's what I would like you to do there will be some rollback involved as you can imagine okay so next we will look at something called the monotone condition or the consistency condition when a star will find the optimal paths to every node it picks from open which is what diaster's algorithm do does essentially so we will look at that next and see its implications as well