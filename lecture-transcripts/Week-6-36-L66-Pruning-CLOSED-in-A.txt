foreign and we had made a general observation that for heuristic functions which are not perfect we end up requiring exponential amount of space and time as well of course you can't cut down on both because the heristic function demands that but we built some motivation to see that can we have variations of a star where we are pruning the closed list and then we will also look at variations of a star where we can prune the open list essentially so let's do a very quick recap of what we did we said that for the sequencer alignment problem open tends to grow linearly and close tends to grow quadratically so what are the algorithms that we can think of where we can first prune closed then we will move to algorithms where we can phone open and then a glimpse of an algorithm where we can prune both as much as possible but remember that that this in general this list tend to grow very rapidly so we saw this motivation earlier say the sequence alignment problem one thing that we had done for reducing space was weighted a star so this is just a recap that we looked at four algorithms branch and bound a star w a star and best first in this particular order exploring lesser and lesser amount of the space branch and bound explores most of the space then a star then the blue a star n then we saw best first was the least amount that it required we also looked at the monotone condition in which it underestimates the cost of every Edge in the graph essentially and we had mentioned that that is the nature of the graphs that we had constructed in our examples and for a star the interesting consequence is that every time it picks a node it has found an optimal path to that node and we can design algorithms which never never lead to look back but only look forward essentially so open just keeps growing but we do not worry about what's left behind what was the role of closed in search we needed to maintain the closed list of nodes for two reasons so here's just a small observation that we are calling it a list but in practice if you want to implement close the best thing would be to use a hash table because that has a easiest fastest access in terms of time so one was to avoid getting into Infinite Loops that we always said that we removed those children which are we do not generate them again in general that was a motivation for using closed when we looking look the best first search for example yeah we will look at other ways for search to be stopped from leaking back is the terminology that we sometimes use and the second reason why we need it closed at least in the best first search algorithm was to reconstruct the path once a gold node is picked up by the algorithm Association now in 2000 Richard cough is still working on ah search as you can see with his student Zhang they gave us this idea of Frontier search so the idea of Frontier search is to only maintain nodes which are on open and the ability to throw away nodes which are all closed so in general if you were searching and the start node was on the bottom left and the cyan nodes of the green nodes are the ones on open and let us say we are about to pick that node a from open list and then we will add a to closed essentially the closed nodes are the yellow nodes thank you so let us say a is about to be expanded and its active neighbors are b c d and e essentially out of which C and D are new nodes which have been generated for the first time or which will be generated for the first day and B and E are already on open essentially G and F are on closed and we have already said that we are not interested in going back to the closed node so we will not visit them again essentially we will not generate them again and they are barred as we will see in the next slide how so this this symbol here says that they are bad and we will see how those how this barring of nodes happens so when we pick a from open and put it in closed as shown here what we do is that at that point for the nodes b c d and e which were either new nodes or which were on open we will say that we will add a in some sense to the taboo list for those nodes in the sense that when you expand B or C or D or E do not generate a as a successor of that essentially so they are barred from generating easily and what coffin Zhang did was that they associated with every node on open a list of nodes which were barred essentially foreign so after a let us say it is about to pick e then the situation would look like this e would be added to closed and when e is added to close its neighbors its active Neighbors which are d h n i they are barred from generated e so if you look at this node D for example it is barred from generating a it is also bar from generating ease actually so when it generates its own children it may have c as a child it may have H as a child and it may have two new nodes as a child those it can generate but nodes which have gone into close it will not be generated uh again isn't she so this is the kind of ah Search Frontier that they maintain in which they can delete all these nodes which are shown in the Shaded gray gray colors this thing I have drawn them here to show that they were enclosed but in orphan Zhang's implementation they will not be stored in closed essentially so they called it Frontier search that you are only keeping nodes which are on open along with the bad nodes for each each node in open that these are the nodes that you cannot generate and searching with these moves they can search only in the forward Direction without in some sense The Source leaking back into the closed note that is one of the purposes that we had used closed for so this is how search would look when ah it would find the gold node there would be a whole host of nodes on open each with its taboo list but let us pick the node open but since it has thrown away the nodes in closed how will it reconstruct the path that it has found essentially what they suggested was that don't delete the entire close list but maintain a set of relay nodes as you can see here what are these relay nodes for after a certain point when search has progressed Beyond a certain distance which is roughly halfway the estimated distance to the board which is characterized by the fact where G of n is roughly equal to H of n remember that h of n is an estimate for every time a node which is roughly has this property so this pink nodes are the ones with this property where G of n is roughly h of n we keep them as relay nodes so we maintain a relay layer of nodes and any subsequent nodes will point to them as an ancestor in the node so when the search terminates every node on open will have some relay nodes essentially so some other nodes for example may not be so far so for this node for example G of n maybe you must listen or h of n so it doesn't need to have a relay node but nodes which are going closer to the goal will have relay nodes so for every node we know that there is a particular relay node through which the path to that node comes essentially so at the point that we terminate we know that we have the gold node but we also know that there is a relay node through which we came to this code node so this is how it looks at that point when search picks the goal node it is a pointer to a relay node ah who we have called as a relay known here and it knows that much about the path that if you that we gain through the relay node now what Golf and zang's algorithm says is that make two recursive calls one you solve the problem from start node to relay node and the other you solve from the relay node to the gold load essentially so two two extra costs we already found the gold node but we don't have the path to the gold load to reconstruct the path to the gold node we make this recursive calls to recursively construct the paths and which is why they have called it as a divide and conquer Frontier search algorithm you are doing this extra work essentially how much is the extra work that you are doing if let's say the time complexity of finding the goal node is some function T of depth d then the time complexity of divide and conquer Frontier search would be in the first cycle when you found the paths to the goal then in the next recursive cycle when you found a path from start to the relay node and then from real node to the gold node now remember that then this will in turn have its own relay nodes so they will have four recursive calls one from star to the intermediate relay then from this to this then from this to this and this to this remember that each of these is just a new problem which has been solved recursively from start to relay and from relay to goal so it will have its own relay internal load and which is why we have this value of 4 here so there are four segments that those two segments got broken into and it turns out that if T is an exponential function that the amount of time you need is exponential then the complexity of divide and conquer Frontier so search is that exponential function t multiplied by D so it if you solve this recurrence equation this is what you get essentially so what is happening here is that we are not keeping the entire closed list we are only keeping one relay layer in between but we are doing an extra amount of work which is described by these recursive calls that you are making and in general we expect that the complexity will get multiplied by the depth D which is the depth of the distance to the cool so your your trading of time versus Space you are willing to spend this extra time but because you are saving on Space which means that you can work with problems with larger depths essentially where the state space is much larger A variation of this sir one down yeah so we are creating a list of bad uh nodes right let's say with every node we Associated an identifier this is the let's say N1 this is N2 and so that also will take some space so it will be you can imagine the open list has a certain space requirement and every node will have a constant number of Neighbors at most which are available is so it will get multiplied by a constant Factor but it will linearly increase right since they are just the previous of open uh well linearly in the sense that supposing the space requirement for open is some n nodes so this will become like n into two or n into three or something like that where how many neighbors you are storing which are about this so yes it will get inflated by a constant amount not linear amount in the sense that it doesn't develop on depth so that was divide and conquer Frontier search A variation of this was given to us by another pair of researchers so Hanson and his student Zao ah this was in 2003 and it's a similar similar this thing except for the fact that they want to exploit the fact that memory is getting cheaper so you don't necessarily want to do this extra work of recursive calls in a in a fixed fashion essentially so one need not make this recursive calls if you can solve the problem by using just a star why not just solve it for you so it's only when the problem size becomes large that you may want to do this divide and conquer kind of an approach and what this is what the Owen Hansen's algorithm does they called it smart memory Graph Search and it is aware of in some sense how much memory is available to it and then decides whether to break up the problem into smaller problems or not so this SMGs or smart memory Graph Search keeps track of the available memory and only when it senses that the memory is running out it creates a relay layer essentially so every time it feels that ok I am allowed to have let's say 10 million nodes or 10 100 000 nodes and I am approaching that much size then it says create a relay layer in the process it may create more than one relay layers essentially so which is interesting that is why they call it smart memory Graph Search so they may create many relay layers or even no real layers depends on the size of the problem that you have solved something smart memory Graph Search has given by coffin Zhang it always said that okay take a problem break it up into half roughly speaking then take that problem into half and make that problem into half and so on they did not say that at some point stop breaking the problem into half and just solved using a star this is what John Hansen have said that if you can solve it in the available memory then just go ahead and solve it no need to break it down further in that sense the algorithm is smart what it does is that at every at all points ah it identifies a layer of boundary nodes as the search is progressing it classifies some nodes as boundary nodes essentially and it is these boundary nodes which will be potentially converted into relay nodes actually so a figure should make that clear I hope and this boundary node will also search the additional purpose of stopping the search from leaking back orphanzo added some information with every node on open zawan Hansen are creating an explicit boundary node essentially so this is how the cell space for smart memory Graph Search looks the nodes in yellow and gray are really the nodes in closed essentially the nodes in green are the frontier nodes on openness history now a closed node is on the boundary shown in yellow here if it has at least one child on open so you can see that for all these nodes which have been colored yellow they have at least one neighbor which is on open recently so in for those nodes this will serve as a boundary that don't go back in that direction nodes which have no children in boundary are called as kernel so a closed node is on the Kernel if all its neighbors are closed essentially so all the nodes that you can see on Gray all their neighbors are either gray or yellow and therefore they are the kernel nodes these kernel nodes are potentially the ones which can be deleted essentially so when the need arises so the boundary nodes in the closed lists are enough to prevent the search from leaking back essentially so if you just maintain an open layer and a boundary layer you know that the search will never go back to this thing essentially so what SMGs does is that it converts the boundary nodes into relay nodes and it deletes all the kernel nodes so all those gray nodes have been deleted and all those yellow nodes which are on the boundary now they are converted into a relay layer and then search progresses from there again the same principles will apply so for example you may have one boundary layer constructed at some point and at a later point another boundary layer so here we have two boundary layers as you can see here ah and so there are two relay nodes there but from the last relay note to the goal node we do not break it up to puzzle because the space is enough to just use a star so there it is the unpluned closed nodes which is towards what we sometimes call says the dense path dense path is the one that we are interested in go from start to node N1 go to node N2 and so on what the relay nodes do is they they call a it's called as parse path essentially so you go from start to the first relay then you go from the first related to the second layer relay and after that the dense part starts so for converting the sparse path into the dense path we will have to make that many recursive calls so in this figure that we are seeing we will still have two recursive calls but it could have been more than two also it could have been less than two also so on termination SMGs has a sparse solution paths to the goal and it makes an appropriate number of recursive calls