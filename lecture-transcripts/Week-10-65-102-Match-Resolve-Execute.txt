foreign looking at rule-based production systems in Ops 5 and we saw that the typical forward chaining influence engine works in a cycle of match resolve and execute let us look at this in a little bit more detail so a rule as shown on the right hand side is a set of patterns which must each pattern must match some element in the working memory which is called the working memory element and then of course the right hand side will be action so the match part is not really concerned with what the actions are it is concerned with that all the patterns in the rule must match some working memory element so a rule has a match in the working memory if each positive pattern in the rule has a matching working memory element we will again revise as to what this means and unsigned patterns are positive by default so we do not have to write Plus in front of a pattern ah unsigned patterns are positive by default but if there is a negative pattern which is indicated by a negative sign before the pattern then there must be no working memory element in the working memory that matches that pattern essentially so no element of this kind so if you remember we said some student has got let us say Mark 79 and no one has got marks more than 79 that was that there must be no record with let us say marks 80 or 81 or things like that so a pattern in a rule matches the working memory element which is in the working memory if the class name of the pattern is the class name of the working memory that is the first thing that you have to do if you are talking about matching rank then the class name in both the pattern and the working memory must feel like each attribute condition in the pattern matches the attribute value in the working memory the working memory element does not have variables it has only constants it has only propositional statements that this is a value or whatever the case may be it is the pattern which can have conditions it should be less than something or greater than something or not equal to something but all that is in the pattern it must match the attribute value in the working memory element working memory elements have only values attributes not mentioned in the pattern but present in the working memory elements are ignored so patterns do not have to specify the complete working management you might simply say is there a student with marks more than 79 and that itself is a pattern essentially if you do not have to mention the date or age or the name or the discipline or whatever essentially so patterns are subsets of what the working memory contains so attribute matching conditions in the left hand side we have already had a glimpse at this let's just do it again if the attribute in the rule has a value which is a constant this is we are talking about conditions in the left hand side of a rule or conditions in the pattern of a rule if the pattern says name must be Suresh then the working memory will only match that working memory element in which the name is sewage so if the value is a constant then it must be an identical constant in the working memory element so this attribute is in the rule and the matching value is in the working memory element if the value of an attribute is a variable which as we have already said ah is written by putting them in angular brackets then that variable can match anything essentially which is like a universal statement you know that it it will match anything that is there in the working memory element so in that sense the rules become like Universal then we said we have Boolean tests so we have already seen this a little bit same type as and not equal to so you can say for example on the right you can see that a variable n is equal to three or you can say a variable X is the same as the variable y essentially these variables X and Y could be in different patterns in the same rule essentially then we have said Boolean test on numbers less than less than equal to greater than greater than and equal to so you can say for example the variable is less than 3 or the variable X is greater than or equal to variable y then we said there are conjunctions of test that you know there are multiple tests and all of them must be matched essentially for the test to be true ah and conjunctions are written in curly brackets and then there are these Junctions of test where it says that sum of this test that have been mentioned here test one up to test n one of them at least must match and then that is a match so this tells you when a pattern matches a working memory element so now let's talk about this conflict set essentially so we have these two rules ah we have seen both these rules earlier the first rule is for sorting it's called swap sort and just to recap it says that if at index I there is a value X and if at an index J which is greater than I there is a value Y which is less than value X then change the X and Y in the two records that you are matching essentially so in the first one you make it Y and the second one you make it X so now you can see that in the first index the value will be smaller than the one in the second index so its basically swapping the other role that we saw was ranking it says that if the next rank you want to assign is r and there is some student s s is a variable ah who has got marks M where m is also a variable and there is no student who has got marks more than M then you assign that rank R to the student s and increment R by 1 for the next rank to be assigned so we saw these two rules so let us see that we have these array elements in the working memory on the right hand side so there are four arrays four elements with indexes one two three four you can see the values are seven nine three eight they are not in as you can see increasing order what will our rule do essentially we will see an example ah working memory element starting with five to nine they are to do with ranking the next rank to be assigned is two there are some students who have got some marks and of those students the one with the highest marks which is Rashmi who's got highest marks 89 but she already has a rank which has been assigned whereas our rule says that the rank must be nil that you must not have got a rank so Rashmi has already got this rank one now we want to find out who should get Rank 2 essentially and there are three candidates with as you can see in Mark 79 79 69 and none of those three have a rank been assigned so three candidates are there to be considered of course of these only two will be considered which is the ones which have the highest marks which is 79 because they have Mark 79 and there was there is nobody else who does not have a rank who's got marks more than 79 that's the pattern so you can see that the first rule will match these two elements why it says that there is a value 7 which is on index 1 and there is a value 3 which is on index three three index 3 is greater than index one but value three is less than value seven so we need to swap that seven and the three so this rule will match or this will this one instance will go into the conflict set then you see that there is another little bit matches here which says that the next slang to be assigned is to so remember that the pattern is matching the working memory element and you can see that the pattern says next so next must match next then the attribute name is rank so that must match and then there is a variable R so it can match anything so it will match this Rank 2 essentially and then the second pattern in the ranking rule says that the the class name is total marks so that that must match then there must be some student name it is a variable so it can match anything there must be some marks m which will it is a variable so it that will also match anything and the rank must be three so which is why this working memory element six does not figure into ah the match process because it already has a rank assigned to heat essentially now there is a negative condition which says that there must be no other pattern who has got marks which are greater than M and which has not been given a rank so there are two candidates which qualify as I said earlier Eva with 79 and Anil with also 79 both of them have got marks and nobody has got higher marks so if you look at the conflict set it will have these elements ok so there should have been yeah its got its matching as you can see five and seven and the rule name is ranking so this is shown up as ranking with five and seven five is the timestamp of the matching element and 7 is also the timestamp likewise this one and three that we saw earlier that is also shown off as shown in the Z here but apart from these two there will be others so for example you can ah do swaps out with two and three what is two two has got a value of nine ah stored there and three has got a value of three stored there so that is also a candidate for swapping you can also swap two with four because nine is bigger than eight so you may want to swap them so you can see that it will just Identify two things which are out of place and swap it essentially likewise ranking we said Eva is the candidate that we have shown in red here but Anil is another candidate and that also shows up in the conflict set so given this working memory element given these two rules we would have these five elements in the conflict set and the next task is which of these two execute next in this particular example maybe the order does not matter except for maybe the swapping case in swapping case it would matter essentially for example it would be easier to bring three first and then talk about the rest but we want to talk about conflict resolution in a very general fashion what is conflict resolution that you have a conflict set which has got a set of rules with matching data and you have to decide which rule to execute next essentially we have already said that swap sort basically spots two values which are out of place and then it will do something with that so we are only talking about the match part the conflict resolution is nothing to do with the execute parts so if there is a large number of working memory elements the rule May match many pairs of elements so we already saw that with those four working memory elements there were three instances of swap sort ready to fire essentially so as we have already said the set of matching rules along with data matching rules with data is called the conflict set and the inference engine selects one rule from the conflict set essentially so as a point to ponder I would like you to think about the fact that what would happen to the system if nowadays you know we have multi he threaded programs possible what if we could execute many rules in parallel how would the system work so its just something that you should think about for some problems it should work well but for other problems there may be a conflict of updating some records at the same time and so on so assuming that we that we have things in a sequential manner which is what op swipe does ah the conflict resolution is the task of choosing which rule to execute or fire and these are the strategies which can be used and these strategies will essentially determine how efficient of a program is so the key to effective problem solving is to make informed choices we have been talking about this all along starting with Brute Force search we introduce this notion of the heuristic function and so on in rule-based system by rules encapsulates such knowledge greens from expert essentially so an expert can tell you that you know this rule is applying to these conditions and so on but we are concerned now about this thing when more than one rule matches the data the influence engine has to make the choice as to which rule instance to execute this is somewhat analogous to which candidate to inspect next during search essentially and we look at various conflicts resolution strategies the first strategy is called refactoriness and it is a simple strategy it says that a rule instance May Fire only once with a set of matching working memory elements so if a rule is matching some working in the elements and our strategy somehow selected that rule to fire then you you can fire it only once you cannot fire the same rule with the same data again and again because if that was the case then this selected rule could fire indefinitely especially ah if it does not modify the working memory if the rule simply says print a message or found the largest number or something like that then you do not want to keep you know firing the same rule again and again ah and this is done by the strategical refractoryness essentially otherwise it is possible that the same rule could keep firing essentially the idea of refractoriness comes from the way neurons file in animal brains so a neuron basically we have seen this earlier that a neuron receives inputs from many um it has many inputs and then it when it crosses a certain threshold ah then it fires essentially but then it does not fire again essentially that input has been received and consumed essentially in the case of animal brains the input is received as chemicals so they are actually consumed and so on and it has to wait for a new signal to build up essentially and this is called refactoriness a neuron does not keep firing essentially I mean if it does then there is something wrong with that particular brain the Merriam-Webster Dictionary says the incensive insensitivity to further immediate stimulation I mean that develops in irritable and specially nervous tissue as a result of intense or prolonged stimulation essentially but basically the idea is that if you have consumed that data then you are done with it and it as we will see in our algorithm this happens naturally in this algorithm that we are going to study which is called the DT algorithm the detail also the name comes from Network which is like a brain Network or something so the next strategy that we can think of is lexical order this says that all the rules that have the matching instances choose the one choose the first one that the user has stated so the onus is on the user so if you are writing these rules in some file then just pick the first rule in the file essentially and if a rule has multiple instances with different data then choose the instance that matches the earlier data this is basically lexical order essentially and as I said this places the onus of this choice on the user the user becomes more like a programmer essentially so we said that the ideal of declarative programming is that the user only specifies the connections between input and output in terms of patterns but now we are saying that you must be careful as to which order you write the rule spin that's why you are becoming more like a programmer the strategy is in fact used in the programming language prologue so prologue in that sense deviates from the ideal of declarative programming which is there in what we sometimes call as pure logic programming where the order in which you write rules do not matter but if you have written programs in prologue especially for example this is written recursive programs you would know that you would need to write the base Clause first and then the recursive Clause because it's a prologue program always searches from top to down which translates to that first search in the Android tree that it creates so in that sense the prologue that we work with is not an ideal declarative programming language it has elements of user defined control which means which statement should be executed considered first essentially which is what Kowalski wanted to get away with goalski said that a program is equal to logic plus control the user must give us a logic and the influence engine will give us the control essentially but real implementations of prologue do not do that now coming to what we actually use in Ops 5 ah one of the conflict resolution strategies we use a specificity and what does that say it says that of all the rules that have matching instances that is the conflict set choose that instance of the rule that is the most specific essentially what do we mean by that specificity can be measured in terms of the number of tests that patterns in the rules need so remember that a rule has got a left hand side ah left hand side has got a set of patterns each pattern has got set of attributes to be tested so either they will match a constant or a variable or they will certify some condition and so on but each attribute in the rule has is a test essentially so if you simply count the number of tests that a particular rule has that will give you some idea of specificity a more specific rule would have more tests a less specific route would have less test essentially the intuition here is that the more specific the conditions of a rule So when you say conditions of rule basically we mean more specific more than the number of tests are the more appropriate the rule is likely to be in a given situation essentially so essentially what we are saying is that if there are two rules which are let us say matching and they are similar both of them match with the same data but one of the rules is more specific than another then use that rule essentially so remember that the working memory models the short term memory of the problem solver it actually contains all the data that the problem solver knows whereas role constitute the problem solvers knowledge which decides in the long term memory so it may have rules for specific situations it may have rules for General situations also we will see an example using the strategy of specificity can facilitate what we call as default reasoning essentially and the standard example if you look up default reasoning for example if you search on the net would be an example of a bird called Tweety and the situation is like this that if you know that Twitty is a bird you can go ahead and make an inference that VT can fly so that's like a default rule essentially but if you know something more if you know that Tweety is a bird but Tweety is a penguin or Twitty is in a cage or Tweety is injured or Twitty is dead if you know something else then you can override the in the default inference that PT can fly by a more specific influence so I have taken an example from my favorite domain which is the game of contract bridge and default rules happen naturally there without going into the details of the game basically you can either in the bidding phase you can either pass or you can make some bid so you are bidding for a contract in the game that's why the game is called contract which the default rule is simply that follows if you look at the pattern then the first pattern is simply saying that it is the turn of player X to make a bit and the second pattern simply is a kind of a device so as I had said Ops 5 programming is encapsulates not only programming and control but also heuristic knowledge and stuff like that so the second pattern is simply saying that after X has made a bit then the next layer two bit would be a player called White so essentially there are four players typically we call them north east south west and we say if North is to bid now next will be East to bid next will be so this four patterns will tell you what's the order in which the game proceeds and if there is nothing else known all you are saying is that the player has to make a bit then the default bid is to width which is called pass say I am not bidding for a contract maybe you do not have good cards or whatever but if you have a better hand so that better hand again in bridge Terms has got some value in terms of strength and it has got some shape so then there is a bit called one NT which stands for one note lump if no one else has been anything so you can see that the first pattern is the same in both the rules essentially that its the turn of player X to bit the second pattern is also the same in both the rules essentially that after X Y will bid essentially but now there is a third pattern in this rule which we have called as one n opening one n and 190 Sam for the same thing and it says that if the player is X and its got either 15 points or 16 points or Seventeen points remember that this is a disjunction and its shape can be classified as balanced and also that there is no record that player y has bid in round one essentially nobody else has bit so far then you can make this bid ah in this round one and you can call it an opening bit so this is some Bridge terminology and the build name is no Trump and the denomination is one so that's where one no term comes from essentially the point I am trying to make here is that there can be many such rules as to what is the bid that a player can make and if you use specificity then the first rule will fire only if none of the other Rules match if any of the other routes for example one note from opening or one Spade opening or one Diamond opening matches then the password will not fire so it is a default rule if you are using specificity as a conflict resolution strategy then this bidding program will work correctly so as an exercise ah I would ask you to write the rules to conclude that birds fly but not if they are for example Penguins essentially so you will need to decide as to what is the class that you will use say for example what is the how will you structure your working memory elements and what will the rules but the rules will be simple first we will say if x is a bird then X flies the second load will say if x is a birth and X is a penguin then X does not fly but just try to write that in an upside like language so default reasoning is something that humans do all the time essentially so if I were to finish this ah class I am kind of assuming by default that it will not be leaning when I go out so which is good because I have not brought an umbrella but we do all this kind of things all the time essentially as I said specificity this strategy of conflict resolution selects the most specific rules when both the rules are matching and you can see that in this example whenever rule 2 matches rule 1 will always match because they have it has a subset of the conditions of rule two now there is another strategy called recency what does this say this says that of all the rules that have matching instances choose the instance that matches the most recent working memory element what and we have already mentioned this earlier that we have a time stamp associated with working memory elements and by most recent we would mean the one with the highest time timestamp and what would that be that would be the element that has been added last to the working memory because you are increasing order of time stamp you are adding this thing so this rule says that if there are many rules which are matching then look at the timestamp of the working memory in each rule and whichever has a matching working memory element which is the latest choose that rule essentially so it can be implemented by looking at the timestamps of the matching working memory elements this is what I just said the intuition behind recency is that when a problem solver is reasoning or thinking or planning or whatever searching when it adds a new element to the working memory element then any rule that matches that working memory element which has just been added should get priority that is the intuition behind this recency strategy so it can facilitate what you can think of as a chain of thought that you may you draw some conclusion then use that conclusion to draw another conclusion use that to draw another conclusion and so on that can be captured by recency how do you implement this ah you simply can maintain the conflict set base as a priority queue that as we will see this conflict set as we will see soon this conflict set is not something that you recompute at every stage we will see we will see it's something which is kind of alive to which you add and delete elements essentially and you can do that right ah maintaining it as a priority queue the last strategy that we want to mention is called mean sense analysis and ah which we have already spoken about a few times in this course the implementation of language like Ops 5 from CMA has a strategy called me a mean sense analysis based on the problem solving strategy espoused by Simon and Newell so remember what means and Analysis is that look at the differences between the current state and the desired State and order the differences in terms of The Operators that we can use to resolve those differences and select the largest difference first we have seen examples of this the idea in OBS wave is to partition the set of rules based on the context and in Ops 5 the context is set by the first pattern in the rule essentially so you set the context which tells you that the first pattern sets the context then you focus only on those rules ah which match those context all those rules where the first element is matching will be considered first and of course the contexts are ordered in terms of differences so one can think of each partition as solving a specific sub goal or reducing specific difference that we say there may be more than one way of reducing a specific specific difference for example ah we consider this task of going from from let us say Chennai to um parashar Lake in Himachal Pradesh then one of the largest differences that you should be as close to Himachal as possible and we said that ok there is a difference between Chennai and Delhi and resolve that but that could be resolved in different ways you could take a train or you could take a flight or you or if you are adventurous enough you could even think of driving but there may be more than one way of reducing a specific difference and as I said the context is set by the first pattern in a rule essentially and in that sense a user can control so you can see some elements of programming coming here as well all rules in the same partition have the same first pattern essentially what mea does is that it applies recency to the first pattern remember that the first pattern sets the rule sets the context so the latest context that you have set you address that this is the largest difference that I want to resolve but of all the other rules which are matching which match the first pattern for them for this subset of rules for this partition of rules use specificity for resolving ah no conflict essentially so so it is a combination of recency and specificity as you can see which they call as main sense analysis next we will look at this algorithm which I have mentioned called writtenet which is an efficient implementation of a forward chaining rule based system