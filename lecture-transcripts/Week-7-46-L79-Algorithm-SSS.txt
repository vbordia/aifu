foreign works so let us now move on to looking at the algorithm itself and this algorithm that we are looking at is an iterative version and it works in three phases as we will see so in the initial phase you start by adding the root node to the priority queue its the best first version so it will maintain a priority queue and we will continue till the root is solved and it and it is at the head of the priority Queue at that point when the root is solved and at the head we will terminate so every node in our priority queue will have two possible values so it will be either solved or it will be live essentially so we start off by saying that root is a live node we still need to work on it but at some point when root will become solved then we will be able to terminate in the sense that we have we know the value Minimax value of the root essentially the Minimax value is stored as a third parameter here we start off by saying infinity or which is equal to saying plus large that plus large is the limit for Max essentially the plus Stars corresponds to win if you remember that it can go as high as a win essentially and then of course we will see if that is really the case so every node will be represented as a triple what is the triple it contains three values one is n which is a node itself then it has a status which we have seen will be either live or it will be solved and the third is its value which we will call as the H values actually so at the point when the root is labeled solved and it is the best node which means it is at the head of the priority queue we will return terminal the algorithm and return the value h but we started off by saying that we will add root as live and infinity to or plus large to the priority queue so we take out one one node at a time so initially what happens we put this root node with live and plus large or Infinity into the priority query and we extract it right so we pop the element at from the open which is the priority queue if the status of that node is live then we do the following now this we are entering the second part we will look at this again in a bit more detail ah in the forward phase we add live nodes till we reach the Horizon essentially and what is happening here ah if the node that you have popped from the queue is a live node then we check is it a terminal node if it is a terminal node then make it solved because we are not going to look Beyond the Horizon and its value will become the lower of the value that we pass to it and the evaluation function so remember that the value we are passing to it is infinity and therefore obviously its value will now come down to the value returned by the evaluation function so in this way we reach the terminal nodes if it is not a terminal load that means if it is an internal node we will look at this in more detail is that if it is a Max node then for each child C of that node we add that child to the priority queue so remember this is the process of of computing the Clusters at at max level choose all children essentially and of course give the same same upper bound as it that was passed to it essentially but at the main level you just choose one child and by without loss of generality we will choose the first style of n and all these nodes will be live nodes essentially so we will keep adding these live nodes which means we will keep going down the tree and building the Clusters so as long as the the node we are popping is live we'll keep going down the tree and adding more live nodes till the point where we reach the horizon or The Terminal nodes at that point we give evaluate those nodes and put their value into those nodes actually so after a certain point all the nodes on the horizon would have been accounted for in the in the Clusters and all of them would be solved because we would have applied the evaluation function to them so the next time that you pop a node so remember that you are still popping a node the next time we do that it will be a solved node essentially by by now all the values have come down from Infinity to the particular specific evaluation functions so the best node will be at the head of the queue and it will be a solved node essentially then what do we do ah if N is a solved node ah we go we identify its parent p and if N is a Max node and N is a last child that means all the siblings have been evaluated then we add the parent as a small node to open essentially if n is not the last cell and it is a Max node then we add the next style of P to open this corresponds to the recursive call that I was talking about where once you have solved the max node you recursively solve its sibling essentially so this will is taken care of here and if N is a Min node then we simply label its parent as solved because n is not only its a Min node its a small node and inside the head of the cube so its solved so all its siblings would be only lower than what this value has and therefore its parent can be labeled as solved essentially and it is added as salt to open and also we remove all successes of this parent from open again so we will see this in more detail so there are two phases in the forward phases we construct the Clusters ah and live nodes till we reach the Horizon only evaluate it at the horizon in the backward phase when we are looking at solved nodes we apply the backup Rule and we keep refining these things so this is just the same algorithm in the in three separate parts so the initial stage is what add the root node as a live node with a value Infinity this thing and at every s in every cycle of this Loop we are going to pop the node from the open which is a priority queue which means the node with the highest H value will come out essentially if the node which comes out is the root so remember this n is the one that is being popped if n is equal to is the root and it is solved then we will terminate the algorithm and return the value of H so that is the termination Criterion the second phase which is the forward phase again we have popped this thing remember we are in this Loop in every cycle we are doing this in the forward phase we will pop a live node essentially so we are checking here if the status is like and for terminal loads invoke eval n and choose the minimum of the bound what is the bound the bound is H that is coming from the top and Val n is the value that we get from the evaluation function remember that this bound H was Infinity to start with essentially so obviously if a leaf node has a value lower than infinity then that should be its value and that is what we are doing by saying that choose the minimum of these two values that is when you reach the terminal nodes actually you reach the Horizon if you are not in the Horizon then you are still constructing the Clusters and the strategy for constructing the Clusters was that all children of Max and one child of mean so if N is a Max note then for each child C you add C to open necessarily otherwise N is a Min node just add the first child to open essentially in this manner we will keep going down towards the horizon and at the Horizon we will also replace them by solved nodes with the evaluation function value so this is a forward phase in which clusters are formed and finally the backward phase which starts from line 15 of our algorithm again we have popped this node n and if the status of this n is solved and that will be the case so remember that in the forward phase we will go down to the horizon then we will evaluate every node on the horizon replace the value with the evaluation function value and call them as solved nodes so at the end of the first forward phase we have a set of solved nodes each with their evaluation function values actually but they are organized in a priority queue so the highest value node comes out first essentially now the node that we we extract is a solid node at that stage from The Horizon and we identify its parent as p and then we say if that N is a Max node and N is the last child essentially what does this mean that you let us say evaluated this as thirteen then its sibling you you evaluated as fifteen then its sibling you evaluate it as let us say 10 and let me introduce a negative number here as well just to remind you of the fact that there could be negative numbers so I have a habit of writing only positive numbers in these examples so what is happening uh this is the parent p and these are all Max nodes okay and these are all evaluated so let me use a double line to represent the fact that they are evaluated they are solved essentially that we know their value exactly easily which is true at the horizon so we are popping this these these nodes ok so what happens ah let us say we pop this one first thirteen OK the other nodes are not yet there essentially if Max so look at this this second case here so it is a Max node but it is not the last child so if so this is the max node lets say 13 that we pop but it's not the last style of its parent so what do we do we add the next child of that parent as a live node that means we add this as a live node essentially you know we will add this as a live node then if you pop it again it will be a life node on the Horizon and it will become a solid node which is fifteen so in that manner we will pop this then we will add this and so on and in the end we will add one more node and let us say that is the one that is popped out once that happens it is this case it is the last child if the node that we have popped remember that we always profit the node with the highest H value it is a Max node so this test is applied here and it is a last node which is also a test which is applied here in which case what we say is that if minus n is the best that we are looking at then we add the parent which is the main parent and we say it is solved and its value is in this example minus 10 essentially and we added to open individually so when you are looking at solved nodes there are two two cases one is when it is a Max node and the other is when it is a Min node essentially when it is a Max node it depends on whether it's a last node of its present or not the last node of its parent if it is the last node of its parent then we say we have solved the pivot if it is not the last node of its parent then we say OK solve The Sibling of that Max node and then we will decide what is happening that's one case the other case is if N is a Min node essentially so just imagine that let us say this is got its value minus 10 or something like that okay and let us say this is the one that is pop next so we have a Min node whose value is minus 10 and it is a solved node and we have popped it from the queue essentially now the the key thing to remember is that when we have popped this from the queue from the priority queue it means it is a value node with the highest value because the priority queue has always the best node as its head essentially so if we have popped this node from the priority queue and it is a solved node it is a Min node and it is a solid node remember these two properties are there it is a Min node and it is the solid node and we are popping it from the queues actually so Min because it is a circle in my notation and solved because it got a double circle essentially so we pop it now what happens supposing there were other main children of this node other main siblings of this node those siblings would have a value of less than minus 10 because minus 10 is at the head of the priority queue and there is a Max parent sitting here OK this Max parent says ok I am getting the the best value I can get from its mean children is minus 10 why why do I again repeat this the best value because it is at the head of the priority queue and we extracted the node from the head of the priority queue if the if the value of its best child is minus 10 then the other children need not be looked at all essentially and then we can say add this as a solid load so add P the parent as solved with the same value which is minus 10 in this example to open um this is the backward phase remember that we are propagating these values of the essentially what is more you remove all successes of P from open what are the successes these three nodes that we have done we do not want them anymore their values are known to be less than minus 10 because minus 10 was at the head of the priority queue so we can just remove them and we can say we its Max parent is solved essentially so in either case if you are looking at a means all node its simpler its you can simply say its Max parent can be labeled as solved because that is the best value that we could have got if it is a Max node that you are looking at this all as a solid no there are two cases is it the last node if it is the last node then there are no more children and its parent can be labeled as short if it is not the last node then we will add its ah siblings to the priority queue and you know refine them and this is what we called as a ah recursive call we we call it a recursive call but in practice in this iterative version this is how it happens because remember that when we added the sibling then we created clusters and think everything all that was happening so let us look at this process in more ah in a little bit detail oops so anyway what I have written here is that if Max node is not the last style then add sibling else for both Max and Min you add the parent as sold which is what I was explaining just now essentially now just a programming note if you are implementing this SSS star algorithm we keep saying if it's a Max node do this if it's a Min node do that how do we identify that essentially so one way that you could do that is by adding another parameter to the ah represents instead of making it a three Tuple or instead of making it as a triple which had root and status and H now you add this fourth parameter called player essentially so it is a its a node in the name of the node which is n in this case ah and whether it is a Max node or a Min node that additional information you can add so that this is the kind of check you can do that instead of saying it's a Max node you can say if player is equal to Max or something like that or if player is equal to Min which is just a programming note so let us again go through this process ah first we initially add the root node as a live node with a value of plus large to open essentially so our priority queue has only one node in it essentially then we follow the algorithm what does the algorithm say pop this node from the priority queue if it is a live node and if it is a Max node and if it is not a terminal load then add all its children that's what happens in the next this thing so we pop the top element which is the root element so this cross simply says that it is no longer there in the priority queue and it is a Max node and a live node we add all its children as as live nodes to the priority queue so these three nodes now have gone into the priority queue all of them have a h value of plus large because that's what we have started it this plus large value will change only when we hit the Horizon when we hit the Horizon at that point we will call the evaluation function for a particular node and then we will get its real value so for the moment we are saying it has an upper bound of plus large then the same thing happens at the next level the next three elements will be popped are live nodes and these are main nodes with a value of plus large for each of these Min nodes we add one child which is a live child with a value of plus large at the Horizon things change because the test of its if it is a terminal node then and it is a live node then replace it with its valuation function value or at least a minimum of H and evalu N value and that is what happens in this example here ah we have in this slightly bigger game three we have six initial clusters essentially and if you look at the thick arrows you will see that those arrows represent how those clusters were formed at the max level we chose all children at the main level we chose one child in particular we chose the leftmost side so they all those all the nodes that you see this six nodes will go into the priority queue only this sixth note the rest of the three is just for our reference this six node will go into the priority queue these six nodes are terminal nodes and they are live nodes